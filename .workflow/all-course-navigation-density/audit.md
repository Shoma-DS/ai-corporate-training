# 全講座ナビゲーション・図解密度監査メモ

- 実行日: 2026-06-29
- 対象ルール: 各回S03付近の `目次/全体像`、各章開始の `章見出し/現在位置`、高密度テキスト入りラスター図解
- 公開安全: Drive URL、APIレスポンス、顧客情報、価格、個人情報は記載しない

## GAS講座で実施済み

対象: `講座/生成AI・GASで実践する業務変革・DX推進講座/`

実施内容:

- 6回すべての `スライド案.md` に、S03 `目次/全体像` を明確化。
- 各時間配分ブロックの開始位置へ `章見出し/現在位置` スライドを追加。
- 既存説明スライドの本文密度は落とさず、S番号を新構成に合わせて再整理。
- `scripts/build_editable_google_slides_sources.py` を再実行し、各回の `Googleスライド編集用アウトライン.md` と `図解パーツ生成プロンプト.md` を更新。
- 付録セクションやMarkdown区切り線が編集用アウトライン/図解プロンプト本文へ混ざらないよう、生成スクリプトも修正。

| 回 | 新スライド数 | 目次/全体像 | 章見出し/現在位置 | アウトライン/図解プロンプト |
|---:|---:|---|---:|---|
| 1 | 49 | S03 | 6枚 | 49/49一致 |
| 2 | 45 | S03 | 5枚 | 45/45一致 |
| 3 | 50 | S03 | 6枚 | 50/50一致 |
| 4 | 46 | S03 | 6枚 | 46/46一致 |
| 5 | 46 | S03 | 6枚 | 46/46一致 |
| 6 | 47 | S03 | 7枚 | 47/47一致 |

## GAS講座の図解PNG状態

`python3 scripts/check_diagram_integrity.py` はまだ失敗。今回S番号が増えたため、既存 `図解パーツ/Sxx.png` は完成版として扱わず、Codex App Server / GPT image 2 / `imagegen` で再生成・検品が必要。

直近結果:

| 回 | 状態 |
|---:|---|
| 1 | 43/49 needs_fix。少なくとも S44-S49 が未生成 |
| 2 | 40/45 needs_fix。少なくとも S41-S45 が未生成 |
| 3 | 24/50 needs_fix。少なくとも S25-S32 が未生成 |
| 4 | 3/46 needs_fix。少なくとも S04-S11 が未生成 |
| 5 | 9/46 needs_fix。少なくとも S10-S17 が未生成 |
| 6 | 7/47 needs_fix。少なくとも S08-S15 が未生成 |

重複PNG:

- 第5回 `図解パーツ/S09.png` == 第6回 `図解パーツ/S07.png`
- 第3回 `図解パーツ/S08.png` == 第4回 `図解パーツ/S03.png`
- 第1回 `図解パーツ/S03.png` == 第5回 `図解パーツ/S03.png`

対応ルール:

- 既存PNGの上から文字を載せて修正しない。
- SVG/HTML/CSS/canvas/PIL/ImageMagick/ブラウザスクリーンショットで代替PNGを作らない。
- `図解パーツ生成プロンプト.md` の各S番号から、読める日本語テキスト入りの横長・やや短めラスター資料画像として再生成する。
- `check_diagram_integrity.py` が通るまで、Google Slides本番書き出しは行わない。

## GAS講座以外の監査結果

GAS以外は未修正。今後修正する場合は、GAS講座の今回の構成を密度ベンチマークにしつつ、各講座固有の業務課題・成果物・事例へ置き換える。

| 講座 | セッション数 | 現状 |
|---|---:|---|
| AIエージェント実装・連携設計アカデミー | 6 | 各回40枚の標準S番号あり。ただし `目次/全体像` と `章見出し/現在位置` は未反映 |
| 中小企業AIエージェント導入・定着化実践講座 | 6 | 各回40枚の標準S番号あり。ただし `目次/全体像` と `章見出し/現在位置` は未反映 |
| AX・DXワークショップ講座 | 2 | 第1回52枚、第2回64枚。合格基準PDFを密度参照しつつ、目次/章見出しの追加が必要 |
| AIエージェントに仕事を任せる業務改善・DX推進講座 | 6 | `スライド案.md` はあるが標準 `### Sxx` 見出しとしては未整備。目次/章見出し以前に標準形式化が必要 |
| 社内ナレッジ活用・根拠確認DX推進講座 | 6 | `スライド案.md` はあるが標準 `### Sxx` 見出しとしては未整備。目次/章見出し以前に標準形式化が必要 |

## 次にやるべきこと

1. GAS講座の `図解パーツ/Sxx.png` を、更新後のS番号に合わせて全不足・重複分から再生成する。
2. 生成後は `view_image` 低負荷確認、OCR、`file`/`shasum`、`check_diagram_integrity.py` で検品する。
3. 欠番・重複・薄い情報量が消えてから、`--embed-diagram-parts` と `--make-diagram-images-readable-by-link` 付きでGoogle Slidesへdry-run、問題なければ本番書き出しへ進む。
4. GAS以外の講座は、このメモを出発点にスライド案を標準形式化し、S03目次と章見出しを追加してから図解プロンプト/画像生成へ進む。
