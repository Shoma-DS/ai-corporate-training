interface:
  display_name: "Course Workflow Coordination"
  short_description: "Downstream helper for supervised packets, subagents, and approvals inside the course-production workflow."
  default_prompt: "Use this only as a downstream coordination helper when corporate-training-course-builder needs explicit packets, subagents, approvals, or reusable orchestration notes. Do not use it as the entrypoint for 講座作成 or as a separate course-production standard."
