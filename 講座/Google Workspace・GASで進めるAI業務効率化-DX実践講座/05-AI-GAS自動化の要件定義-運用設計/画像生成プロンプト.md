# 第5回 画像生成プロンプト

対象講座: Google Workspace・GASで進めるAI業務効率化/DX実践講座。

対象スライド: S01-S40

採用テンプレート: `soft-isometric-corporate-warm`

第5回は、AI/GAS自動化を現場で止めずに運用するための要件定義・運用設計の回。全スライドで白背景、ネイビー・ブルー・グリーン、薄いグレー罫線、カード型、柔らかい人物イラスト、淡い角丸カード、読みやすい実務図解を統一する。実在ロゴ、実在UI、実在サービス画面は画像生成AIに記憶で描かせない。

## 共通ルール

- 画角は16:9、1920x1080想定。
- 使用テンプレートは全スライド `soft-isometric-corporate-warm` に固定する。
- 日本語テキストは大きく、読みやすいゴシック体風にする。
- 画像内テキストは各スライドで指定した文言だけにする。長文、余計な英語、謎文字、ダミー文字列は入れない。
- 実在Googleロゴ、Google Workspaceロゴ、Google Meetロゴ、Google Apps Scriptロゴ、Geminiロゴ、実在UI、実在サービス画面は生成しない。
- 公式ロゴが必要な説明でも、生成画像内にロゴ枠、素材配置枠、公式ロゴプレースホルダーを作らない。生成画像では抽象アイコンで表現する。
- 会社名、顧客名、社員名、メールアドレス、価格、連絡先、契約情報、APIキー、社内固有情報は入れない。
- 画面共有スライドは図解パターン `screen-share-transition` とし、操作画面の偽UIではなく、抽象モニターと確認カードで見せる。

## 共通ベースプロンプト

```text
16:9、1920x1080の法人研修用スライド画像を作成。soft-isometric-corporate-warm の白背景、ネイビーの大見出し、ブルーとグリーンのアクセント、薄いグレー罫線、控えめな影、カード型整理、柔らかい人物イラスト、淡い角丸カード、読みやすい実務図解、実務的で読みやすいビジネス資料デザインで統一する。日本語テキストは大きく正確に、読みやすいゴシック体風で配置。指定した日本語以外の文字は入れない。実在ロゴ、実在UI、実在サービス画面、実在企業名、人物名、メールアドレス、価格、連絡先、QRコード、透かしは入れない。抽象的なフォーム、台帳、ファイル、ログ、AI補助、確認者、盾、チェックマーク、ワークフロー矢印で表現する。
```

## 共通ネガティブプロンプト

```text
文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、長文の詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでのGeminiロゴ再現、参照画像なしでのUI再現、実在サービス画面、実在企業名、人物名、メールアドレス、価格、連絡先、QRコード、透かし、写真風人物、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、過度な3D、雑然としたレイアウトを避ける。
```

## Slide S01. 自動化は作って終わりではない

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `summary-takeaway` / まとめ・持ち帰り型
- 画像内テキスト: `第5回`、`自動化は作って終わりではない`、`要件定義・運用設計`、`ログ`、`例外対応`、`改善`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。表紙スライド。左側に大きく「第5回」「自動化は作って終わりではない」「要件定義・運用設計」。右側にアイソメトリックな継続運用ループを配置し、ループ内に「ログ」「例外対応」「改善」を大きく表示。台帳カード、自動化ブロック、確認者アイコン、チェックマークを組み合わせる。落ち着いた最終回の導入デザイン。
```

## Slide S02. 要件定義で決めること

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `checklist-review` / チェックリスト・レビュー型
- 画像内テキスト: `要件定義で決めること`、`対象業務`、`利用者`、`入力元`、`出力先`、`GAS処理`、`AI処理`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「要件定義で決めること」。中央に大きな設計チェックリストを置き、6つのカード「対象業務」「利用者」「入力元」「出力先」「GAS処理」「AI処理」を整列。右下に小さな確認者アイコンとペン。要件を先に決める実務感を出す。
```

## Slide S03. 自動化方式の選び方

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `自動化方式の選び方`、`関数`、`手動AI`、`Gem/Gemini`、`GAS`、`トリガー`、`頻度`、`リスク`、`確認コスト`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「自動化方式の選び方」。中央に柔らかい横フローを配置し、左から抽象的な業務カードが入り、5つの方式カード「関数」「手動AI」「Gem/Gemini」「GAS」「トリガー」へ分岐する。下部に3つの判断基準カード「頻度」「リスク」「確認コスト」。全部自動化ではなく選ぶ印象。実在アプリ風の色やGoogle系アプリアイコンに見える表現は避ける。
```

## Slide S04. フォーム送信・通知・帳票

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `フォーム送信・通知・帳票`、`受付`、`番号発行`、`通知`、`保存`、`帳票`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「フォーム送信・通知・帳票」。中央に5ステップの横フロー「受付」「番号発行」「通知」「保存」「帳票」。抽象フォームカード、台帳カード、メール風通知カード、Drive風ファイル箱、帳票カードをアイソメトリックに描く。外部送信は人の確認がある雰囲気としてチェックアイコンを添える。
```

## Slide S05. Meet文字起こしを入力にする設計

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `Meet文字起こしを入力にする設計`、`文字起こし`、`GASで読む`、`AIで抽出`、`アクション台帳`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「Meet文字起こしを入力にする設計」。中央に4ステップの横フロー「文字起こし」「GASで読む」「AIで抽出」「アクション台帳」。実在Meetロゴや実画面は使わず、会議メモ文書カード、歯車ブロック、AI補助キューブ、表形式台帳カードで表現。Meet終了の自動検知に依存しない設計を、フォルダ入力と手動実行ボタン風カードで示す。
```

## Slide S06. 担当者別リマインドと議事録整形

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `data-table-checkpoints` / データ表・確認観点型
- 画像内テキスト: `担当者別リマインドと議事録整形`、`期限`、`担当者`、`リマインド案`、`議事録`、`確認`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「担当者別リマインドと議事録整形」。中央にアクション台帳の抽象表カードを置き、列見出しとして「期限」「担当者」「リマインド案」「議事録」「確認」を大きく表示。右側に確認者がチェックするアイコン。自動送信ではなく確認後に進める落ち着いた構図。
```

## Slide S07. 情報管理と権限

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `caution-risk-check` / 注意喚起・リスク確認型
- 画像内テキスト: `情報管理と権限`、`個人情報`、`AI入力禁止`、`共有範囲`、`編集権限`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「情報管理と権限」。中央に盾とチェックリスト、周囲に4つのカード「個人情報」「AI入力禁止」「共有範囲」「編集権限」。鍵、注意三角、アクセス制御、確認スタンプの抽象アイコンを配置。怖すぎないが、管理の重要性が伝わるスライド。
```

## Slide S08. GASの制限

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `caution-risk-check` / 注意喚起・リスク確認型
- 画像内テキスト: `GASの制限`、`実行時間`、`送信数`、`トリガー`、`同時実行`、`公式で確認`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「GASの制限」。中央にダッシュボード風カードと警告アイコンを配置し、5つの確認カード「実行時間」「送信数」「トリガー」「同時実行」「公式で確認」を大きく表示。具体的な数値は入れない。処理を分割する小さな矢印とキューの抽象図を添える。
```

## Slide S09. ログとAI出力レビュー

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `data-table-checkpoints` / データ表・確認観点型
- 画像内テキスト: `ログとAI出力レビュー`、`実行日時`、`処理結果`、`要確認`、`確認者`、`修正メモ`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「ログとAI出力レビュー」。中央に2つの大きな表カードを並べる。左はログ表で「実行日時」「処理結果」、右はAI出力レビュー表で「要確認」「確認者」「修正メモ」。虫眼鏡、チェックマーク、修正ペンのアイコンを置き、後から追える設計を表す。
```

## Slide S10. テスト観点

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `checklist-review` / チェックリスト・レビュー型
- 画像内テキスト: `テスト観点`、`正常系`、`空欄`、`重複`、`AI誤分類`、`権限ミス`、`通知漏れ`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「テスト観点」。中央に大きなテストチェックリストを配置し、6つの項目「正常系」「空欄」「重複」「AI誤分類」「権限ミス」「通知漏れ」を大きく整列。右側にテスト担当者の抽象アイコンとチェック済みのカード。品質確認の実務感を出す。
```

## Slide S11. Gemini連携利用不可時の代替設計

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `before-after-cards` / Before/Afterカード型
- 画像内テキスト: `連携利用不可時の代替設計`、`Sheets台帳`、`Docs本文`、`手動貼り付け`、`GAS手動実行`、`止まらない運用`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「連携利用不可時の代替設計」。左右比較。左は連携機能が使えない状態を淡い注意カードで表現。右は「Sheets台帳」「Docs本文」「手動貼り付け」「GAS手動実行」をつなぐ代替フロー。下部に「止まらない運用」を大きく表示。実在Geminiロゴや実画面は描かない。
```

## Slide S12. 定着運用

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `定着運用`、`担当者`、`引き継ぎ`、`改善会議`、`更新ルール`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「定着運用」。中央に4段階の継続運用ループ「担当者」「引き継ぎ」「改善会議」「更新ルール」。周囲にログ表、運用設計書、会議カード、更新履歴カードの抽象アイコン。自走できる運用に育てる落ち着いた印象。
```

## Slide S13. 演習: 運用設計書とリスクチェックリストを作る

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `workshop-instruction` / ワーク指示・演習導入型
- 画像内テキスト: `演習`、`運用設計書`、`リスクチェックリスト`、`要件定義メモ`、`現場へ持ち帰る`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。演習導入スライド。左側に大きく「演習」。中央に3つの成果物カード「要件定義メモ」「運用設計書」「リスクチェックリスト」。右側に受講者がチェックリストを書き込む抽象アイコン。下部に「現場へ持ち帰る」を大きく配置。最終成果物が明確に見える構成。
```

## Slide S14. 作ったけど止まる典型パターン

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `before-after-cards` / Before/Afterカード型
- 画像内テキスト: `作ったけど止まる典型パターン`、`担当者不在`、`権限不足`、`例外未定義`、`ログなし`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「作ったけど止まる典型パターン」。左右の大きなカード構成。左は散らかった処理カードと止まった矢印、右は見直し前に確認する4つのリスクカード「担当者不在」「権限不足」「例外未定義」「ログなし」。赤は注意アクセントに抑え、現場で起こりがちな停止リスクを実務的に表現する。
```

## Slide S15. 要件定義は現場の約束事

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `role-card-set` / 役割カード型
- 画像内テキスト: `要件定義は現場の約束事`、`誰が`、`何を`、`どこまで`、`どう確認`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「要件定義は現場の約束事」。中央に4つの役割・合意カード「誰が」「何を」「どこまで」「どう確認」を横並びで配置。複数の担当者が同じ運用設計書を見て合意している抽象イラスト。契約書や実名のような表現は避け、社内の約束事を穏やかに表す。
```

## Slide S16. 対象業務を1文で絞る

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `workshop-instruction` / ワーク指示・演習導入型
- 画像内テキスト: `対象業務を1文で絞る`、`誰の業務`、`何を改善`、`何のために`、`除外範囲`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。ワーク導入スライド。上部にタイトル「対象業務を1文で絞る」。中央に大きなワークシート風カードを置き、4つの記入欄「誰の業務」「何を改善」「何のために」「除外範囲」を大きく表示。鉛筆、チェック、付箋の抽象アイコンを添える。実在の会社名や業務名は入れない。
```

## Slide S17. 利用者と確認者を分ける

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `role-card-set` / 役割カード型
- 画像内テキスト: `利用者と確認者を分ける`、`入力者`、`処理担当`、`確認者`、`管理者`、`代替担当`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「利用者と確認者を分ける」。中央に5つの役割カード「入力者」「処理担当」「確認者」「管理者」「代替担当」を並べる。各カードに柔らかい人物アイコンとチェックバッジを置き、処理の流れを細い矢印でつなぐ。確認者だけが最終チェックを行うことが伝わる構図。
```

## Slide S18. 入力元を固定する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `folder-structure` / 保存場所・フォルダ構造型
- 画像内テキスト: `入力元を固定する`、`Forms`、`Sheets`、`Docs本文`、`Meet文字起こし`、`CSV`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「入力元を固定する」。中央にメインの入力フォルダを置き、下に5つの抽象入力カード「Forms」「Sheets」「Docs本文」「Meet文字起こし」「CSV」を接続する。実在ロゴや実在UIは描かず、フォーム、表、文書、会議メモ、CSVファイルをシンプルな抽象アイコンで表現する。
```

## Slide S19. 出力先を固定する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `folder-structure` / 保存場所・フォルダ構造型
- 画像内テキスト: `出力先を固定する`、`管理台帳`、`ログ`、`Docs議事録`、`通知案`、`保存フォルダ`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「出力先を固定する」。中央に保存フォルダの階層図を作り、5つの出力先カード「管理台帳」「ログ」「Docs議事録」「通知案」「保存フォルダ」を配置。各カードに鍵やチェックの小アイコンを添え、どこに残すかを先に決める印象にする。実在サービス画面は使わない。
```

## Slide S20. AIに任せる処理を限定する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `permission-role-matrix` / 権限・役割マトリクス型
- 画像内テキスト: `AIに任せる処理を限定する`、`分類案`、`要約案`、`返信案`、`アクション抽出`、`要確認`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「AIに任せる処理を限定する」。中央にAI補助の範囲を示すマトリクスを置き、列またはカードに「分類案」「要約案」「返信案」「アクション抽出」「要確認」を大きく表示。AIは案を出し、人が確認する流れを人物アイコンとチェックマークで示す。確定判断をAIが自動で行う表現は避ける。
```

## Slide S21. GASに任せる処理を限定する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `GASに任せる処理を限定する`、`転記`、`採番`、`抽出`、`保存`、`ログ`、`下書き作成`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「GASに任せる処理を限定する」。中央に6ステップの横フロー「転記」「採番」「抽出」「保存」「ログ」「下書き作成」。歯車、表、番号札、フォルダ、ログカード、下書き文書の抽象アイコンを使う。例外判断は人に戻る小さな確認カードを添える。
```

## Slide S22. 人が止めるポイントを決める

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `caution-risk-check` / 注意喚起・リスク確認型
- 画像内テキスト: `人が止めるポイントを決める`、`外部送信`、`承認`、`権限変更`、`AI確定値`、`例外判断`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「人が止めるポイントを決める」。中央に赤を抑えた注意バナーと5つの確認カード「外部送信」「承認」「権限変更」「AI確定値」「例外判断」。人物の手元に停止ボタン風の抽象アイコンと確認チェックを置く。怖すぎないが、勝手に進めない設計が伝わる構図。
```

## Slide S23. 方式選定はリスクで決める

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `data-table-checkpoints` / データ表・確認観点型
- 画像内テキスト: `方式選定はリスクで決める`、`頻度`、`失敗時影響`、`確認コスト`、`環境差`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「方式選定はリスクで決める」。中央に読みやすい判断表を置き、見出しとして「頻度」「失敗時影響」「確認コスト」「環境差」を大きく表示。右側に虫眼鏡を持つ確認者と、低・中・高を表す抽象ゲージを配置。具体的な数値、価格、制度条件は入れない。
```

## Slide S24. 画面共有: 要件定義ケースを読む

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `screen-share-transition` / 画面共有案内型
- 画像内テキスト: `画面共有`、`要件定義ケースを読む`、`対象業務`、`分担`、`確認ポイント`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。画面共有への切り替えスライド。中央に大きな抽象モニターを置き、実在UIやCSV画面を描かず、モニター内はシンプルな抽象カードとチェックだけにする。下部に3つの確認カード「対象業務」「分担」「確認ポイント」。上部に「画面共有」「要件定義ケースを読む」を大きく表示。スクリーンショット枠、ロゴ枠、素材配置枠は作らない。
```

## Slide S25. フォーム通知は小さく始める

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `フォーム通知は小さく始める`、`受付番号`、`確認メール`、`担当者通知`、`手動確認`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「フォーム通知は小さく始める」。中央に4ステップの横フロー「受付番号」「確認メール」「担当者通知」「手動確認」。抽象フォーム、番号札、通知カード、担当者カード、確認者のチェックを配置。最初から完全自動化せず、小さく始めて確認する温度感にする。
```

## Slide S26. 帳票保存は命名と保存先が命

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `folder-structure` / 保存場所・フォルダ構造型
- 画像内テキスト: `帳票保存は命名と保存先が命`、`ファイル名`、`保存フォルダ`、`閲覧権限`、`更新履歴`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「帳票保存は命名と保存先が命」。中央にフォルダ構造図と帳票カードを配置し、4つの確認カード「ファイル名」「保存フォルダ」「閲覧権限」「更新履歴」を大きく表示。鍵、履歴、ラベル、チェックの抽象アイコンを使う。実在Drive画面やロゴは描かない。
```

## Slide S27. Meet文字起こしは直接トリガーに依存しない

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `before-after-cards` / Before/Afterカード型
- 画像内テキスト: `Meet文字起こしは直接トリガーに依存しない`、`Drive上のDocs`、`配布テキスト`、`手動配置`、`手動実行`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「Meet文字起こしは直接トリガーに依存しない」。左右比較。左は会議終了後に自動連携を待って止まる抽象カード、右は「Drive上のDocs」「配布テキスト」「手動配置」「手動実行」を使う安定した入力フロー。Meetロゴや実在画面は描かず、会議メモ文書とフォルダの抽象アイコンで表現する。
```

## Slide S28. 画面共有: Meet文字起こしから台帳へ

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `screen-share-transition` / 画面共有案内型
- 画像内テキスト: `画面共有`、`Meet文字起こしから台帳へ`、`入力元`、`抽出`、`台帳`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。画面共有への切り替えスライド。中央に大きな抽象モニターを配置し、モニター内は実在画面ではなく文書カードから表カードへ流れる簡単な矢印のみ。下部に3つの確認カード「入力元」「抽出」「台帳」。上部に「画面共有」「Meet文字起こしから台帳へ」を大きく表示。実在Meetロゴ、実在Docs画面、実在Sheets画面、スクリーンショット枠は入れない。
```

## Slide S29. AI入力禁止情報を先に消す

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `caution-risk-check` / 注意喚起・リスク確認型
- 画像内テキスト: `AI入力禁止情報を先に消す`、`顧客名`、`社員名`、`メール`、`契約情報`、`未公開資料`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「AI入力禁止情報を先に消す」。中央に注意バナーと5つの情報カード「顧客名」「社員名」「メール」「契約情報」「未公開資料」を配置し、マスク処理や削除を示す抽象アイコンを添える。実在の氏名、メールアドレス、契約内容は絶対に入れない。赤は注意アクセントとして限定的に使う。
```

## Slide S30. 匿名化してから貼り付ける

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `匿名化してから貼り付ける`、`置換`、`削除`、`要約範囲`、`最小必要情報`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「匿名化してから貼り付ける」。中央に4ステップの横フロー「置換」「削除」「要約範囲」「最小必要情報」。左に原稿カード、右に匿名化済みカードを置き、個人情報を伏せる抽象マスクとチェックを表現する。実在データやメール形式の文字列は入れない。
```

## Slide S31. 権限は閲覧と編集を分ける

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `permission-role-matrix` / 権限・役割マトリクス型
- 画像内テキスト: `権限は閲覧と編集を分ける`、`閲覧者`、`編集者`、`管理者`、`外部共有`、`ログ閲覧`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「権限は閲覧と編集を分ける」。中央に権限マトリクスを置き、列またはカードに「閲覧者」「編集者」「管理者」「外部共有」「ログ閲覧」を大きく表示。チェック、ダッシュ、鍵の抽象アイコンで権限差を示す。実在共有設定画面やロゴは描かない。
```

## Slide S32. 画面共有: 権限と保存先を確認する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `screen-share-transition` / 画面共有案内型
- 画像内テキスト: `画面共有`、`権限と保存先を確認する`、`保存先`、`共有範囲`、`外部共有`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。画面共有への切り替えスライド。中央に抽象モニターと鍵付きフォルダ、チェックカードを配置。下部に3つの確認カード「保存先」「共有範囲」「外部共有」。上部に「画面共有」「権限と保存先を確認する」を大きく表示。実在Drive画面、共有設定UI、ロゴ、スクリーンショット枠、公式ロゴプレースホルダーは入れない。
```

## Slide S33. ログは後から責めるためでなく守るため

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `data-table-checkpoints` / データ表・確認観点型
- 画像内テキスト: `ログは後から責めるためでなく守るため`、`実行日時`、`処理件数`、`結果`、`エラー`、`再実行`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「ログは後から責めるためでなく守るため」。中央に大きなログ表カードを配置し、列見出し「実行日時」「処理件数」「結果」「エラー」「再実行」を大きく表示。盾、虫眼鏡、再実行矢印、確認者アイコンを添え、責任追及ではなく業務を守る記録として見せる。
```

## Slide S34. エラーを3種類に分ける

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `caution-risk-check` / 注意喚起・リスク確認型
- 画像内テキスト: `エラーを3種類に分ける`、`止める`、`要確認に出す`、`後で直す`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「エラーを3種類に分ける」。中央に3つの大きな判断カード「止める」「要確認に出す」「後で直す」を横並びで配置。赤の停止、オレンジの確認、青の後続対応をアクセントに使う。エラーが出たら全部失敗ではなく、分けて扱う判断設計を表現する。
```

## Slide S35. 手動復旧の5ステップ

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `soft-process-flow` / ソフトプロセス・フロー型
- 画像内テキスト: `手動復旧の5ステップ`、`気づく`、`止める`、`戻す`、`再実行`、`記録する`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「手動復旧の5ステップ」。中央に5ステップの横フロー「気づく」「止める」「戻す」「再実行」「記録する」。通知、停止、戻す矢印、再実行、ログ記録の抽象アイコンを配置。復旧手順が落ち着いて読めるよう余白を広く取る。
```

## Slide S36. 代替運用を先に書く

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `before-after-cards` / Before/Afterカード型
- 画像内テキスト: `代替運用を先に書く`、`連携不可`、`文字起こしなし`、`GAS不可`、`通知不可`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「代替運用を先に書く」。左右比較。左は想定外で止まる業務カード、右は代替手順が用意されている運用カード。右側に4つのリスクカード「連携不可」「文字起こしなし」「GAS不可」「通知不可」と、手動ルートの細い矢印を配置する。
```

## Slide S37. 引き継ぎは設計書で行う

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `role-card-set` / 役割カード型
- 画像内テキスト: `引き継ぎは設計書で行う`、`担当者`、`更新頻度`、`変更履歴`、`判断基準`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「引き継ぎは設計書で行う」。中央に運用設計書カードを置き、周囲に4つの確認カード「担当者」「更新頻度」「変更履歴」「判断基準」。前任者から後任者へ書類とチェックが渡る抽象人物イラストを添える。実在氏名や部署名は入れない。
```

## Slide S38. 画面共有: テストケースと復旧手順を作る

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `screen-share-transition` / 画面共有案内型
- 画像内テキスト: `画面共有`、`テストケースと復旧手順を作る`、`正常系`、`異常系`、`復旧`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。画面共有への切り替えスライド。中央に大きな抽象モニターを置き、モニター内はチェックリストと復旧矢印の抽象図だけにする。下部に3つの確認カード「正常系」「異常系」「復旧」。上部に「画面共有」「テストケースと復旧手順を作る」を大きく表示。実在CSV画面、表計算UI、スクリーンショット枠は描かない。
```

## Slide S39. 演習レビュー: OK/NGを判断する

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `screen-share-transition` / 画面共有案内型
- 画像内テキスト: `画面共有`、`演習レビュー`、`OK/NG`、`リスクチェック`、`対応メモ`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。演習レビューの画面共有切り替えスライド。中央に抽象モニターとレビュー用チェックカードを配置し、実在ワークシート画面は描かない。下部に3つの確認カード「OK/NG」「リスクチェック」「対応メモ」。上部に「画面共有」「演習レビュー」を大きく表示。講師記入例を実画面風に再現せず、自己レビューへ進む案内として表現する。
```

## Slide S40. 第6回への橋渡し

- 使用テンプレート: `soft-isometric-corporate-warm`
- 図解パターン: `summary-takeaway` / まとめ・持ち帰り型
- 画像内テキスト: `第6回への橋渡し`、`運用設計`、`リスク`、`KPI`、`ロードマップ`、`提案書`
- 生成プロンプト:

```text
共通ベースプロンプトに従う。まとめスライド。上部にタイトル「第6回への橋渡し」。中央に第5回成果物「運用設計」から第6回成果物「提案書」へ向かう穏やかなロードマップ矢印を配置。途中に「リスク」「KPI」「ロードマップ」の3カードを置く。受講者が書類束を持ち帰る抽象イラストを添え、次回につながる前向きな実務感にする。
```
