# 第6回 画像生成プロンプト

対象講座: Google Workspace・GASで進めるAI業務効率化/DX実践講座。

対象スライド: S01-S36

採用テンプレート: `soft-isometric-corporate-warm`

第6回は、受講者がAI業務効率化プロジェクト提案書を作成し、導入判断に必要な背景、課題、効果、リスク、運用、次アクションを説明できるようにする最終回。全スライドで、白または少し暖色寄りの背景、柔らかい人物イラスト、淡い角丸カード、太めのネイビー見出し、ブルー/グリーンのアクセント、控えめな注意色を統一する。

## 共通ルール

- 画角は16:9、1920x1080想定。
- 使用テンプレートは全スライド `soft-isometric-corporate-warm` に固定する。
- 日本語テキストは大きく、読みやすいゴシック体風にする。
- 画像内テキストは各スライドで指定した文言だけにする。長文、余計な英語、謎文字、ダミー文字列は入れない。
- 実在Googleロゴ、Google Workspaceロゴ、Google Meetロゴ、Google Apps Scriptロゴ、Geminiロゴ、実在UI、実在サービス画面は生成しない。
- 公式ロゴや実画面が必要な場合は、`素材/ロゴ/` や `スクリーンショット/` の取得済み素材を別途配置する。生成画像では抽象アイコンで表現する。
- 会社名、顧客名、社員名、メールアドレス、価格、連絡先、契約情報、APIキー、社内固有情報は入れない。
- 画面共有スライドでは、偽UIやスクショ配置枠を作らず、抽象的なモニターと3つの確認カードで切り替えを示す。

## 共通ベースプロンプト

```text
Create a 16:9 Japanese corporate training slide in a soft warm isometric vector illustration style. Use a clean white or slightly warm off-white background, large bold Japanese title area, gentle rounded cards, thin light-gray borders, soft shadows, friendly simplified people illustrations, clear tables or diagrams, navy headline text, blue primary accents, green success accents, red caution accents, and small orange or purple highlights only when useful. Make the slide practical, readable, calm, and business-friendly. Use generous white space and keep all in-image Japanese text short and large. No real brand logos, no real UI screenshots, no confidential information, no personal data, no pricing, no contact details.
```

## 共通ネガティブプロンプト

```text
Avoid dark backgrounds, decorative gradient blobs, glossy 3D realism, stiff geometric block-only isometric style, photorealistic people, comic style, handwritten style, cluttered layout, dense paragraphs, tiny unreadable table text, fake brand logos, fake real-service screens, real company names, real personal data, email addresses, API keys, pricing, contact information, official subsidy rates.
```

## Slide S01. 第6回 AI業務効率化プロジェクト提案書の作成

- 図解パターン: `summary-takeaway`
- 画像内テキスト: `第6回`、`AI業務効率化プロジェクト提案書`、`導入判断に使える形へ`
- プロンプト:

```text
共通ベースプロンプトに従う。表紙スライド。左側に大きく「第6回」「AI業務効率化プロジェクト提案書」「導入判断に使える形へ」。右側に、提案書、チェックリスト、KPIカード、ロードマップを机上に整理する柔らかい人物イラスト。温かく実務的な最終回の雰囲気。
```

## Slide S02. 提案書は導入判断の材料

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `提案書は導入判断の材料`、`背景`、`課題`、`効果`、`リスク`、`次アクション`
- プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「提案書は導入判断の材料」。中央に5ステップの柔らかい横フロー「背景」「課題」「効果」「リスク」「次アクション」。各カードを矢印でつなぎ、右端に判断する人の小さなチェックアイコンを置く。
```

## Slide S03. 作ったものの説明で終わらせない

- 図解パターン: `before-after-cards`
- 画像内テキスト: `作ったものの説明で終わらせない`、`作業報告`、`導入判断`
- プロンプト:

```text
共通ベースプロンプトに従う。左右比較。左カードは「作業報告」としてバラバラな成果物カード。右カードは「導入判断」として課題、効果、リスク、次アクションが整理された提案書カード。中央に柔らかい矢印。誇張せず実務的に。
```

## Slide S04. 今日完成させる3つの成果物

- 図解パターン: `checklist-review`
- 画像内テキスト: `今日完成させる3つの成果物`、`提案書骨子`、`KPI表`、`次アクションメモ`
- プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「今日完成させる3つの成果物」。中央に大きなチェックリストと3つの成果物カード「提案書骨子」「KPI表」「次アクションメモ」。人物が完了チェックを付けている。
```

## Slide S05. 提案書に入れるもの・入れないもの

- 図解パターン: `caution-risk-check`
- 画像内テキスト: `提案書に入れるもの・入れないもの`、`入れる`、`入れない`、`ダミーデータ`、`未確認機能は断定しない`
- プロンプト:

```text
共通ベースプロンプトに従う。2列カード。左は「入れる」で課題、KPI、リスクの抽象カード。右は「入れない」でロック付き文書と注意アイコン。下部に「ダミーデータ」「未確認機能は断定しない」。赤は注意アクセントのみ。
```

## Slide S06. ユースケースを1文で言う

- 図解パターン: `workshop-instruction`
- 画像内テキスト: `ユースケースを1文で言う`、`誰の業務`、`何を改善`、`何のために`
- プロンプト:

```text
共通ベースプロンプトに従う。ワーク導入スライド。机上にワークシート、ペン、ノートPC。3つの番号付きカード「誰の業務」「何を改善」「何のために」。受講者が書き込む雰囲気。
```

## Slide S07. 課題は3つ以内に絞る

- 図解パターン: `checklist-review`
- 画像内テキスト: `課題は3つ以内に絞る`、`転記が多い`、`確認に時間`、`対応漏れ`
- プロンプト:

```text
共通ベースプロンプトに従う。大きなチェックリストに3つの課題カード「転記が多い」「確認に時間」「対応漏れ」。周囲に余分な課題カードが薄く整理される表現。読み手に伝わる絞り込みを示す。
```

## Slide S08. As-Isを業務の流れで描く

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `As-Isを業務の流れで描く`、`入力`、`処理`、`確認`、`保存`、`通知`
- プロンプト:

```text
共通ベースプロンプトに従う。5ステップの柔らかい横フロー「入力」「処理」「確認」「保存」「通知」。少し散らかった書類から順に流れるが、暗くしすぎない。現状把握のスライド。
```

## Slide S09. To-Beは人の確認まで含める

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `To-Beは人の確認まで含める`、`自動化`、`AI下書き`、`人の確認`、`ログ`
- プロンプト:

```text
共通ベースプロンプトに従う。4ステップの横フロー「自動化」「AI下書き」「人の確認」「ログ」。人の確認カードを中央に強調し、緑のチェックを添える。全自動ではない安心感。
```

## Slide S10. Gapが提案書の中身になる

- 図解パターン: `before-after-cards`
- 画像内テキスト: `Gapが提案書の中身になる`、`As-Is`、`Gap`、`To-Be`、`必要な部品`
- プロンプト:

```text
共通ベースプロンプトに従う。左に「As-Is」、中央に「Gap」、右に「To-Be」。Gap部分に「必要な部品」としてフォーム、台帳、AI、GAS、運用設計の抽象カード。やさしい比較構図。
```

## Slide S11. 非対象範囲を先に書く

- 図解パターン: `caution-risk-check`
- 画像内テキスト: `非対象範囲を先に書く`、`外部送信`、`承認判断`、`個人情報`、`未確認機能`
- プロンプト:

```text
共通ベースプロンプトに従う。注意喚起スライド。4つの確認カード「外部送信」「承認判断」「個人情報」「未確認機能」。ロック、停止マーク、確認者アイコン。赤は強すぎず、実務的な注意として表現。
```

## Slide S12. 画面共有: 提案書骨子の記入例

- 図解パターン: `screen-share-transition`
- 画像内テキスト: `ここから画面共有`、`提案書骨子の記入例`、`As-Is / To-Be`、`ダミーデータ`
- プロンプト:

```text
共通ベースプロンプトに従う。中央に抽象モニターとカーソル。大きく「ここから画面共有」「提案書骨子の記入例」。下部に3カード「As-Is / To-Be」「ダミーデータ」「ワークシート」。偽UIやスクショ枠は作らない。
```

## Slide S13. プロトタイプは判断材料

- 図解パターン: `ui-mockup-comparison`
- 画像内テキスト: `プロトタイプは判断材料`、`画面`、`台帳`、`ログ`、`レビュー`
- プロンプト:

```text
共通ベースプロンプトに従う。抽象UIモックの4カード「画面」「台帳」「ログ」「レビュー」。人物が提案書の横で確認している。完成品ではなく判断材料として見せる雰囲気。
```

## Slide S14. スクリーンショットの安全確認

- 図解パターン: `caution-risk-check`
- 画像内テキスト: `スクリーンショットの安全確認`、`個人情報`、`メールアドレス`、`共有リンク`、`APIキー`
- プロンプト:

```text
共通ベースプロンプトに従う。メインにロック付きスクリーンカードとチェックリスト。4つの注意カード「個人情報」「メールアドレス」「共有リンク」「APIキー」。録画前確認の落ち着いた注意スライド。
```

## Slide S15. 技術構成は入力・処理・出力で見せる

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `技術構成は入力・処理・出力で見せる`、`入力`、`処理`、`出力`、`確認`、`運用`
- プロンプト:

```text
共通ベースプロンプトに従う。5ステップフロー「入力」「処理」「出力」「確認」「運用」。各カードに抽象フォーム、歯車、文書、人物チェック、ログのアイコン。実在ロゴなし。
```

## Slide S16. Meet文字起こし活用を提案に入れる

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `Meet文字起こし活用を提案に入れる`、`文字起こし`、`アクション抽出`、`台帳化`、`確認`
- プロンプト:

```text
共通ベースプロンプトに従う。4ステップフロー「文字起こし」「アクション抽出」「台帳化」「確認」。会議メモ、AI補助、表、確認者を抽象的に描く。実在Meetロゴは描かない。
```

## Slide S17. Gemini連携と代替運用をセットにする

- 図解パターン: `before-after-cards`
- 画像内テキスト: `連携と代替運用をセットにする`、`使える場合`、`使えない場合`、`手動貼り付け`、`止まらない運用`
- プロンプト:

```text
共通ベースプロンプトに従う。左右比較。左「使える場合」は連携された文書と台帳。右「使えない場合」は「手動貼り付け」と「止まらない運用」。製品ロゴなし、業務フロー重視。
```

## Slide S18. 画面共有: プロトタイプ構成を確認する

- 図解パターン: `screen-share-transition`
- 画像内テキスト: `ここから画面共有`、`プロトタイプ構成を確認`、`フォーム`、`台帳`、`ログ`
- プロンプト:

```text
共通ベースプロンプトに従う。画面共有案内。中央にモニター、下部に3カード「フォーム」「台帳」「ログ」。柔らかい人物が確認している。偽UIやスクリーンショット枠は作らない。
```

## Slide S19. KPIは導入後に測るもの

- 図解パターン: `data-table-checkpoints`
- 画像内テキスト: `KPIは導入後に測るもの`、`作業時間`、`対応速度`、`確認漏れ`、`成功率`
- プロンプト:

```text
共通ベースプロンプトに従う。大きなKPI表と4つの確認カード「作業時間」「対応速度」「確認漏れ」「成功率」。人物が虫眼鏡で表を見ている。数値は入れない。
```

## Slide S20. 効果試算は控えめに置く

- 図解パターン: `data-table-checkpoints`
- 画像内テキスト: `効果試算は控えめに置く`、`仮試算`、`根拠`、`確認時間`、`教育コスト`
- プロンプト:

```text
共通ベースプロンプトに従う。試算表と電卓、4カード「仮試算」「根拠」「確認時間」「教育コスト」。過大な成果を避け、落ち着いた検討資料として表現。
```

## Slide S21. AI活用の効果は確認時間で見る

- 図解パターン: `checklist-review`
- 画像内テキスト: `AI活用の効果は確認時間で見る`、`分類案`、`要約案`、`抽出案`、`人が確定`
- プロンプト:

```text
共通ベースプロンプトに従う。中央にAI補助カード、周囲に「分類案」「要約案」「抽出案」。右側に人物が「人が確定」するチェックカード。全自動に見せない。
```

## Slide S22. GAS自動化の効果はログで見る

- 図解パターン: `data-table-checkpoints`
- 画像内テキスト: `GAS自動化の効果はログで見る`、`実行日時`、`処理件数`、`成功`、`エラー`
- プロンプト:

```text
共通ベースプロンプトに従う。抽象ログ表を大きく配置し、列見出しとして「実行日時」「処理件数」「成功」「エラー」。歯車とチェック、注意アイコン。実在UIは描かない。
```

## Slide S23. 画面共有: KPI表と仮試算の記入例

- 図解パターン: `screen-share-transition`
- 画像内テキスト: `ここから画面共有`、`KPI表と仮試算`、`測定方法`、`仮試算`
- プロンプト:

```text
共通ベースプロンプトに従う。画面共有案内。中央にモニターと表の抽象アイコン。大きく「ここから画面共有」「KPI表と仮試算」。下部に「測定方法」「仮試算」カード。
```

## Slide S24. リスクを先に書く

- 図解パターン: `caution-risk-check`
- 画像内テキスト: `リスクを先に書く`、`権限`、`通知漏れ`、`停止時対応`、`教育`、`運用負荷`
- プロンプト:

```text
共通ベースプロンプトに従う。メインに盾とチェックリスト。5つのカード「権限」「通知漏れ」「停止時対応」「教育」「運用負荷」。赤はアクセントだけにして、信頼につながるリスク整理を表す。
```

## Slide S25. 情報管理リスクと対策

- 図解パターン: `permission-role-matrix`
- 画像内テキスト: `情報管理リスクと対策`、`入力禁止`、`匿名化`、`共有範囲`、`ログ閲覧`
- プロンプト:

```text
共通ベースプロンプトに従う。権限マトリクス風の大きな表とロックアイコン。4つの確認項目「入力禁止」「匿名化」「共有範囲」「ログ閲覧」。人物が権限を確認している。
```

## Slide S26. 運用体制を役割で分ける

- 図解パターン: `role-card-set`
- 画像内テキスト: `運用体制を役割で分ける`、`利用者`、`確認者`、`管理者`、`更新担当`
- プロンプト:

```text
共通ベースプロンプトに従う。4つの役割カード「利用者」「確認者」「管理者」「更新担当」。人物アイコンとチェックバッジ。中央に共有ドキュメントを置き、役割がつながる構図。
```

## Slide S27. ロードマップは段階で書く

- 図解パターン: `soft-process-flow`
- 画像内テキスト: `ロードマップは段階で書く`、`試行`、`本番化`、`教育`、`運用`、`改善`、`横展開`
- プロンプト:

```text
共通ベースプロンプトに従う。6ステップの横ロードマップ「試行」「本番化」「教育」「運用」「改善」「横展開」。柔らかい道筋と小さな旗。現実的な段階導入。
```

## Slide S28. 3分版・10分版・詳細版を分ける

- 図解パターン: `role-card-set`
- 画像内テキスト: `説明時間で資料を分ける`、`3分版`、`10分版`、`詳細版`
- プロンプト:

```text
共通ベースプロンプトに従う。3つの資料カード「3分版」「10分版」「詳細版」。カードの厚みを少しずつ増やし、説明時間に応じた情報量の違いを表す。人物が資料を選ぶ。
```

## Slide S29. 録画素材はダミーデータだけ使う

- 図解パターン: `caution-risk-check`
- 画像内テキスト: `録画素材はダミーデータだけ使う`、`作業風景`、`スクショ`、`出典メモ`、`安全確認`
- プロンプト:

```text
共通ベースプロンプトに従う。録画前チェックのスライド。大きなカメラ/モニター抽象アイコンと4カード「作業風景」「スクショ」「出典メモ」「安全確認」。個人情報を映さないロック表現。
```

## Slide S30. 画面共有: 3分版提案の構成例

- 図解パターン: `screen-share-transition`
- 画像内テキスト: `ここから画面共有`、`3分版提案の構成例`、`課題`、`KPI`、`次アクション`
- プロンプト:

```text
共通ベースプロンプトに従う。画面共有案内。中央にモニターと提案資料カード。下部に「課題」「KPI」「次アクション」。偽UIや実在サービス画面は入れない。
```

## Slide S31. 提案書骨子を組み立てる

- 図解パターン: `workshop-instruction`
- 画像内テキスト: `提案書骨子を組み立てる`、`背景`、`To-Be`、`KPI`、`リスク`、`ロードマップ`
- プロンプト:

```text
共通ベースプロンプトに従う。ワークスライド。机上にワークシートとペン。5つの骨子カード「背景」「To-Be」「KPI」「リスク」「ロードマップ」。書き込み作業の雰囲気。
```

## Slide S32. 講師記入例と比較する

- 図解パターン: `checklist-review`
- 画像内テキスト: `講師記入例と比較する`、`良い例`、`弱い例`、`改善メモ`
- プロンプト:

```text
共通ベースプロンプトに従う。左右に「良い例」「弱い例」のカード、中央または下部に「改善メモ」。人物がチェックリストを見ている。責める雰囲気ではなく学習用比較。
```

## Slide S33. 自己レビュー8観点

- 図解パターン: `checklist-review`
- 画像内テキスト: `自己レビュー8観点`、`課題`、`役割分担`、`代替運用`、`効果`、`次アクション`
- プロンプト:

```text
共通ベースプロンプトに従う。大きなチェックリストと5つの代表カード「課題」「役割分担」「代替運用」「効果」「次アクション」。8観点を示すが、文字は代表項目だけに絞る。
```

## Slide S34. 画面共有: 自己レビュー記入例

- 図解パターン: `screen-share-transition`
- 画像内テキスト: `ここから画面共有`、`自己レビュー記入例`、`改善メモ`、`次アクション`
- プロンプト:

```text
共通ベースプロンプトに従う。画面共有案内。中央にモニターとチェックリスト、下部に「改善メモ」「次アクション」カード。柔らかい人物が確認している。
```

## Slide S35. 全6回の成果物をつなぐ

- 図解パターン: `summary-takeaway`
- 画像内テキスト: `全6回の成果物をつなぐ`、`棚卸し`、`台帳`、`GAS`、`AI補助`、`運用設計`、`提案書`
- プロンプト:

```text
共通ベースプロンプトに従う。6つの成果物カード「棚卸し」「台帳」「GAS」「AI補助」「運用設計」「提案書」を弧を描くように接続。人物が最後の提案書カードにチェックを付ける。
```

## Slide S36. 講座後の次アクション

- 図解パターン: `summary-takeaway`
- 画像内テキスト: `講座後の次アクション`、`1業務で試す`、`関係者確認`、`ログ運用`、`改善`
- プロンプト:

```text
共通ベースプロンプトに従う。最終まとめ。4つのカード「1業務で試す」「関係者確認」「ログ運用」「改善」。右下に小さなゴール旗。受講後に自走する落ち着いた前向きな雰囲気。
```
