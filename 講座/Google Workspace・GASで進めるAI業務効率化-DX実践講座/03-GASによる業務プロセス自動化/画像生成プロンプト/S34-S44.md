# 第3回 画像生成プロンプト S34-S44

対象講座: Google Workspace・GASで進めるAI業務効率化/DX実践講座

対象スライド: S34-S44

採用テンプレート: `soft-isometric-corporate-warm`

## 共通ルール

- 画角は16:9、1920x1080。
- 白背景、ネイビー・ブルー・グリーン、薄いグレー罫線、カード型、アイソメトリック図解で統一する。
- 日本語テキストは大きく、読みやすいゴシック体風にする。
- 画像内テキストは、各スライドで指定したタイトルと短い箇条書きだけにする。
- 長文、実在ロゴ、実在UI、実名、メール、価格、連絡先、QRコードは入れない。
- GAS、表、フォーム、メール、ログは抽象アイコンや素材配置枠で表現する。

## 共通ベースプロンプト

```text
16:9、1920x1080の法人研修用スライド画像を作成。白背景、ネイビーの大見出し、ブルーとグリーンのアクセント、薄いグレーのカード罫線、控えめな影、清潔なアイソメトリック資料デザイン。日本語テキストは大きく正確に、ゴシック体風で配置。指定した日本語以外の文字は入れない。実在ロゴ、実在UI、実在企業名、人物名、価格、連絡先、QRコード、透かしは入れない。
```

## 共通ネガティブプロンプト

```text
文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、長文の詰め込み、実在ロゴ、実在UI、実在企業名、人物名、価格、連絡先、QRコード、透かし、写真風人物、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、過度な3D、雑然としたレイアウトを避ける。
```

## Slide S34. 手動実行と自動実行を分ける

- 使用素材: 図解
- 図解パターン: comparison-contrast
- 画像内テキスト:
  - タイトル: 手動実行と自動実行を分ける
  - 本文: 手動メニュー
  - 本文: 時間主導トリガー
  - 本文: フォーム送信トリガー
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「手動実行と自動実行を分ける」。中央に3つのカードを横並びで配置し、「手動メニュー」「時間主導トリガー」「フォーム送信トリガー」と大きく表示。各カードには、クリックする手、時計、送信フォームを抽象アイコンで配置。カード下部を小さな自動処理ブロックへつなぐティールの矢印で表現する。実在ロゴや実在UIは入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。実在サービス画面、細かい表、長い説明文、読めない小文字を避ける。
```

## Slide S35. エラーは隠さず見える化する

- 使用素材: 図解
- 図解パターン: alert-log
- 画像内テキスト:
  - タイトル: エラーは隠さず見える化する
  - 本文: 必要な列がない
  - 本文: 権限が足りない
  - 本文: 対象行が0件
  - 本文: 重複がある
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「エラーは隠さず見える化する」。左側に注意アイコン付きのチェックリストカードを配置し、「必要な列がない」「権限が足りない」「対象行が0件」「重複がある」を大きく表示。右側にログ確認パネル風の抽象カードを置き、エラーがログへ集まる流れをティールの線で示す。実在UIやコード文字列は入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。赤色だらけの警告画面、実在ログ画面、長いエラーメッセージ、英数字の羅列を避ける。
```

## Slide S36. 入力チェック

- 使用素材: 図解
- 図解パターン: validation-flow
- 画像内テキスト:
  - タイトル: 入力チェック
  - 本文: 必須ヘッダー
  - 本文: 空欄
  - 本文: 想定外ステータス
  - 本文: エラーと注意を分ける
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「入力チェック」。中央に表カードから検証ゲートへ進む流れをアイソメトリックに配置。検証ゲートの周囲に「必須ヘッダー」「空欄」「想定外ステータス」「エラーと注意を分ける」の4項目カードを置く。合格はティールのチェック、要確認は淡いアンバーの注意アイコンで表現。実在UIは入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。細かいセル文字、実在スプレッドシート画面、長いルール説明を避ける。
```

## Slide S37. 対象行なし・重複ありの扱い

- 使用素材: 図解
- 図解パターン: decision-table
- 画像内テキスト:
  - タイトル: 対象行なし・重複ありの扱い
  - 本文: 対象行なし: 正常終了
  - 本文: 重複あり: 要確認
  - 本文: 必須列なし: 止める
  - 本文: 空欄あり: 確認
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「対象行なし・重複ありの扱い」。中央に4分割の判定ボードを配置し、「対象行なし: 正常終了」「重複あり: 要確認」「必須列なし: 止める」「空欄あり: 確認」を大きく表示。正常、確認、停止がひと目で分かるように、チェック、虫眼鏡、停止マークの抽象アイコンを添える。表は大きく単純にし、細かい罫線は少なくする。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。小さな表文字、複雑な条件分岐図、実在UI、長文を避ける。
```

## Slide S38. 制限と通知

- 使用素材: 図解
- 図解パターン: operations-guardrail
- 画像内テキスト:
  - タイトル: 制限と通知
  - 本文: 実行時間
  - 本文: 利用回数
  - 本文: 通知条件
  - 本文: 復旧手順
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「制限と通知」。中央に自動処理のパイプラインを置き、その周囲に「実行時間」「利用回数」「通知条件」「復旧手順」の4つの安全確認カードを配置。時計、メーター、通知ベル、手順書の抽象アイコンを使う。小さく始めて運用を安定させる印象の、落ち着いたネイビー・ブルー・グリーン構成。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。具体的な制限数値、公式情報の断定、実在メール画面、実在ロゴを避ける。
```

## Slide S39. 演習: 未請求行抽出と転記

- 使用素材: 図解
- 図解パターン: workflow-transfer
- 画像内テキスト:
  - タイトル: 演習: 未請求行抽出と転記
  - 本文: 売上CSV
  - 本文: 未請求・重複疑い
  - 本文: 未請求一覧
  - 本文: 処理ログ
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「演習: 未請求行抽出と転記」。左に入力カード「売上CSV」、中央に条件フィルター「未請求・重複疑い」、右に出力カード「未請求一覧」、下にログカード「処理ログ」を配置。カード間をティールの矢印でつなぐ。CSVや表は抽象化し、実在データ、会社名、金額、メールは入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。実在取引先名、金額、メール、細かい表データ、実在UIを避ける。
```

## Slide S40. サンプルコードの全体構造

- 使用素材: 図解
- 図解パターン: process-steps
- 画像内テキスト:
  - タイトル: サンプルコードの全体構造
  - 本文: 設定
  - 本文: 入力
  - 本文: 検証
  - 本文: 処理
  - 本文: 出力
  - 本文: ログ
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「サンプルコードの全体構造」。中央に6つのステップカードを左から右へ流れるように配置し、「設定」「入力」「検証」「処理」「出力」「ログ」と大きく表示。各カードには歯車、取り込み、チェック、処理ブロック、書き出し、記録の抽象アイコン。コード本文や実在UIは描かず、構造だけを見せる。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。読めないコード、英数字の羅列、黒い開発画面、実在UIを避ける。
```

## Slide S41. 読み替える5項目

- 使用素材: 図解
- 図解パターン: editable-settings
- 画像内テキスト:
  - タイトル: 読み替える5項目
  - 本文: シート名
  - 本文: ヘッダー名
  - 本文: 抽出条件
  - 本文: 出力先
  - 本文: ログ
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「読み替える5項目」。中央に設定パネル風の大きなカードを置き、「シート名」「ヘッダー名」「抽出条件」「出力先」「ログ」の5項目を大きな行として並べる。右側に鉛筆と置き換え矢印の抽象アイコンを配置し、自分の業務に合わせて差し替える印象にする。実在UIやコードは入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。細かい設定画面、実在シート名、長文、コード文字列を避ける。
```

## Slide S42. テスト観点

- 使用素材: 図解
- 図解パターン: test-cases
- 画像内テキスト:
  - タイトル: テスト観点
  - 本文: 対象行あり
  - 本文: 対象行なし
  - 本文: 空欄あり
  - 本文: 重複あり
  - 本文: 期待結果を書く
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「テスト観点」。中央にチェックリストとテストケースカードを配置し、「対象行あり」「対象行なし」「空欄あり」「重複あり」を大きく表示。下部に強調カードとして「期待結果を書く」。虫眼鏡、チェック、メモの抽象アイコンで、実行前に確認する雰囲気を出す。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。小さな表、長いテスト仕様、実在データ、読めない文字を避ける。
```

## Slide S43. 自分の業務に置き換える

- 使用素材: 図解
- 図解パターン: usecase-mapping
- 画像内テキスト:
  - タイトル: 自分の業務に置き換える
  - 本文: 問い合わせ: 未対応
  - 本文: 申請: 期限が近い
  - 本文: 日報: 未提出
  - 本文: 期限管理: 期限超過
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「自分の業務に置き換える」。中央に業務カードと条件カードの対応表を大きく配置し、「問い合わせ: 未対応」「申請: 期限が近い」「日報: 未提出」「期限管理: 期限超過」を読みやすく表示。各行に抽象アイコンを添える。職種横断で使える汎用的な業務改善イメージにし、実在企業名や個人情報は入れない。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。実在部署名、実名、メール、細かい社内情報、長文を避ける。
```

## Slide S44. まとめと次回準備

- 使用素材: 図解
- 図解パターン: summary-next-step
- 画像内テキスト:
  - タイトル: まとめと次回準備
  - 本文: 決まった表処理を安定化
  - 本文: 入力・条件・出力・ログ
  - 本文: 原本を守る
  - 本文: 次回: 分類・要約・下書き
- 画像プロンプト:

```text
共通ベースプロンプトに従う。上部にタイトル「まとめと次回準備」。中央に4つのまとめカードを配置し、「決まった表処理を安定化」「入力・条件・出力・ログ」「原本を守る」「次回: 分類・要約・下書き」を大きく表示。左側はGASによる安定化、右側は次回のAI活用へ進む矢印を抽象アイコンで表現。講座の締めにふさわしい整理されたレイアウト。
```

- ネガティブプロンプト:

```text
共通ネガティブプロンプトに従う。実在ロゴ、実在AI画面、長文、派手な広告風演出、読めない日本語を避ける。
```
