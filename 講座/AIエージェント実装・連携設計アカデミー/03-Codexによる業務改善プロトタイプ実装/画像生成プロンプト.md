# 03-Codexによる業務改善プロトタイプ実装 画像生成プロンプト（提出用・高密度版）

対象講座: AIエージェント実装・連携設計アカデミー
対象セッション: 03-Codexによる業務改善プロトタイプ実装
対象スライド: S01-S40
採用テンプレート: `isometric-corporate-clean`
密度基準: Google Workspace講座および直近サンプルS02「導入判断キャンバス」と同等。固有名詞・数値・事例は `skills/corporate-training-course-builder/references/concrete-tools-mcp-case-bank.md` に合わせる。

## 共通ルール

- `Course`、`Session`、フォルダ名は生成時の文脈情報であり、画像内の見出しやヘッダーとして描かない。画像内に出す文字は各スライドの `SOURCE TEXT` を正とする。
- GPT image 2 / built-in `image_gen` で1枚まるごとの完成ラスター画像として生成する。SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成は使わない。
- 各スライドは、タイトル、So What型ヘッドライン、3〜6個の内容カードまたは表・フロー・チェックリスト、架空事例、成果物、確認観点を入れる。
- サービス名やMCP名は目的とセットでテキストカードにする。実在ロゴ・実在UIは参照素材がある場合だけ使い、参照素材なしでは描かせない。
- 会社名は架空企業のみ。顧客名、社員名、メールアドレス、価格、連絡先、契約情報、APIキーは入れない。

## 具体サービス・MCP候補（この回で使う正本）

| 候補 | 業務での使いどころ |
| --- | --- |
| Codex | CSV処理、レポート生成、テスト追加を小さな差分で依頼する |
| GitHub MCP | 変更履歴、レビュー依頼、修正ログをプロトタイプ検証の根拠にする |
| PostgreSQL / SQLite MCP | 読み取り専用の集計確認やスキーマ確認を発展例として扱う |
| Playwright MCP | 画面がある試作品の動作確認を自動化する候補として扱う |

導入時の注意: MCP、コネクタ、サービスの提供状況、利用条件、料金、管理者設定、データ保持、監査ログ、商用利用条件は、必ず最新の公式情報で確認する。

## 共通ベースプロンプト

```text
Use case: productivity-visual / infographic-diagram. Create one complete 16:9 Japanese corporate training slide as a final raster image. Clean white background, navy headline, blue/teal/mint accents, thin light-gray card borders, subtle soft shadows, crisp isometric corporate infographic style. Make the slide understandable without speaker notes: include the title, So What headline, structured content cards/table/process/checklist, a fictional numeric business case, learner output or exercise band near the bottom, and risk/review notes. Do not render real brand logos or real UI unless reference assets are provided. Keep Japanese text short, readable, and faithful to SOURCE TEXT.
```

## 共通ネガティブプロンプト

```text
Avoid incorrect Japanese, mojibake, unreadable tiny text, placeholder text, blank editable areas, dashed logo boxes, fake UI, invented real logos, real company names, real personal data, email addresses, phone numbers, pricing, contact information, credentials, QR codes, watermarks, dark backgrounds, decorative gradient blobs, comic style, messy layout, or any appearance of SVG/HTML/CSS/canvas/browser screenshot/local overlay output.
```

---

## Slide S01. Codexによる業務改善プロトタイプ実装

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 小さなプロトタイプを作り、テスト結果と修正ログまで残して実務化の判断材料にする
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: Codexによる業務改善プロトタイプ実装
ヘッドライン: 小さなプロトタイプを作り、テスト結果と修正ログまで残して実務化の判断材料にする
カード1 判断: 小さなプロトタイプ、テスト、修正ログ、MCP引き継ぎ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 小さなプロトタイプ、テスト、修正ログ、MCP引き継ぎを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S01: Codexによる業務改善プロトタイプ実装
Headline: 小さなプロトタイプを作り、テスト結果と修正ログまで残して実務化の判断材料にする
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: Codexによる業務改善プロトタイプ実装
ヘッドライン: 小さなプロトタイプを作り、テスト結果と修正ログまで残して実務化の判断材料にする
判断カード: 小さなプロトタイプ、テスト、修正ログ、MCP引き継ぎ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 小さなプロトタイプ、テスト、修正ログ、MCP引き継ぎを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S02. 今日の成果物

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `module-block`
- ヘッドライン: 今日作る成果物は、次回以降の設計・実装・提案へそのまま接続できる形で残す
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 今日の成果物
ヘッドライン: 今日作る成果物は、次回以降の設計・実装・提案へそのまま接続できる形で残す
カード1 判断: プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S02: 今日の成果物
Headline: 今日作る成果物は、次回以降の設計・実装・提案へそのまま接続できる形で残す
Diagram/layout pattern: module-block
Recommended layout: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 今日の成果物
ヘッドライン: 今日作る成果物は、次回以降の設計・実装・提案へそのまま接続できる形で残す
判断カード: プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S03. プロトタイプは小さく作る

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `funnel-selection`
- ヘッドライン: 1入力、1処理、1出力で始める方針を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 多数の候補から、頻度・効果・リスク・実現性で絞り込むファネルにする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: プロトタイプは小さく作る
ヘッドライン: 1入力、1処理、1出力で始める方針を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
カード1 判断: 1入力、1処理、1出力で始める
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 1入力、1処理、1出力で始める方針を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S03: プロトタイプは小さく作る
Headline: 1入力、1処理、1出力で始める方針を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
Diagram/layout pattern: funnel-selection
Recommended layout: 多数の候補から、頻度・効果・リスク・実現性で絞り込むファネルにする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: プロトタイプは小さく作る
ヘッドライン: 1入力、1処理、1出力で始める方針を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
判断カード: 1入力、1処理、1出力で始める
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 1入力、1処理、1出力で始める方針を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S04. 第1回・第2回の成果物を使う

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `process-flow`
- ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左から右へ、入力、処理、出力、人の確認、運用改善の5ステップで示す。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 第1回・第2回の成果物を使う
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
カード1 判断: タスクブリーフ、AGENTS.md、ダミーデータ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: タスクブリーフ、AGENTS.md、ダミーデータを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S04: 第1回・第2回の成果物を使う
Headline: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
Diagram/layout pattern: process-flow
Recommended layout: 左から右へ、入力、処理、出力、人の確認、運用改善の5ステップで示す。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 第1回・第2回の成果物を使う
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
判断カード: タスクブリーフ、AGENTS.md、ダミーデータ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: タスクブリーフ、AGENTS.md、ダミーデータを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S05. 作るもの・作らないもの

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `comparison-contrast`
- ヘッドライン: 作るもの・作らないものでは、任せる範囲と任せない範囲を分けて誤実行を防ぐ
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左右比較で、任せる範囲/任せない範囲、現状/改善後、良い例/悪い例を示す。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 作るもの・作らないもの
ヘッドライン: 作るもの・作らないものでは、任せる範囲と任せない範囲を分けて誤実行を防ぐ
カード1 判断: CSV処理は作る、UIや外部送信は作らない
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: CSV処理は作る、UIや外部送信は作らないを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S05: 作るもの・作らないもの
Headline: 作るもの・作らないものでは、任せる範囲と任せない範囲を分けて誤実行を防ぐ
Diagram/layout pattern: comparison-contrast
Recommended layout: 左右比較で、任せる範囲/任せない範囲、現状/改善後、良い例/悪い例を示す。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 作るもの・作らないもの
ヘッドライン: 作るもの・作らないものでは、任せる範囲と任せない範囲を分けて誤実行を防ぐ
判断カード: CSV処理は作る、UIや外部送信は作らない
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: CSV処理は作る、UIや外部送信は作らないを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S06. 業務プロトタイプ3例

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `module-block`
- ヘッドライン: 問い合わせ分類、週次レポート、申請不備チェックを業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 業務プロトタイプ3例
ヘッドライン: 問い合わせ分類、週次レポート、申請不備チェックを業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
カード1 判断: 問い合わせ分類、週次レポート、申請不備チェック
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 問い合わせ分類、週次レポート、申請不備チェックを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S06: 業務プロトタイプ3例
Headline: 問い合わせ分類、週次レポート、申請不備チェックを業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
Diagram/layout pattern: module-block
Recommended layout: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 業務プロトタイプ3例
ヘッドライン: 問い合わせ分類、週次レポート、申請不備チェックを業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
判断カード: 問い合わせ分類、週次レポート、申請不備チェック
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 問い合わせ分類、週次レポート、申請不備チェックを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S07. 入力データの確認観点

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 列名、空欄、重複、形式不正、文字量をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 入力データの確認観点
ヘッドライン: 列名、空欄、重複、形式不正、文字量をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: 列名、空欄、重複、形式不正、文字量
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 列名、空欄、重複、形式不正、文字量を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S07: 入力データの確認観点
Headline: 列名、空欄、重複、形式不正、文字量をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 入力データの確認観点
ヘッドライン: 列名、空欄、重複、形式不正、文字量をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: 列名、空欄、重複、形式不正、文字量
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 列名、空欄、重複、形式不正、文字量を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S08. 期待出力の確認観点

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 列、ファイル名、レビュー列、エラー列をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 期待出力の確認観点
ヘッドライン: 列、ファイル名、レビュー列、エラー列をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: 列、ファイル名、レビュー列、エラー列
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 列、ファイル名、レビュー列、エラー列を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S08: 期待出力の確認観点
Headline: 列、ファイル名、レビュー列、エラー列をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 期待出力の確認観点
ヘッドライン: 列、ファイル名、レビュー列、エラー列をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: 列、ファイル名、レビュー列、エラー列
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 列、ファイル名、レビュー列、エラー列を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S09. 画面共有: 問い合わせCSVを確認する

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 問い合わせCSVを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: `問い合わせ対応サンプル.csv` の列と行
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では問い合わせ対応サンプル.csvの列と行、または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S09: 画面共有: 問い合わせCSVを確認する
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 問い合わせCSVを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: `問い合わせ対応サンプル.csv` の列と行
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では問い合わせ対応サンプル.csvの列と行、または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S10. Codexへの依頼の型

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 背景、目的、対象、制約、検証、完成条件をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: Codexへの依頼の型
ヘッドライン: 背景、目的、対象、制約、検証、完成条件をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: 背景、目的、対象、制約、検証、完成条件
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 背景、目的、対象、制約、検証、完成条件を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S10: Codexへの依頼の型
Headline: 背景、目的、対象、制約、検証、完成条件をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: Codexへの依頼の型
ヘッドライン: 背景、目的、対象、制約、検証、完成条件をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: 背景、目的、対象、制約、検証、完成条件
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 背景、目的、対象、制約、検証、完成条件を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S11. 悪い依頼と良い依頼

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `comparison-contrast`
- ヘッドライン: 悪い依頼と良い依頼では、任せる範囲と任せない範囲を分けて誤実行を防ぐ
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左右比較で、任せる範囲/任せない範囲、現状/改善後、良い例/悪い例を示す。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 悪い依頼と良い依頼
ヘッドライン: 悪い依頼と良い依頼では、任せる範囲と任せない範囲を分けて誤実行を防ぐ
カード1 判断: 「効率化して」から検証可能な依頼へ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 「効率化して」から検証可能な依頼へを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S11: 悪い依頼と良い依頼
Headline: 悪い依頼と良い依頼では、任せる範囲と任せない範囲を分けて誤実行を防ぐ
Diagram/layout pattern: comparison-contrast
Recommended layout: 左右比較で、任せる範囲/任せない範囲、現状/改善後、良い例/悪い例を示す。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 悪い依頼と良い依頼
ヘッドライン: 悪い依頼と良い依頼では、任せる範囲と任せない範囲を分けて誤実行を防ぐ
判断カード: 「効率化して」から検証可能な依頼へ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 「効率化して」から検証可能な依頼へを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S12. 制約を先に書く

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `governance-risk`
- ヘッドライン: 制約を書くときは、便利さより先に禁止事項・承認・ログ・停止条件を決める
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 制約を先に書く
ヘッドライン: 制約を書くときは、便利さより先に禁止事項・承認・ログ・停止条件を決める
カード1 判断: 実データ禁止、外部送信禁止、削除禁止、UI不要
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 実データ禁止、外部送信禁止、削除禁止、UI不要を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S12: 制約を先に書く
Headline: 制約を書くときは、便利さより先に禁止事項・承認・ログ・停止条件を決める
Diagram/layout pattern: governance-risk
Recommended layout: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 制約を先に書く
ヘッドライン: 制約を書くときは、便利さより先に禁止事項・承認・ログ・停止条件を決める
判断カード: 実データ禁止、外部送信禁止、削除禁止、UI不要
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 実データ禁止、外部送信禁止、削除禁止、UI不要を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S13. 完成条件を先に書く

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: README、出力例、テスト、レビュー列をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 完成条件を先に書く
ヘッドライン: README、出力例、テスト、レビュー列をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: README、出力例、テスト、レビュー列
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: README、出力例、テスト、レビュー列を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S13: 完成条件を先に書く
Headline: README、出力例、テスト、レビュー列をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 完成条件を先に書く
ヘッドライン: README、出力例、テスト、レビュー列をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: README、出力例、テスト、レビュー列
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: README、出力例、テスト、レビュー列を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S14. 画面共有: タスクブリーフから依頼文へ

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: タスクブリーフから依頼文へ
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: 依頼文の組み立て
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `依頼文の組み立て` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S14: 画面共有: タスクブリーフから依頼文へ
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: タスクブリーフから依頼文へ
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: 依頼文の組み立て
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `依頼文の組み立て` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S15. Codex実装後に見る場所

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `structure-layer`
- ヘッドライン: 追加ファイル、変更行、実行手順を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 業務、ツール、データ、ガバナンス/ログの4層レイヤーで整理する。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: Codex実装後に見る場所
ヘッドライン: 追加ファイル、変更行、実行手順を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
カード1 判断: 追加ファイル、変更行、実行手順
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 追加ファイル、変更行、実行手順を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S15: Codex実装後に見る場所
Headline: 追加ファイル、変更行、実行手順を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
Diagram/layout pattern: structure-layer
Recommended layout: 業務、ツール、データ、ガバナンス/ログの4層レイヤーで整理する。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: Codex実装後に見る場所
ヘッドライン: 追加ファイル、変更行、実行手順を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
判断カード: 追加ファイル、変更行、実行手順
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 追加ファイル、変更行、実行手順を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S16. 画面共有: 差分を確認する

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 差分を確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: 意図した範囲だけ変わったか
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `意図した範囲だけ変わったか` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S16: 画面共有: 差分を確認する
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 差分を確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: 意図した範囲だけ変わったか
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `意図した範囲だけ変わったか` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S17. 差分レビューの赤信号

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `governance-risk`
- ヘッドライン: 差分レビューの赤信号では、便利さより先に禁止事項・承認・ログ・停止条件を決める
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 差分レビューの赤信号
ヘッドライン: 差分レビューの赤信号では、便利さより先に禁止事項・承認・ログ・停止条件を決める
カード1 判断: 実データ参照、削除、外部送信、広すぎる変更
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 実データ参照、削除、外部送信、広すぎる変更を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S17: 差分レビューの赤信号
Headline: 差分レビューの赤信号では、便利さより先に禁止事項・承認・ログ・停止条件を決める
Diagram/layout pattern: governance-risk
Recommended layout: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 差分レビューの赤信号
ヘッドライン: 差分レビューの赤信号では、便利さより先に禁止事項・承認・ログ・停止条件を決める
判断カード: 実データ参照、削除、外部送信、広すぎる変更
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 実データ参照、削除、外部送信、広すぎる変更を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S18. 実行前チェック

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 実行前チェック
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
カード1 判断: サンプルデータ、出力先、上書き、ログ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: サンプルデータ、出力先、上書き、ログを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S18: 実行前チェック
Headline: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 実行前チェック
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
判断カード: サンプルデータ、出力先、上書き、ログ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: サンプルデータ、出力先、上書き、ログを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S19. 画面共有: プロトタイプを実行する

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: プロトタイプを実行する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: CSV処理の実行例
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `CSV処理の実行例` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S19: 画面共有: プロトタイプを実行する
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: プロトタイプを実行する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: CSV処理の実行例
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `CSV処理の実行例` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S20. 出力結果を見る

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `data-insight`
- ヘッドライン: category_candidate, priority_candidate, review_noteは、サンプルデータ・ログ・人の確認列で検証できる形に残す
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 出力結果を見る
ヘッドライン: category_candidate, priority_candidate, review_noteは、サンプルデータ・ログ・人の確認列で検証できる形に残す
カード1 判断: category_candidate, priority_candidate, review_note
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: category_candidate, priority_candidate, review_noteを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S20: 出力結果を見る
Headline: category_candidate, priority_candidate, review_noteは、サンプルデータ・ログ・人の確認列で検証できる形に残す
Diagram/layout pattern: data-insight
Recommended layout: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 出力結果を見る
ヘッドライン: category_candidate, priority_candidate, review_noteは、サンプルデータ・ログ・人の確認列で検証できる形に残す
判断カード: category_candidate, priority_candidate, review_note
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: category_candidate, priority_candidate, review_noteを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S21. 人が確認する列を残す

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `governance-risk`
- ヘッドライン: 人が確認する列を残すでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 人が確認する列を残す
ヘッドライン: 人が確認する列を残すでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
カード1 判断: AI候補と人の最終判断を分ける
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: AI候補と人の最終判断を分けるを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S21: 人が確認する列を残す
Headline: 人が確認する列を残すでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
Diagram/layout pattern: governance-risk
Recommended layout: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 人が確認する列を残す
ヘッドライン: 人が確認する列を残すでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
判断カード: AI候補と人の最終判断を分ける
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: AI候補と人の最終判断を分けるを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S22. テスト観点の全体像

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 正常系、空欄、重複、形式不正、長文をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: テスト観点の全体像
ヘッドライン: 正常系、空欄、重複、形式不正、長文をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: 正常系、空欄、重複、形式不正、長文
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 正常系、空欄、重複、形式不正、長文を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S22: テスト観点の全体像
Headline: 正常系、空欄、重複、形式不正、長文をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: テスト観点の全体像
ヘッドライン: 正常系、空欄、重複、形式不正、長文をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: 正常系、空欄、重複、形式不正、長文
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 正常系、空欄、重複、形式不正、長文を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S23. 画面共有: 空欄ケースを確認する

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 空欄ケースを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: 本文空欄の警告
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `本文空欄の警告` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S23: 画面共有: 空欄ケースを確認する
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 空欄ケースを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: 本文空欄の警告
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `本文空欄の警告` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S24. 画面共有: 重複ケースを確認する

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 重複ケースを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: 同一本文の検出
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `同一本文の検出` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S24: 画面共有: 重複ケースを確認する
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 重複ケースを確認する
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: 同一本文の検出
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `同一本文の検出` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S25. 画面共有: 週次レポート生成例

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 週次レポート生成例
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: `週次レポート元データ.csv`
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では ``週次レポート元データ.csv`` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S25: 画面共有: 週次レポート生成例
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 週次レポート生成例
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: `週次レポート元データ.csv`
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では ``週次レポート元データ.csv`` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S26. レポート出力の確認観点

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `data-insight`
- ヘッドライン: 集計、注意行、コメント、人の確認は、サンプルデータ・ログ・人の確認列で検証できる形に残す
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: レポート出力の確認観点
ヘッドライン: 集計、注意行、コメント、人の確認は、サンプルデータ・ログ・人の確認列で検証できる形に残す
カード1 判断: 集計、注意行、コメント、人の確認
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 集計、注意行、コメント、人の確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S26: レポート出力の確認観点
Headline: 集計、注意行、コメント、人の確認は、サンプルデータ・ログ・人の確認列で検証できる形に残す
Diagram/layout pattern: data-insight
Recommended layout: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: レポート出力の確認観点
ヘッドライン: 集計、注意行、コメント、人の確認は、サンプルデータ・ログ・人の確認列で検証できる形に残す
判断カード: 集計、注意行、コメント、人の確認
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 集計、注意行、コメント、人の確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S27. エラーが出た時の依頼文

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: エラー全文、再現手順、期待結果、制約をチェックリスト化し、AIエージェント作業の合格条件として使う
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: エラーが出た時の依頼文
ヘッドライン: エラー全文、再現手順、期待結果、制約をチェックリスト化し、AIエージェント作業の合格条件として使う
カード1 判断: エラー全文、再現手順、期待結果、制約
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: エラー全文、再現手順、期待結果、制約を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S27: エラーが出た時の依頼文
Headline: エラー全文、再現手順、期待結果、制約をチェックリスト化し、AIエージェント作業の合格条件として使う
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: エラーが出た時の依頼文
ヘッドライン: エラー全文、再現手順、期待結果、制約をチェックリスト化し、AIエージェント作業の合格条件として使う
判断カード: エラー全文、再現手順、期待結果、制約
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: エラー全文、再現手順、期待結果、制約を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S28. 画面共有: デバッグ依頼を作る

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: デバッグ依頼を作る
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: エラーから修正依頼へ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `エラーから修正依頼へ` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S28: 画面共有: デバッグ依頼を作る
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: デバッグ依頼を作る
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: エラーから修正依頼へ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `エラーから修正依頼へ` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S29. テスト結果を残す

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `data-insight`
- ヘッドライン: 実施日、入力、期待、結果、対応は、サンプルデータ・ログ・人の確認列で検証できる形に残す
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: テスト結果を残す
ヘッドライン: 実施日、入力、期待、結果、対応は、サンプルデータ・ログ・人の確認列で検証できる形に残す
カード1 判断: 実施日、入力、期待、結果、対応
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 実施日、入力、期待、結果、対応を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S29: テスト結果を残す
Headline: 実施日、入力、期待、結果、対応は、サンプルデータ・ログ・人の確認列で検証できる形に残す
Diagram/layout pattern: data-insight
Recommended layout: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: テスト結果を残す
ヘッドライン: 実施日、入力、期待、結果、対応は、サンプルデータ・ログ・人の確認列で検証できる形に残す
判断カード: 実施日、入力、期待、結果、対応
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 実施日、入力、期待、結果、対応を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S30. ワーク1: テスト観点表を作る

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: ワーク1: テスト観点表を作る
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
カード1 判断: 一時停止して記入
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: ここで動画を一時停止し、一時停止して記入をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S30: ワーク1: テスト観点表を作る
Headline: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: ワーク1: テスト観点表を作る
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
判断カード: 一時停止して記入
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: ここで動画を一時停止し、一時停止して記入をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S31. 修正ログの役割

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `continuous-operation-loop`
- ヘッドライン: 何を、なぜ、どう確認したかは、一度作って終わりではなくログ確認と改善で定着させる
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 計画、実行、確認、改善のループにログ・教育・見直しを入れる。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 修正ログの役割
ヘッドライン: 何を、なぜ、どう確認したかは、一度作って終わりではなくログ確認と改善で定着させる
カード1 判断: 何を、なぜ、どう確認したか
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 何を、なぜ、どう確認したかを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S31: 修正ログの役割
Headline: 何を、なぜ、どう確認したかは、一度作って終わりではなくログ確認と改善で定着させる
Diagram/layout pattern: continuous-operation-loop
Recommended layout: 計画、実行、確認、改善のループにログ・教育・見直しを入れる。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 修正ログの役割
ヘッドライン: 何を、なぜ、どう確認したかは、一度作って終わりではなくログ確認と改善で定着させる
判断カード: 何を、なぜ、どう確認したか
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 何を、なぜ、どう確認したかを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S32. 画面共有: 修正ログを書く

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `scene-environment`
- ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 画面共有: 修正ログを書く
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
カード1 判断: 変更理由と確認結果
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 画面共有では `変更理由と確認結果` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S32: 画面共有: 修正ログを書く
Headline: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
Diagram/layout pattern: scene-environment
Recommended layout: 右側に小さな業務場面、左側にタイトル・ヘッドライン・3枚の要点カードを置く。実在UIやロゴは描かない。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 画面共有: 修正ログを書く
ヘッドライン: 画面共有ではダミーデータだけを開き、見る項目・判断基準・次の成果物への接続を確認する
判断カード: 変更理由と確認結果
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 画面共有では `変更理由と確認結果` または講師記入例だけを開き、列名、入力、出力、確認者、保存先、停止条件を見る。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S33. 過剰実装を防ぐ

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `funnel-selection`
- ヘッドライン: 便利機能追加より、検証可能性を優先を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 多数の候補から、頻度・効果・リスク・実現性で絞り込むファネルにする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 過剰実装を防ぐ
ヘッドライン: 便利機能追加より、検証可能性を優先を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
カード1 判断: 便利機能追加より、検証可能性を優先
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 便利機能追加より、検証可能性を優先を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S33: 過剰実装を防ぐ
Headline: 便利機能追加より、検証可能性を優先を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
Diagram/layout pattern: funnel-selection
Recommended layout: 多数の候補から、頻度・効果・リスク・実現性で絞り込むファネルにする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 過剰実装を防ぐ
ヘッドライン: 便利機能追加より、検証可能性を優先を業務成果物・確認者・停止条件とセットで整理し、ツール紹介にしない
判断カード: 便利機能追加より、検証可能性を優先
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 便利機能追加より、検証可能性を優先を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S34. セキュリティレビュー

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `governance-risk`
- ヘッドライン: セキュリティレビューでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: セキュリティレビュー
ヘッドライン: セキュリティレビューでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
カード1 判断: 実データ、資格情報、外部送信、削除を確認
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 実データ、資格情報、外部送信、削除を確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S34: セキュリティレビュー
Headline: セキュリティレビューでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
Diagram/layout pattern: governance-risk
Recommended layout: 中央に盾とチェックリスト、周囲に禁止事項・承認・ログ・停止条件カードを置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: セキュリティレビュー
ヘッドライン: セキュリティレビューでは、便利さより先に禁止事項・承認・ログ・停止条件を決める
判断カード: 実データ、資格情報、外部送信、削除を確認
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 実データ、資格情報、外部送信、削除を確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S35. ワーク2: プロトタイプ仕様へ置き換える

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: ワーク2: プロトタイプ仕様へ置き換える
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
カード1 判断: 自分のテーマへ適用
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: ここで動画を一時停止し、自分のテーマへ適用をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S35: ワーク2: プロトタイプ仕様へ置き換える
Headline: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: ワーク2: プロトタイプ仕様へ置き換える
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
判断カード: 自分のテーマへ適用
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: ここで動画を一時停止し、自分のテーマへ適用をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S36. 講師記入例

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `data-insight`
- ヘッドライン: 週次レポート生成プロトタイプは、サンプルデータ・ログ・人の確認列で検証できる形に残す
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 講師記入例
ヘッドライン: 週次レポート生成プロトタイプは、サンプルデータ・ログ・人の確認列で検証できる形に残す
カード1 判断: 週次レポート生成プロトタイプ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 週次レポート生成プロトタイプを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S36: 講師記入例
Headline: 週次レポート生成プロトタイプは、サンプルデータ・ログ・人の確認列で検証できる形に残す
Diagram/layout pattern: data-insight
Recommended layout: KPIカード、表、ログ、サンプルデータを組み合わせたダッシュボード型にする。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 講師記入例
ヘッドライン: 週次レポート生成プロトタイプは、サンプルデータ・ログ・人の確認列で検証できる形に残す
判断カード: 週次レポート生成プロトタイプ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 週次レポート生成プロトタイプを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S37. 第3回成果物チェック

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 第3回成果物チェック
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
カード1 判断: 仕様、出力、テスト、修正ログ、人の確認
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 仕様、出力、テスト、修正ログ、人の確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S37: 第3回成果物チェック
Headline: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 第3回成果物チェック
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
判断カード: 仕様、出力、テスト、修正ログ、人の確認
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 仕様、出力、テスト、修正ログ、人の確認を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S38. 第4回へ渡す情報

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `structure-layer`
- ヘッドライン: Resources候補、Tools候補、Prompts候補を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 業務、ツール、データ、ガバナンス/ログの4層レイヤーで整理する。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: 第4回へ渡す情報
ヘッドライン: Resources候補、Tools候補、Prompts候補を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
カード1 判断: Resources候補、Tools候補、Prompts候補
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: Resources候補、Tools候補、Prompts候補を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S38: 第4回へ渡す情報
Headline: Resources候補、Tools候補、Prompts候補を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
Diagram/layout pattern: structure-layer
Recommended layout: 業務、ツール、データ、ガバナンス/ログの4層レイヤーで整理する。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: 第4回へ渡す情報
ヘッドライン: Resources候補、Tools候補、Prompts候補を業務・ツール・データ・ガバナンスの層に分けると、抜け漏れを確認しやすい
判断カード: Resources候補、Tools候補、Prompts候補
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: Resources候補、Tools候補、Prompts候補を自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S39. ワーク3: MCP引き継ぎメモ

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `checklist-confirmation`
- ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: ワーク3: MCP引き継ぎメモ
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
カード1 判断: 入力、出力、権限、承認
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: ここで動画を一時停止し、入力、出力、権限、承認をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S39: ワーク3: MCP引き継ぎメモ
Headline: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
Diagram/layout pattern: checklist-confirmation
Recommended layout: 左にチェックリスト、右に確認タイル、下部に自己レビュー帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: ワーク3: MCP引き継ぎメモ
ヘッドライン: 動画を一時停止して、業務効率化プロトタイプ仕様に必要な判断を自分の業務へ置き換える
判断カード: 入力、出力、権限、承認
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: ここで動画を一時停止し、入力、出力、権限、承認をワークシートへ記入する。実在の顧客名、社員名、連絡先、認証情報は書かない。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。

## Slide S40. まとめ

- 使用テンプレート: `isometric-corporate-clean`
- 図解パターン: `module-block`
- ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
- 参照素材・スクリーンショット: 生成図解。実在ロゴ・実在UIは使わず、サービス名やMCP名は文字カードとして記載する。
- 推奨レイアウト: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
- 画像内に必ず入れる短い日本語文言:

```markdown
タイトル: まとめ
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
カード1 判断: 小さく作り、確認してからつなぐ
カード2 使う候補: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP
カード3 架空事例: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
カード4 成果物: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
下部帯: 小さく作り、確認してからつなぐを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
確認: 実データ・個人情報・認証情報・未公開資料は使わない。導入時は最新の公式情報を確認。
```

- 画像プロンプト:

```text
Use the common base prompt. Context only, do not render as visible text: Course title = AIエージェント実装・連携設計アカデミー. Context only, do not render as visible text: Session = 03-Codexによる業務改善プロトタイプ実装.
Slide S40: まとめ
Headline: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
Diagram/layout pattern: module-block
Recommended layout: 3〜6個の成果物カードをグリッドで配置し、下部に成果物帯を置く。
Render the following SOURCE TEXT faithfully as readable cards, labels, table/process/checklist text, and a bottom output/review band. Keep text compact; do not add unrelated claims.
--- SOURCE TEXT START ---
タイトル: まとめ
ヘッドライン: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモを確認し、次の回または実務導入で使える状態にする
判断カード: 小さく作り、確認してからつなぐ
サービス/MCPカード: Codex、GitHub MCP、PostgreSQL / SQLite MCP、Playwright MCP。目的とセットで表示し、導入時に最新の公式情報を確認と添える。
架空事例カード: みどり商事（卸売業・従業員60名）では、問い合わせCSV40件/日を分類し、週次レポート作成8時間/月を短縮できるかを演習上の例として検証する。
成果物カード: 業務効率化プロトタイプ仕様、テスト結果、修正ログ、MCP引き継ぎメモ
受講者アクション帯: 小さく作り、確認してからつなぐを自部署の業務に置き換え、入力、処理、出力、人の確認、ログ/保存先、停止条件を書けるか確認する。
リスク確認: 実データ、顧客情報、社員情報、価格、連絡先、契約情報、認証情報、未公開資料は使わない。
--- SOURCE TEXT END ---
Apply the common negative prompt.
```

- ネガティブプロンプト: 共通ネガティブプロンプトを適用する。
