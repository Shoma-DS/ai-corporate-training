# 第2回 図解パーツ生成プロンプト

- 生成日時: 2026-07-05 20:01:01 JST
- 講座名: 生成AI・GASで実践する業務変革・DX推進講座
- セッション/テキスト名: 第2回 業務データ基盤の設計
- 用途: HTML/Google Slides上で編集可能な講座名・回・S番号・タイトル・ヘッドラインを残したうえで、スライド本文の情報量を落とさない横長資料画像を1枚ずつ生成する。
- 生成サイズ: 全スライド共通で1536×1024（横3:2）。1枚ごとにサイズや縦横比を変えない。全ページ同一の図解ゾーン（600×322pt・約1.86:1）へ一括配置されるため、横に少し引き伸ばされる前提で平たく横長の構図にする。
- 上部余白: 画像上部約20%は白余白として空ける。テンプレートが編集可能なタイトルとヘッドラインをその位置に重ねる。下端にも小さな余白を残す（すぐ下に目次ストリップが来る）。
- 保存先: 生成・検品済みPNGを `図解パーツ/Sxx.png` として保存し、HTML/Google Slidesテンプレートへ添付する。
- 重要: 講座名、セッション名、S番号、セクション名、スピーカーノート、長い段落は画像内に固定しない。比較表、プロセス表、判断表、業種別例など元スライドの中核となる短いセル文は画像内に入れてよい。
- 画像内テキスト: 文字なし固定は禁止。役割名・プロセス名・成果物名・判断軸・手順・確認観点・具体例を、見出し、表セル、カード内説明として入れる。
- 密度基準: 元の全面スライド画像/スライド案にある判断軸、成果物、手順、リスク、確認観点、具体例を落とさない。S04のようなスライドでは、Before/After表、階層図、業種別例を1枚に併置し、2〜4領域・8〜20個程度の読める見出し/セル/短文を入れる。短いラベルだけの抽象図にしない。
- 例外: `章見出し/現在位置` または `章見出し` の目次切り替えスライドだけは、ユーザー指示により情報量を減らす。現在の目次項目を最も大きく、次の目次項目を次に大きく、前後関係と進捗だけを見せる。通常解説用の比較表・詳細カード・業種別例は入れない。
- 添付ルール: 生成後は `--embed-diagram-parts` でテンプレートへ挿入する。未生成・未添付・文字なしの飾り画像は完成扱いにしない。
- 共通画風: `isometric-corporate-clean`。白背景、ネイビー/ティール、薄いグレー罫線、法人研修向け、公式ロゴや実在UIを想像生成しない。

## 共通ネガティブプロンプト

```text
Do not create the editable template chrome. Do not render course title, session title, slide number, section header, speaker notes, long paragraph dumps, fake Google logos, fake Google UI, real personal data, email addresses, phone numbers, prices, QR codes, placeholder boxes, unreadable or incorrect Japanese, recruitment-ad or poster layouts, dramatic manga advertising, decorative dark backgrounds, or text areas meant to be filled later. The output is a dense supplemental diagram/reference-sheet image. Do not make it text-free, icon-only, or short-label-only; use readable Japanese headings, table cells, card text, and examples. If a comparison table or example table is the core source information, preserve it as visible structured text.
```

## S01 第2回 業務データ基盤の設計

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 入力の入口と台帳の形を今日決めると、第3回の自動化がそのまま設計書になる
- 推奨図解パターン: cover-title
- 参照素材・スクリーンショット: なし（表紙スライド）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S01 / 導入とケース選定.
Slide topic for context: 第2回 業務データ基盤の設計.
Concept to visualize: 入力の入口と台帳の形を今日決めると、第3回の自動化がそのまま設計書になる.
Suggested visual pattern: cover-title.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 入力の入口と台帳の形を今日決めると、第3回の自動化がそのまま設計書になる
**内容ブロック①：第2回のテーマ**
- フォームとスプレッドシートで「受付→管理→活用」の流れを設計する
- 今日のゴール: フォーム設計表・管理台帳列定義・権限・保存設計メモを完成させる
**内容ブロック②：第1回と今回のつながり**
| 第1回でやったこと | 第2回でやること |
|---|---|
| 業務棚卸しでA候補を選んだ | A候補のフォームを設計する |
| As-Is/To-Beで入力の入口を決めた | フォームの設問と選択肢を具体的に作る |
| 台帳の重要性を確認した | 台帳の列・ID・ステータスを設計する |
Visible text candidates to use when useful, without reducing the source density: 第2回のテーマ / 第1回と今回のつながり / フォームとスプレッドシートで「受付→管理→活用」の流れを設計する / 今日のゴール / 第1回でやったこと / 第2回でやること / 業務棚卸しでA候補を選んだ / A候補のフォームを設計する / As-Is/To-Beで入力の入口を決めた / フォームの設問と選択肢を具体的に作る / 台帳の重要性を確認した / 台帳の列・ID・ステータスを設計する / 受付→管理→活用.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S02 今日の2時間で作るもの

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 3つの設計書を完成させると、第3回のGAS実装がすぐに始められる
- 推奨図解パターン: card-trio
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S02 / 導入とケース選定.
Slide topic for context: 今日の2時間で作るもの.
Concept to visualize: 3つの設計書を完成させると、第3回のGAS実装がすぐに始められる.
Suggested visual pattern: card-trio.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 3つの設計書を完成させると、第3回のGAS実装がすぐに始められる
**内容ブロック①：3成果物カード**
| 成果物 | 内容 | 使い道 |
|---|---|---|
| フォーム設計表 | 設問名・入力形式・必須・台帳列名 | Googleフォームの実装に使う |
| 管理台帳列定義 | 列名・入力元・更新者・用途・注意点 | Sheetsの台帳を作る土台になる |
| 権限・保存設計メモ | 共有範囲・フォルダ構造・命名規則 | Drive整理とGAS実装の準備になる |
**内容ブロック②：今日のワーク時間**
- 動画を一時停止して各ワークに取り組んでください
- フォーム設計ワーク: 約15分
- 台帳列設計ワーク: 約10分
- 権限・保存設計メモ: 約10分
Visible text candidates to use when useful, without reducing the source density: 3成果物カード / 今日のワーク時間 / フォーム設計表 / 設問名・入力形式・必須・台帳列名 / 管理台帳列定義 / 列名・入力元・更新者・用途・注意点 / 権限・保存設計メモ / 共有範囲・フォルダ構造・命名規則 / 動画を一時停止して各ワークに取り組んでください / フォーム設計ワーク / 台帳列設計ワーク.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S03 第2回の目次/全体像

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 今日は5つの章を順番に進み、成果物を作りながら次の回へ接続する
- 推奨図解パターン: roadmap-timeline（目次/全体像）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S03 / 導入とケース選定.
Slide topic for context: 第2回の目次/全体像.
Concept to visualize: 今日は5つの章を順番に進み、成果物を作りながら次の回へ接続する.
Suggested visual pattern: roadmap-timeline（目次/全体像）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 今日は5つの章を順番に進み、成果物を作りながら次の回へ接続する
**内容ブロック①：目次と成果物の全体像**
| No. | 目次項目 | 時間 | スライド範囲 | この章で支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入とケース選定 | 15分 | S01–S07 | フォーム化対象ケース |
| 2 | Googleフォーム設計 | 30分 | S08–S19 | フォーム設計表 |
| 3 | 回答シートと管理台帳 | 30分 | S20–S30 | 管理台帳列定義 |
| 4 | 台帳の品質管理 | 20分 | S31–S38 | 台帳品質チェック |
| 5 | 権限・保存設計と演習レビュー | 25分 | S39–S45 | 権限・保存設計メモ |
**内容ブロック②：デモ/演習の位置**
| スライド | ここで行うこと |
|---|---|
| S15 | 画面共有: Googleフォームの設問作成画面を確認する |
| S17 | 画面共有: フォーム送信からSheets反映を確認する |
| S18 | フォーム設計ワーク（15分） |
| S28 | 画面共有: 回答シートから管理台帳を設計する |
| S29 | 台帳列設計ワーク（10分） |
| S38 | 画面共有: 入力規則・条件付き書式・集計式を確認する |
| S43 | 画面共有: Drive権限と保存場所を確認する |
| S44 | 演習レビュー: GASで扱いやすい台帳になっているか確認する |
**内容ブロック③：見方のルール**
- 各章の開始では「章見出し/現在位置」スライドで、今どこを学ぶかを確認する
- 通常スライドでは右上のセクション名とS番号で現在位置を追う
- 章ごとの成果物が、最終的に第6回の提案書へつながる
Visible text candidates to use when useful, without reducing the source density: 目次と成果物の全体像 / デモ/演習の位置 / 見方のルール / No. / 目次項目 / 1 / 導入とケース選定 / 2 / Googleフォーム設計 / 3 / 回答シートと管理台帳 / 4 / 台帳の品質管理 / 5 / 権限・保存設計と演習レビュー / スライド / ここで行うこと / S15 / 画面共有 / S17.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S04 章見出し/現在位置 1/5: 導入とケース選定

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 導入とケース選定では、第1回のA候補から、フォーム化する業務ケースを1つ選ぶことで、フォーム化対象ケースにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S04 / 導入とケース選定.
Slide topic for context: 章見出し/現在位置 1/5: 導入とケース選定.
Concept to visualize: 導入とケース選定では、第1回のA候補から、フォーム化する業務ケースを1つ選ぶことで、フォーム化対象ケースにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 1/5 導入とケース選定
現在章の範囲: S01-S07
時間目安: 15分
次の章: Googleフォーム設計
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 導入とケース選定では、第1回のA候補から、フォーム化する業務ケースを1つ選ぶことで、フォーム化対象ケースにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 1/5 / 導入とケース選定 / S01-S07 / 15分 / 次の章 / Googleフォーム設計.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S05 今回の判断軸: フォーム・台帳・GASの役割を分ける

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 「誰が入力・更新・処理するか」を列ごとに決めると、台帳が崩れない
- 推奨図解パターン: classification-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S05 / 導入とケース選定.
Slide topic for context: 今回の判断軸: フォーム・台帳・GASの役割を分ける.
Concept to visualize: 「誰が入力・更新・処理するか」を列ごとに決めると、台帳が崩れない.
Suggested visual pattern: classification-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 「誰が入力・更新・処理するか」を列ごとに決めると、台帳が崩れない
**内容ブロック①：情報の入力元別分類**
| 入力元 | 内容 | 台帳上の扱い |
|---|---|---|
| フォーム（送信者） | 受付日時・依頼内容・連絡先など | 原本シートに自動追加 |
| 担当者（人） | ステータス・担当者名・対応メモ | 管理台帳で手動更新 |
| GAS（自動処理） | 受付番号・完了日時・通知ログ | GASが列に書き込む |
| AI（Gemini） | 分類ラベル・要約テキスト | 人が確認してから確定 |
**内容ブロック②：なぜ入力元を分けるのか**
- フォーム列を担当者が上書きすると、原本データが汚染される
- GASが書く列を人が上書きすると、自動処理が誤作動する
- Geminiの出力は人が確認してから台帳に反映する
**内容ブロック③：今日の設計での活用**
- フォーム設計表に「台帳の列名」を書く
- 台帳列定義に「入力元（フォーム/人/GAS/AI）」を記入する
Visible text candidates to use when useful, without reducing the source density: 情報の入力元別分類 / なぜ入力元を分けるのか / 今日の設計での活用 / フォーム / 受付日時・依頼内容・連絡先など / 担当者 / ステータス・担当者名・対応メモ / GAS / 受付番号・完了日時・通知ログ / AI / 分類ラベル・要約テキスト / フォーム列を担当者が上書きすると、原本データが汚染される / GASが書く列を人が上書きすると、自動処理が誤作動する / Geminiの出力は人が確認してから台帳に反映する / フォーム設計表に「台帳の列名」を書く / 台帳列定義に「入力元」を記入する / 誰が入力・更新・処理するか / 台帳の列名.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S06 フォーム化に向く業務: 入力の標準化がデータ活用と自動化の土台になる

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: 入力をフォームに統一するだけで、集計・通知・AI分類が同時に動き出す
- 推奨図解パターン: pattern-list-with-icons
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S06 / 導入とケース選定.
Slide topic for context: フォーム化に向く業務: 入力の標準化がデータ活用と自動化の土台になる.
Concept to visualize: 入力をフォームに統一するだけで、集計・通知・AI分類が同時に動き出す.
Suggested visual pattern: pattern-list-with-icons.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 入力をフォームに統一するだけで、集計・通知・AI分類が同時に動き出す
**内容ブロック①：フォーム化に向く業務パターン**
| パターン | 具体例 | 業種 |
|---|---|---|
| 問い合わせ受付 | 顧客から製品・サービスへの問い合わせ | 全業種 |
| 申請・申込管理 | 有給申請・備品申請・会議室予約 | 全業種 |
| 報告・日報 | 日報・作業報告・巡回チェック | 製造・建設・サービス |
| アンケート・集計 | 顧客満足度・研修後アンケート | 全業種 |
| 在庫・棚卸し報告 | 在庫カウント・納品確認 | 小売・製造・建設 |
| 採用・応募受付 | エントリーシート・面接日程 | 全業種 |
**内容ブロック②：業種別の適用例**
| 業種 | フォーム化する業務 | 期待効果 |
|---|---|---|
| サービス業（飲食・宿泊） | 予約受付・クレーム報告 | 転記ゼロ・担当者割当自動化 |
| 製造業 | 日報・品質検査報告・設備異常報告 | 集計自動化・異常値アラート |
| 小売業 | 在庫補充依頼・棚卸し報告 | 補充依頼の自動集計 |
| 士業（税理士・社労士） | 依頼受付・書類提出確認 | 案件台帳の自動更新 |
| 建設業 | 現場報告・安全確認チェック | ペーパーレス・集計自動化 |
**内容ブロック③：フォーム化に向かない業務**
- 内容が毎回異なる・自由記述が必須の業務（→ Gemini要約と組み合わせる）
- 法的に書面・押印が必要な書類（→ 補助的な入力受付のみに使う）
- 個人情報を大量に取得する業務（→ 権限設計・同意フローを先に整える）
Visible text candidates to use when useful, without reducing the source density: フォーム化に向く業務パターン / 業種別の適用例 / フォーム化に向かない業務 / 具体例 / 問い合わせ受付 / 顧客から製品・サービスへの問い合わせ / 申請・申込管理 / 有給申請・備品申請・会議室予約 / 報告・日報 / 日報・作業報告・巡回チェック / アンケート・集計 / 顧客満足度・研修後アンケート / 在庫・棚卸し報告 / 在庫カウント・納品確認 / 採用・応募受付 / エントリーシート・面接日程 / フォーム化する業務 / サービス業 / 予約受付・クレーム報告 / 製造業.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S07 ケース選定: 第1回のA候補からフォーム化対象を決める

- セクション: 導入とケース選定
- スライド側ヘッドライン（画像内には原則入れない）: ケースを1つに絞ってから設計を始めると、今日中に設計書が完成する
- 推奨図解パターン: comparison-card-trio
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S07 / 導入とケース選定.
Slide topic for context: ケース選定: 第1回のA候補からフォーム化対象を決める.
Concept to visualize: ケースを1つに絞ってから設計を始めると、今日中に設計書が完成する.
Suggested visual pattern: comparison-card-trio.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: ケースを1つに絞ってから設計を始めると、今日中に設計書が完成する
**内容ブロック①：業務ケースの比較（ダミーデータ）**
| ケース | 現状の課題 | フォーム化のメリット | 難易度 |
|---|---|---|---|
| 問い合わせ受付 | メール転記・担当者バラバラ | 入力統一・自動台帳追記 | 低（最初に推奨） |
| 申請管理 | メール・口頭でバラバラ受付 | 申請内容標準化・進捗管理 | 低〜中 |
| 日報集計 | 紙・メール・Excelで分散 | 毎日の自動集計・異常値検知 | 中 |
**内容ブロック②：ケース選定の基準**
- 第1回のA候補業務を使う（確定していない場合は「問い合わせ受付」を仮テーマに）
- 設問が10個以内に収まるか確認する（10個を超える場合は第2回で絞り込む）
- 個人情報の扱いが少ないケースから始める
**内容ブロック③：今日の演習対象**
- 今日の演習は「問い合わせ受付」を共通サンプルとして使います
- 自分のA候補業務がある場合は、演習と並行して自分のシートを記入してください
Visible text candidates to use when useful, without reducing the source density: 業務ケースの比較 / ケース選定の基準 / 今日の演習対象 / 現状の課題 / 問い合わせ受付 / メール転記・担当者バラバラ / 申請管理 / メール・口頭でバラバラ受付 / 日報集計 / 紙・メール・Excelで分散 / 第1回のA候補業務を使う / 設問が10個以内に収まるか確認する / 個人情報の扱いが少ないケースから始める / 今日の演習は「問い合わせ受付」を共通サンプルとして使います.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S08 章見出し/現在位置 2/5: Googleフォーム設計

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: Googleフォーム設計では、1設問1情報・必須・選択肢・回答先を設計することで、フォーム設計表につなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S08 / Googleフォーム設計.
Slide topic for context: 章見出し/現在位置 2/5: Googleフォーム設計.
Concept to visualize: Googleフォーム設計では、1設問1情報・必須・選択肢・回答先を設計することで、フォーム設計表につなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 2/5 Googleフォーム設計
現在章の範囲: S08-S19
時間目安: 30分
前の章: 導入とケース選定
次の章: 回答シートと管理台帳
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: Googleフォーム設計では、1設問1情報・必須・選択肢・回答先を設計することで、フォーム設計表につなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 2/5 / Googleフォーム設計 / S08-S19 / 30分 / 前の章 / 導入とケース選定 / 次の章 / 回答シートと管理台帳.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S09 Googleフォームは「入力の入口」: ばらばらな連絡を1つの形式に統一する

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: メール・電話・口頭でバラバラだった入力が、フォームで1行1件に揃う
- 推奨図解パターン: before-after-split
- 参照素材・スクリーンショット: `スクリーンショット/google-forms-official-og.png`（取得先: https://workspace.google.com/products/forms/ の公式OG画像）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S09 / Googleフォーム設計.
Slide topic for context: Googleフォームは「入力の入口」: ばらばらな連絡を1つの形式に統一する.
Concept to visualize: メール・電話・口頭でバラバラだった入力が、フォームで1行1件に揃う.
Suggested visual pattern: before-after-split.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: メール・電話・口頭でバラバラだった入力が、フォームで1行1件に揃う
**内容ブロック①：Before/After 対比**
| | Before（フォームなし） | After（フォームあり） |
|---|---|---|
| 入力経路 | メール・電話・Slack・口頭 | Google Formsに統一 |
| 入力形式 | 担当者によってバラバラ | 設問ごとに形式が固定 |
| 台帳への反映 | 担当者が手動転記 | 回答が自動で1行追加 |
| 記録の正確性 | 転記ミス・漏れが発生 | 送信者の入力がそのまま保存 |
| タイムスタンプ | 手入力または記録なし | 送信日時が自動記録 |
**内容ブロック②：フォームを使うメリット3点**
- 転記が不要になる（回答がそのままSheetsに保存される）
- 入力形式が揃う（選択肢・日付・数値を入力タイプで制限できる）
- タイムスタンプが自動付与される（受付日時の手入力が不要）
**内容ブロック③：フォームが向くケースの目安**
- 同じ情報を複数の人から繰り返し収集する
- 集めた情報をSheets・GASで処理したい
- 入力者のITリテラシーがバラバラでもルールを統一したい
Visible text candidates to use when useful, without reducing the source density: Before/After 対比 / フォームを使うメリット3点 / フォームが向くケースの目安 / Before / 入力経路 / メール・電話・Slack・口頭 / 入力形式 / 担当者によってバラバラ / 台帳への反映 / 担当者が手動転記 / 記録の正確性 / 転記ミス・漏れが発生 / タイムスタンプ / 手入力または記録なし / 転記が不要になる / 入力形式が揃う / タイムスタンプが自動付与される / 同じ情報を複数の人から繰り返し収集する / 集めた情報をSheets・GASで処理したい / 入力者のITリテラシーがバラバラでもルールを統一したい.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S10 設問設計の基本: 1設問1情報・必須・説明文・迷わない選択肢

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 設問の設計が甘いと、台帳のデータが揃わず自動化もAIも使えなくなる
- 推奨図解パターン: checklist-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S10 / Googleフォーム設計.
Slide topic for context: 設問設計の基本: 1設問1情報・必須・説明文・迷わない選択肢.
Concept to visualize: 設問の設計が甘いと、台帳のデータが揃わず自動化もAIも使えなくなる.
Suggested visual pattern: checklist-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 設問の設計が甘いと、台帳のデータが揃わず自動化もAIも使えなくなる
**内容ブロック①：設問設計の4原則**
| 原則 | NG例 | OK例 |
|---|---|---|
| 1設問1情報 | 「氏名と所属を入力してください」（1つの設問に2情報） | 「氏名」「所属部署」を別設問に分ける |
| 必須項目を絞る | 全設問を必須にする | 集計・GAS処理に必要な設問のみ必須 |
| 説明文を入れる | 「カテゴリ」（何のカテゴリか不明） | 「お問い合わせの種類（製品・サービス・請求・その他から選択）」 |
| 迷わない選択肢 | 「大きい・普通・小さい」（曖昧） | 「高（翌日対応必須）・中（3営業日以内）・低（1週間以内）」 |
**内容ブロック②：設問数の目安**
- 必須設問: 5〜8個（多すぎると回答率が下がる）
- 任意設問: 2〜3個（詳細補足・添付・連絡先など）
- 合計: 10個以内を目安にする
**内容ブロック③：設問を増やしすぎた場合の対処**
- 「GASや集計で本当に使う設問か」を基準に絞る
- 自由記述を1つ入れると「その他の情報」を受け取れる
- 第3回のGAS設計後に必要な設問を追加する（後から追加可能）
Visible text candidates to use when useful, without reducing the source density: 設問設計の4原則 / 設問数の目安 / 設問を増やしすぎた場合の対処 / 1設問1情報 / 「氏名と所属を入力してください」 / 必須項目を絞る / 全設問を必須にする / 説明文を入れる / 「カテゴリ」 / 迷わない選択肢 / 「大きい・普通・小さい」 / 必須設問 / 任意設問 / 合計 / 「GASや集計で本当に使う設問か」を基準に絞る / 自由記述を1つ入れると「その他の情報」を受け取れる / 第3回のGAS設計後に必要な設問を追加する / 氏名と所属を入力してください / 氏名 / 所属部署.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S11 入力形式の使い分け: 設問タイプを正しく選ぶと集計・自動化が楽になる

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 設問タイプを目的に合わせて選ぶと、台帳のデータが揃い自動処理が動く
- 推奨図解パターン: comparison-table
- 参照素材・スクリーンショット: `スクリーンショット/forms-question-types.png`（取得先: https://support.google.com/docs/answer/2839737?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S11 / Googleフォーム設計.
Slide topic for context: 入力形式の使い分け: 設問タイプを正しく選ぶと集計・自動化が楽になる.
Concept to visualize: 設問タイプを目的に合わせて選ぶと、台帳のデータが揃い自動処理が動く.
Suggested visual pattern: comparison-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 設問タイプを目的に合わせて選ぶと、台帳のデータが揃い自動処理が動く
**内容ブロック①：設問タイプ対応表**
| 設問タイプ | 向いている用途 | GASやSheetsでの扱い |
|---|---|---|
| 短文（テキスト） | 氏名・会社名・件名など | 文字列として処理 |
| 段落（長文テキスト） | 問い合わせ内容・備考 | Geminiの要約・分類に使う |
| ラジオボタン（択一） | カテゴリ・優先度・種別 | IFやSWITCHで分岐しやすい |
| チェックボックス（複数選択） | 希望する対応方法など | カンマ区切りで保存される |
| プルダウン | 担当部署・商品名など | マスタと一致させやすい |
| 日付 | 希望日・締切日 | DATE関数・期限比較に使いやすい |
| 時刻 | 希望時間帯など | 時刻型として処理 |
| ファイルアップロード | 添付書類・画像 | Googleアカウント必須・管理者設定依存 |
| 評価スケール（1〜5） | 満足度・難易度 | 平均値・件数集計しやすい |
**内容ブロック②：チェックボックスの注意点**
- 複数選択の回答はカンマ区切りで1セルに入る
- GASで分割処理は可能だが設計が複雑になる
- 可能な場合はラジオボタン（択一）に変更するとシンプルになる
**内容ブロック③：設問タイプの選び方の目安**
- 集計したい → ラジオ・プルダウン・評価スケール
- GASで分岐させたい → ラジオ・プルダウン（選択肢を固定）
- Geminiで処理したい → 段落（長文テキスト）
Visible text candidates to use when useful, without reducing the source density: 設問タイプ対応表 / チェックボックスの注意点 / 設問タイプの選び方の目安 / 向いている用途 / 短文 / 氏名・会社名・件名など / 段落 / 問い合わせ内容・備考 / ラジオボタン / カテゴリ・優先度・種別 / チェックボックス / 希望する対応方法など / プルダウン / 担当部署・商品名など / 日付 / 希望日・締切日 / 時刻 / 希望時間帯など / ファイルアップロード / 添付書類・画像.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S12 自由記述を減らす理由: 集計・分類・GAS処理に使いやすい設問を作る

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 自由記述を減らして選択肢に変えると、集計の手間が9割なくなる
- 推奨図解パターン: before-after-split
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S12 / Googleフォーム設計.
Slide topic for context: 自由記述を減らす理由: 集計・分類・GAS処理に使いやすい設問を作る.
Concept to visualize: 自由記述を減らして選択肢に変えると、集計の手間が9割なくなる.
Suggested visual pattern: before-after-split.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 自由記述を減らして選択肢に変えると、集計の手間が9割なくなる
**内容ブロック①：自由記述と選択肢の比較**
| 比較軸 | 自由記述 | 選択肢（ラジオ・プルダウン） |
|---|---|---|
| 回答の表記 | バラバラ（「製品Aの件」「製品Aについて」「AについてのQ」） | 「製品A」で統一 |
| Sheetsでの集計 | COUNTIFで集計できない | COUNTIFで即集計可能 |
| GASでの分岐 | 文字列比較が複雑 | if/switch文がシンプル |
| Geminiでの分類 | 分類が必要 | 選択肢がそのまま分類ラベルに |
| 回答者の手間 | 毎回考えて入力 | クリックで完了 |
**内容ブロック②：選択肢を設計するときのルール**
- 選択肢は「他の選択肢と重ならない」ように設計する
- 「その他」選択肢を入れると漏れを防げる
- 選択肢のラベルはマスタシート（Sheets）と一致させる
**内容ブロック③：自由記述を残すべき場面**
- 詳細・背景・補足説明が必要な設問（問い合わせ内容・対応への要望など）
- Geminiで要約・分類する目的で使う設問
- 数が少ない（1〜2設問）で絞られている場合
Visible text candidates to use when useful, without reducing the source density: 自由記述と選択肢の比較 / 選択肢を設計するときのルール / 自由記述を残すべき場面 / 比較軸 / 自由記述 / 回答の表記 / バラバラ / Sheetsでの集計 / COUNTIFで集計できない / GASでの分岐 / 文字列比較が複雑 / Geminiでの分類 / 分類が必要 / 回答者の手間 / 毎回考えて入力 / 選択肢は「他の選択肢と重ならない」ように設計する / 「その他」選択肢を入れると漏れを防げる / 選択肢のラベルはマスタシートと一致させる / 詳細・背景・補足説明が必要な設問 / Geminiで要約・分類する目的で使う設問.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S13 必須項目と入力ルール: 未入力・形式違い・表記ゆれを入口で防ぐ

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 入口でデータの質を確保すると、台帳の修正作業がなくなる
- 推奨図解パターン: checklist-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S13 / Googleフォーム設計.
Slide topic for context: 必須項目と入力ルール: 未入力・形式違い・表記ゆれを入口で防ぐ.
Concept to visualize: 入口でデータの質を確保すると、台帳の修正作業がなくなる.
Suggested visual pattern: checklist-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 入口でデータの質を確保すると、台帳の修正作業がなくなる
**内容ブロック①：必須設定の基準**
| 設問の種類 | 必須にする | 任意でよい |
|---|---|---|
| GASで使う設問（カテゴリ・担当者割当） | 必須 | - |
| 集計・KPIに使う設問（種別・優先度） | 必須 | - |
| 連絡先（メール・電話） | 必須（返信が必要な場合） | 任意（返信不要な場合） |
| 詳細・備考 | - | 任意 |
| 添付ファイル | - | 任意 |
**内容ブロック②：入力ルールを説明文で伝える方法**
- 設問の「説明文」欄を活用する（例: 「半角英数で入力してください」「例: 2026/05/01」）
- プルダウン・ラジオで入力を制限する（テキスト入力をなくす）
- 数値設問は「数値」タイプにして文字が入らないようにする
**内容ブロック③：入力ルールの業種別例**
| 業種 | 設問 | 入力ルール |
|---|---|---|
| サービス業 | 希望日 | 日付タイプ（当日から2週間以降を推奨） |
| 製造業 | ロット番号 | 短文・半角英数8桁（例を説明文に記載） |
| 士業 | 申告年度 | プルダウン（選択肢: 2024年・2025年・2026年） |
| 小売業 | 商品カテゴリ | プルダウン（マスタと一致した選択肢） |
Visible text candidates to use when useful, without reducing the source density: 必須設定の基準 / 入力ルールを説明文で伝える方法 / 入力ルールの業種別例 / 設問の種類 / 必須にする / GASで使う設問 / 必須 / 集計・KPIに使う設問 / 連絡先 / 詳細・備考 / 添付ファイル / 設問の「説明文」欄を活用する（例 / プルダウン・ラジオで入力を制限する / 数値設問は「数値」タイプにして文字が入らないようにする / 設問 / サービス業 / 希望日 / 製造業 / ロット番号 / 士業.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S14 ファイルアップロードを使うときの注意

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: ファイルアップロードは便利だが、環境・権限・Driveの管理が前提になる
- 推奨図解パターン: warning-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S14 / Googleフォーム設計.
Slide topic for context: ファイルアップロードを使うときの注意.
Concept to visualize: ファイルアップロードは便利だが、環境・権限・Driveの管理が前提になる.
Suggested visual pattern: warning-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: ファイルアップロードは便利だが、環境・権限・Driveの管理が前提になる
**内容ブロック①：ファイルアップロードの条件と注意点**
- 回答者が **Googleアカウント** を持っている必要がある（ログインが必須）
- アップロードされたファイルはフォームオーナーの **Google Drive** に保存される
- 管理者設定によっては使用できない（組織のDriveポリシー確認が必要）
**内容ブロック②：ファイルアップロードを使う場合の管理ルール**
- アップロード先フォルダを専用フォルダに設定する（Drive内に自動作成される）
- ファイルが増えた場合のアーカイブ・削除ルールを事前に決める
- 個人情報・機密情報が含まれる場合は共有範囲を必ず限定する
**内容ブロック③：今日の演習での扱い**
- ファイルアップロードは本日の必須演習に含まない
- 使用する場合は「環境確認 → 管理ルール策定 → 権限設計」の順で準備する
- 詳細は公式ヘルプ（Google Docs Editors Help: Googleフォームのファイルアップロード）を参照
Visible text candidates to use when useful, without reducing the source density: ファイルアップロードの条件と注意点 / ファイルアップロードを使う場合の管理ルール / 今日の演習での扱い / 回答者が Googleアカウント を持っている必要がある / 管理者設定によっては使用できない / アップロード先フォルダを専用フォルダに設定する / ファイルが増えた場合のアーカイブ・削除ルールを事前に決める / 個人情報・機密情報が含まれる場合は共有範囲を必ず限定する / ファイルアップロードは本日の必須演習に含まない / 詳細は公式ヘルプ（Google Docs Editors Help.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S15 画面共有: Googleフォームの設問作成画面を確認する

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 実際の設問作成画面を見ると、設問タイプの切り替えと必須設定がすぐわかる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/forms-question-editor.png`（取得先: https://support.google.com/docs/answer/2839737?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S15 / Googleフォーム設計.
Slide topic for context: 画面共有: Googleフォームの設問作成画面を確認する.
Concept to visualize: 実際の設問作成画面を見ると、設問タイプの切り替えと必須設定がすぐわかる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 実際の設問作成画面を見ると、設問タイプの切り替えと必須設定がすぐわかる
**内容ブロック①：画面共有の内容**
- ダミーの問い合わせ受付フォームを開く
- 設問タイプ（短文・ラジオ・プルダウン・日付）を切り替える操作を確認する
- 必須設定・説明文の入力・選択肢の追加を確認する
**内容ブロック②：確認ポイント**
- 設問タイプの切り替えボタンの位置
- 選択肢の「その他を追加」ボタン
- 必須のトグルボタン（設問右下）
- 設問のコピー・並び替え（ドラッグ&ドロップ）
**内容ブロック③：演習への接続**
- 次のワーク（フォーム設計ワーク）でこの画面を参考にしてください
- 設問を実際に作る演習は第2回ワークで行います
Visible text candidates to use when useful, without reducing the source density: 画面共有の内容 / 確認ポイント / 演習への接続 / ダミーの問い合わせ受付フォームを開く / 設問タイプを切り替える操作を確認する / 必須設定・説明文の入力・選択肢の追加を確認する / 設問タイプの切り替えボタンの位置 / 選択肢の「その他を追加」ボタン / 必須のトグルボタン / 設問のコピー・並び替え / 次のワークでこの画面を参考にしてください / 設問を実際に作る演習は第2回ワークで行います / その他を追加.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S16 回答先スプレッドシートの作り方: 回答がそのまま台帳に流れる仕組みを作る

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 回答先Sheetsを設定するだけで、フォーム送信が台帳への自動記録になる
- 推奨図解パターン: step-list
- 参照素材・スクリーンショット: `スクリーンショット/forms-link-to-sheets.png`（取得先: https://support.google.com/docs/answer/2917686?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S16 / Googleフォーム設計.
Slide topic for context: 回答先スプレッドシートの作り方: 回答がそのまま台帳に流れる仕組みを作る.
Concept to visualize: 回答先Sheetsを設定するだけで、フォーム送信が台帳への自動記録になる.
Suggested visual pattern: step-list.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 回答先Sheetsを設定するだけで、フォーム送信が台帳への自動記録になる
**内容ブロック①：回答先シートの設定手順**
1. Googleフォームの「回答」タブを開く
2. 右上のスプレッドシートアイコン（または「スプレッドシートにリンク」）をクリック
3. 「新しいスプレッドシートを作成」または「既存のスプレッドシートを選択」
4. フォームの各設問が列名として自動生成される
5. 最初の列には「タイムスタンプ」が自動追加される
**内容ブロック②：回答先シート設定後の注意点**
| 注意点 | 内容 |
|---|---|
| 列名を変えない | 設問名が列名になるため、後から変えるとGASが誤作動する |
| 列を削除しない | フォームの設問と対応しているため削除すると不整合が起きる |
| 行を削除しない | 原本データは消さない（管理台帳で処理する） |
| シート名を変えない | GASがシート名でデータを参照する場合があるため |
**内容ブロック③：「新規」と「既存」の使い分け**
- 新しいスプレッドシートを作成: 台帳を新規に作る場合（推奨）
- 既存のスプレッドシートを選択: 複数フォームの回答を1つのSheetにまとめる場合
Visible text candidates to use when useful, without reducing the source density: 回答先シートの設定手順 / 回答先シート設定後の注意点 / 「新規」と「既存」の使い分け / 注意点 / 列名を変えない / 設問名が列名になるため、後から変えるとGASが誤作動する / 列を削除しない / フォームの設問と対応しているため削除すると不整合が起きる / 行を削除しない / 原本データは消さない / シート名を変えない / GASがシート名でデータを参照する場合があるため / 新しいスプレッドシートを作成 / 既存のスプレッドシートを選択 / 回答 / スプレッドシートにリンク / タイムスタンプ / 新規 / 既存.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S17 画面共有: フォーム送信からSheets反映を確認する

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: テスト送信して台帳に1行追加される流れを見ると、仕組みが具体的にわかる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/forms-response-to-sheets.png`（取得先: https://support.google.com/docs/answer/2917686?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S17 / Googleフォーム設計.
Slide topic for context: 画面共有: フォーム送信からSheets反映を確認する.
Concept to visualize: テスト送信して台帳に1行追加される流れを見ると、仕組みが具体的にわかる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: テスト送信して台帳に1行追加される流れを見ると、仕組みが具体的にわかる
**内容ブロック①：画面共有の内容**
- ダミーフォームに回答を入力して送信する（架空の会社名・担当者名を使用）
- 回答先スプレッドシートを開き、タイムスタンプ付きで1行追加されたことを確認する
- 設問の順番と列名の対応を確認する
**内容ブロック②：確認ポイント**
- タイムスタンプが1列目に自動追加される
- 設問と同じ順番で列が並ぶ（フォームの設問順 = Sheetsの列順）
- 入力した値がそのまま保存される（形式変換なし）
**内容ブロック③：よくある確認ミス**
- フォームの「プレビュー」で送信しても回答先Sheetsに反映される（テスト送信も記録される）
- 実業務で使う前にテスト行を1〜2行入れておくと、列の確認がしやすい
- テスト行を後で削除する場合は、原本シートではなく管理台帳のみで処理する
Visible text candidates to use when useful, without reducing the source density: 画面共有の内容 / 確認ポイント / よくある確認ミス / ダミーフォームに回答を入力して送信する / 設問の順番と列名の対応を確認する / タイムスタンプが1列目に自動追加される / 設問と同じ順番で列が並ぶ / 入力した値がそのまま保存される / フォームの「プレビュー」で送信しても回答先Sheetsに反映される / 実業務で使う前にテスト行を1〜2行入れておくと、列の確認がしやすい / プレビュー.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S18 フォーム設計ワーク（15分）

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 設問を書き出すだけで、フォームの実装と台帳の列設計が同時に進む
- 推奨図解パターン: work-instruction
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S18 / Googleフォーム設計.
Slide topic for context: フォーム設計ワーク（15分）.
Concept to visualize: 設問を書き出すだけで、フォームの実装と台帳の列設計が同時に進む.
Suggested visual pattern: work-instruction.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 設問を書き出すだけで、フォームの実装と台帳の列設計が同時に進む
**内容ブロック①：ワーク手順**
1. 動画を一時停止して、ワークシートの「フォーム設計表」を開いてください
2. 今日選んだ業務ケースのフォームで必要な設問を書き出す
3. 各設問に: 設問名 / 入力形式（設問タイプ） / 必須かどうか / 対応する台帳列名 を記入する
**内容ブロック②：記入例（ダミーデータ・問い合わせ受付フォーム）**
| 設問名 | 入力形式 | 必須 | 台帳列名 |
|---|---|---|---|
| お名前 | 短文 | 必須 | 申請者名 |
| 所属部署 | プルダウン | 必須 | 部署 |
| お問い合わせ種別 | ラジオボタン | 必須 | カテゴリ |
| 優先度 | ラジオボタン | 必須 | 優先度 |
| ご希望の対応期限 | 日付 | 任意 | 希望期限 |
| 問い合わせ内容 | 段落（長文） | 必須 | 問い合わせ内容 |
| 連絡先メールアドレス | 短文（メール形式） | 必須 | 連絡先メール |
**内容ブロック③：自己レビューのポイント**
- 設問が10個以内に収まっているか
- GAS処理やCOUNTIFで使う設問は「必須」になっているか
- 台帳列名が既存の台帳と重複・矛盾していないか
Visible text candidates to use when useful, without reducing the source density: ワーク手順 / 記入例 / 自己レビューのポイント / 設問名 / 入力形式 / お名前 / 短文 / 所属部署 / プルダウン / お問い合わせ種別 / ラジオボタン / 優先度 / ご希望の対応期限 / 日付 / 問い合わせ内容 / 段落 / 連絡先メールアドレス / 設問が10個以内に収まっているか / GAS処理やCOUNTIFで使う設問は「必須」になっているか / 台帳列名が既存の台帳と重複・矛盾していないか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S19 フォーム設計まとめ

- セクション: Googleフォーム設計
- スライド側ヘッドライン（画像内には原則入れない）: 設問設計の精度が、その後の台帳・GAS・Gemini活用のすべてに影響する
- 推奨図解パターン: summary-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S19 / Googleフォーム設計.
Slide topic for context: フォーム設計まとめ.
Concept to visualize: 設問設計の精度が、その後の台帳・GAS・Gemini活用のすべてに影響する.
Suggested visual pattern: summary-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 設問設計の精度が、その後の台帳・GAS・Gemini活用のすべてに影響する
**内容ブロック①：フォーム設計のチェックリスト**
- 1設問1情報になっているか
- 必須設問は集計・GASに必要な設問だけか
- 選択肢はマスタシートと一致しているか
- 自由記述は段落タイプで、Gemini活用前提で設計しているか
- 回答先スプレッドシートの設定が完了しているか
**内容ブロック②：次のブロックへ**
- 次は「回答先シート（原本）」と「管理台帳」の設計を行います
- 原本と管理台帳を分ける理由と設計方法を学びます
Visible text candidates to use when useful, without reducing the source density: フォーム設計のチェックリスト / 次のブロックへ / 1設問1情報になっているか / 必須設問は集計・GASに必要な設問だけか / 選択肢はマスタシートと一致しているか / 自由記述は段落タイプで、Gemini活用前提で設計しているか / 回答先スプレッドシートの設定が完了しているか / 次は「回答先シート」と「管理台帳」の設計を行います / 原本と管理台帳を分ける理由と設計方法を学びます / 回答先シート / 管理台帳.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S20 章見出し/現在位置 3/5: 回答シートと管理台帳

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 回答シートと管理台帳では、回答原本と管理台帳を分け、列・ID・ステータス・ログを設計することで、管理台帳列定義につなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S20 / 回答シートと管理台帳.
Slide topic for context: 章見出し/現在位置 3/5: 回答シートと管理台帳.
Concept to visualize: 回答シートと管理台帳では、回答原本と管理台帳を分け、列・ID・ステータス・ログを設計することで、管理台帳列定義につなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 3/5 回答シートと管理台帳
現在章の範囲: S20-S30
時間目安: 30分
前の章: Googleフォーム設計
次の章: 台帳の品質管理
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 回答シートと管理台帳では、回答原本と管理台帳を分け、列・ID・ステータス・ログを設計することで、管理台帳列定義につなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 3/5 / 回答シートと管理台帳 / S20-S30 / 30分 / 前の章 / Googleフォーム設計 / 次の章 / 台帳の品質管理.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S21 回答シートは原本、管理台帳は業務処理用: 2層構造が台帳崩壊を防ぐ

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 原本と処理用台帳を分けると、データが消えず、GASも安定して動く
- 推奨図解パターン: two-layer-structure
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S21 / 回答シートと管理台帳.
Slide topic for context: 回答シートは原本、管理台帳は業務処理用: 2層構造が台帳崩壊を防ぐ.
Concept to visualize: 原本と処理用台帳を分けると、データが消えず、GASも安定して動く.
Suggested visual pattern: two-layer-structure.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 原本と処理用台帳を分けると、データが消えず、GASも安定して動く
**内容ブロック①：2層構造の設計**
コード例:
[Googleフォーム]
  ↓ 自動保存
[回答シート（原本）] ← 触らない・消さない・列名を変えない
  ↓ IMPORTRANGE または QUERY でデータをコピー（またはGASが転記）
[管理台帳（処理用）] ← 担当者が更新・GASが書き込む
コード例:
**内容ブロック②：原本シートでやってはいけないこと**
| NG操作 | 影響 |
|---|---|
| 列名を変更する | GASが列を見つけられなくなる |
| 行を削除する | 回答データが永久に消える |
| ステータスを手入力する | 他の担当者が上書きすると履歴がなくなる |
| 結合セルを作る | 関数・GAS・Geminiが正しく読めなくなる |
| 見た目のための空行を入れる | 1行1件のルールが崩れ自動処理が誤作動する |
**内容ブロック③：業種別の2層設計例**
| 業種 | 原本シート | 管理台帳 |
|---|---|---|
| 士業 | 依頼受付フォームの回答 | 案件台帳（担当者・期限・ステータス） |
| 製造業 | 日報フォームの回答 | 生産実績台帳（集計・異常値フラグ） |
| 小売業 | 在庫報告フォームの回答 | 在庫台帳（累積・補充依頼ステータス） |
Visible text candidates to use when useful, without reducing the source density: 2層構造の設計 / 原本シートでやってはいけないこと / 業種別の2層設計例 / NG操作 / 影響 / 列名を変更する / GASが列を見つけられなくなる / 行を削除する / 回答データが永久に消える / ステータスを手入力する / 他の担当者が上書きすると履歴がなくなる / 結合セルを作る / 関数・GAS・Geminiが正しく読めなくなる / 見た目のための空行を入れる / 1行1件のルールが崩れ自動処理が誤作動する / 原本シート / 士業 / 依頼受付フォームの回答 / 製造業 / 日報フォームの回答.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S22 管理台帳に必要な基本列: 7列を設計するとGASと人が共存できる

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 受付番号・ステータス・担当者・期限の4列が揃うと、進捗管理が始められる
- 推奨図解パターン: column-definition-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S22 / 回答シートと管理台帳.
Slide topic for context: 管理台帳に必要な基本列: 7列を設計するとGASと人が共存できる.
Concept to visualize: 受付番号・ステータス・担当者・期限の4列が揃うと、進捗管理が始められる.
Suggested visual pattern: column-definition-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 受付番号・ステータス・担当者・期限の4列が揃うと、進捗管理が始められる
**内容ブロック①：管理台帳の基本7列**
| 列名 | 入力元 | 役割 | 注意点 |
|---|---|---|---|
| 受付番号 | GAS（自動付与） | 案件の一意キー | 人が変更しない |
| タイムスタンプ | フォーム自動 | 受付日時の記録 | 変更不要 |
| 申請者名 | フォーム | 依頼者の特定 | 原本から参照 |
| カテゴリ | フォーム（プルダウン） | 分類・集計・GAS分岐 | マスタと一致 |
| ステータス | 担当者（プルダウン） | 進捗の可視化 | 選択肢を固定 |
| 担当者 | 担当者（プルダウン） | 責任の明確化 | 担当者マスタと一致 |
| 対応期限 | 担当者（日付） | 期限超過の検知 | GASのアラート対象 |
**内容ブロック②：追加する列の候補**
- 完了日時（GASが記録）
- 通知ログ（GASが送信したメールのタイムスタンプ）
- 対応メモ（担当者が記入）
- AIラベル（Geminiの分類結果・人が確認後確定）
**内容ブロック③：列を追加する順序**
1. まず基本7列を作る
2. 第3回（GAS実装）後に通知ログ・完了日時を追加する
3. 第4回（Gemini）後にAIラベル列を追加する
Visible text candidates to use when useful, without reducing the source density: 管理台帳の基本7列 / 追加する列の候補 / 列を追加する順序 / 受付番号 / GAS / タイムスタンプ / フォーム自動 / 申請者名 / フォーム / カテゴリ / ステータス / 担当者 / 対応期限 / 完了日時 / 通知ログ / 対応メモ / AIラベル.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S23 ID設計: 受付番号を持つことで管理・追跡・GAS処理が安定する

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 受付番号があると、メール・台帳・添付ファイルの3点が一致して管理できる
- 推奨図解パターン: id-design-pattern
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S23 / 回答シートと管理台帳.
Slide topic for context: ID設計: 受付番号を持つことで管理・追跡・GAS処理が安定する.
Concept to visualize: 受付番号があると、メール・台帳・添付ファイルの3点が一致して管理できる.
Suggested visual pattern: id-design-pattern.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 受付番号があると、メール・台帳・添付ファイルの3点が一致して管理できる
**内容ブロック①：受付番号の設計パターン**
| フォーマット例 | 意味 | 用途 |
|---|---|---|
| `INQ-20260521-001` | 問い合わせ-日付-連番 | 問い合わせ受付 |
| `REQ-2026-0521-001` | 申請-年-月日-連番 | 各種申請管理 |
| `RPT-2026W21-001` | 報告-年-週-連番 | 週次報告 |
| `INV-202605-001` | 在庫-年月-連番 | 在庫報告 |
**内容ブロック②：受付番号を使うメリット**
- メールの件名に番号を入れると、どの案件の連絡か即わかる
- 添付ファイルの名前に番号を入れると、台帳との紐付けが確実になる
- GASがSheetsを処理するとき、番号でユニークに行を特定できる
**内容ブロック③：GASによる自動付与（第3回の実装内容）**
- 新しい行が追加されたとき（フォーム送信）にGASが番号を自動付与する
- 今日は「採番ルール」を設計するだけ（実装は第3回）
- 今日の台帳設計表に「受付番号列」「入力元: GAS（自動）」と記入しておく
Visible text candidates to use when useful, without reducing the source density: 受付番号の設計パターン / 受付番号を使うメリット / GASによる自動付与 / フォーマット例 / 意味 / INQ-20260521-001 / 問い合わせ-日付-連番 / REQ-2026-0521-001 / 申請-年-月日-連番 / RPT-2026W21-001 / 報告-年-週-連番 / INV-202605-001 / 在庫-年月-連番 / メールの件名に番号を入れると、どの案件の連絡か即わかる / 添付ファイルの名前に番号を入れると、台帳との紐付けが確実になる / GASがSheetsを処理するとき、番号でユニークに行を特定できる / 新しい行が追加されたときにGASが番号を自動付与する / 今日は「採番ルール」を設計するだけ / 今日の台帳設計表に「受付番号列」「入力元 / 採番ルール.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S24 ステータス設計: 状態を明確にすると進捗管理と自動化が動き出す

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: ステータスの選択肢を固定すると、集計・GAS分岐・条件付き書式が全部使える
- 推奨図解パターン: state-transition
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S24 / 回答シートと管理台帳.
Slide topic for context: ステータス設計: 状態を明確にすると進捗管理と自動化が動き出す.
Concept to visualize: ステータスの選択肢を固定すると、集計・GAS分岐・条件付き書式が全部使える.
Suggested visual pattern: state-transition.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: ステータスの選択肢を固定すると、集計・GAS分岐・条件付き書式が全部使える
**内容ブロック①：ステータスの標準設計（問い合わせ受付の例）**
| ステータス | 意味 | 次のアクション |
|---|---|---|
| 新規 | 受付したが担当者未割当 | 担当者を割り当てる |
| 確認中 | 担当者が内容を確認中 | 対応可否を判断する |
| 対応中 | 担当者が対応を開始 | 完了または保留へ |
| 保留 | 回答待ち・情報待ち | 期日をセットして待機 |
| 完了 | 対応が完了した | GASが完了メールを送信 |
| 差戻し | 情報不足・要再申請 | 申請者に差戻しメールを送信 |
| 取消 | キャンセルされた | 理由を対応メモに記載 |
**内容ブロック②：ステータス設計のルール**
- ステータスは**プルダウン（入力規則）**で選択式にする
- 自由記述にしない（「対応中です」「確認してます」などが混在する）
- GASの通知トリガーとして使う（「完了」になったらメールを送信など）
**内容ブロック③：業種別のステータス設計例**
| 業種 | ステータス候補 |
|---|---|
| 士業（案件管理） | 受付・書類確認中・作業中・確認待ち・完了・保留 |
| 製造業（日報） | 入力済み・確認済み・修正依頼・承認済み |
| 小売業（在庫報告） | 報告済み・補充手配中・完了 |
Visible text candidates to use when useful, without reducing the source density: ステータスの標準設計 / ステータス設計のルール / 業種別のステータス設計例 / ステータス / 意味 / 新規 / 受付したが担当者未割当 / 確認中 / 担当者が内容を確認中 / 対応中 / 担当者が対応を開始 / 保留 / 回答待ち・情報待ち / 完了 / 対応が完了した / 差戻し / 情報不足・要再申請 / 取消 / キャンセルされた / ステータスはプルダウンで選択式にする.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S25 担当者と期限の列: 誰がいつまでに対応するかを台帳で明確にする

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 担当者列と期限列が揃うと、「誰が何をすべきか」が画面を見るだけでわかる
- 推奨図解パターン: responsibility-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S25 / 回答シートと管理台帳.
Slide topic for context: 担当者と期限の列: 誰がいつまでに対応するかを台帳で明確にする.
Concept to visualize: 担当者列と期限列が揃うと、「誰が何をすべきか」が画面を見るだけでわかる.
Suggested visual pattern: responsibility-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 担当者列と期限列が揃うと、「誰が何をすべきか」が画面を見るだけでわかる
**内容ブロック①：担当者列の設計**
- プルダウンで担当者名を選択式にする（自由記述にしない）
- 担当者マスタシートを別シートで管理する（氏名・メールアドレス・部署）
- GASが「担当者列のメールアドレス」を使って通知メールを送信する（第3回）
**内容ブロック②：期限列の設計**
- 日付型（YYYY/MM/DD）で統一する（「来週」「月末」などのテキストは使わない）
- 優先度と期限をセットで管理する（高優先度は翌日、中は3日、低は1週間など）
- 条件付き書式で「期限超過」「期限3日前」を色で表示する（後述）
**内容ブロック③：責任分担の明示**
| 役割 | 台帳上の操作 |
|---|---|
| 受付担当者 | 新規行を確認し担当者と期限を入力する |
| 対応担当者 | ステータスを更新し対応メモを記入する |
| 管理者 | 未対応・期限超過を定期的に確認する |
| GAS | 新規受付通知・完了通知を自動送信する |
Visible text candidates to use when useful, without reducing the source density: 担当者列の設計 / 期限列の設計 / 責任分担の明示 / プルダウンで担当者名を選択式にする / 担当者マスタシートを別シートで管理する / GASが「担当者列のメールアドレス」を使って通知メールを送信する / 日付型で統一する / 優先度と期限をセットで管理する / 条件付き書式で「期限超過」「期限3日前」を色で表示する / 役割 / 台帳上の操作 / 受付担当者 / 新規行を確認し担当者と期限を入力する / 対応担当者 / ステータスを更新し対応メモを記入する / 管理者 / 未対応・期限超過を定期的に確認する / GAS / 新規受付通知・完了通知を自動送信する / 誰が何をすべきか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S26 AIに渡す列・人が更新する列・GASで追記する列を分ける

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 列の入力元を混ぜないと、自動処理が崩れず、誰でも台帳を運用できる
- 推奨図解パターン: color-coded-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S26 / 回答シートと管理台帳.
Slide topic for context: AIに渡す列・人が更新する列・GASで追記する列を分ける.
Concept to visualize: 列の入力元を混ぜないと、自動処理が崩れず、誰でも台帳を運用できる.
Suggested visual pattern: color-coded-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 列の入力元を混ぜないと、自動処理が崩れず、誰でも台帳を運用できる
**内容ブロック①：列の入力元別管理**
| 列グループ | 列名の例 | 誰が更新するか | 色コードの例 |
|---|---|---|---|
| フォーム列 | タイムスタンプ・申請者名・問い合わせ内容 | 触らない（フォーム自動） | グレー（保護） |
| GAS列 | 受付番号・通知送信日時・完了日時 | 触らない（GAS自動） | ブルー（保護） |
| 担当者列 | ステータス・担当者名・期限・対応メモ | 担当者が手動更新 | ホワイト（編集可） |
| AI列 | Geminiラベル・要約テキスト | 人が確認後に確定 | イエロー（要確認） |
**内容ブロック②：色コード管理のメリット**
- 誰でも「どの列を更新すべきか」が一目でわかる
- GASが書く列に人が入力するミスを防げる
- 第4回（Gemini）後に「AI列」を追加するときも混乱しない
**内容ブロック③：シート保護機能との組み合わせ**
- Googleスプレッドシートの「シートとセルの保護」でフォーム列・GAS列を保護する
- 担当者は担当者列のみ編集できる設定にする（第3回で実装）
Visible text candidates to use when useful, without reducing the source density: 列の入力元別管理 / 色コード管理のメリット / シート保護機能との組み合わせ / 列グループ / 列名の例 / フォーム列 / タイムスタンプ・申請者名・問い合わせ内容 / GAS列 / 受付番号・通知送信日時・完了日時 / 担当者列 / ステータス・担当者名・期限・対応メモ / AI列 / Geminiラベル・要約テキスト / 誰でも「どの列を更新すべきか」が一目でわかる / GASが書く列に人が入力するミスを防げる / 第4回後に「AI列」を追加するときも混乱しない / 担当者は担当者列のみ編集できる設定にする / どの列を更新すべきか / シートとセルの保護.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S27 ログ列とエラー列: 自動処理の履歴を残すと運用が安定する

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: ログを残すことで、GASが誤作動したときに原因を特定して復旧できる
- 推奨図解パターン: log-column-design
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S27 / 回答シートと管理台帳.
Slide topic for context: ログ列とエラー列: 自動処理の履歴を残すと運用が安定する.
Concept to visualize: ログを残すことで、GASが誤作動したときに原因を特定して復旧できる.
Suggested visual pattern: log-column-design.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: ログを残すことで、GASが誤作動したときに原因を特定して復旧できる
**内容ブロック①：ログ列の設計**
| ログ列名 | 記録する内容 | 入力元 |
|---|---|---|
| 受付通知送信日時 | 受付確認メールを送った日時 | GAS |
| 完了通知送信日時 | 完了メールを送った日時 | GAS |
| 処理結果 | 成功・エラーの結果メッセージ | GAS |
| エラー内容 | エラーが発生した場合の詳細 | GAS |
| 手動復旧メモ | 手動で対処した場合の記録 | 担当者 |
**内容ブロック②：ログ列の活用場面**
- 「受付メールが届いていない」と言われたとき → 送信日時列で確認
- GASが動いていない疑いがあるとき → 処理結果列でエラーを確認
- 誰が何をしたかの履歴が必要なとき → 更新日時・更新者列を参照
**内容ブロック③：今日の台帳設計での扱い**
- ログ列は台帳列定義表に「GAS追加予定列」として記入しておく
- 実際のログ列の実装は第3回（GAS設計）で行う
Visible text candidates to use when useful, without reducing the source density: ログ列の設計 / ログ列の活用場面 / 今日の台帳設計での扱い / ログ列名 / 記録する内容 / 受付通知送信日時 / 受付確認メールを送った日時 / 完了通知送信日時 / 完了メールを送った日時 / 処理結果 / 成功・エラーの結果メッセージ / エラー内容 / エラーが発生した場合の詳細 / 手動復旧メモ / 手動で対処した場合の記録 / 「受付メールが届いていない」と言われたとき → 送信日時列で確認 / GASが動いていない疑いがあるとき → 処理結果列でエラーを確認 / 誰が何をしたかの履歴が必要なとき → 更新日時・更新者列を参照 / ログ列は台帳列定義表に「GAS追加予定列」として記入しておく / 実際のログ列の実装は第3回で行う.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S28 画面共有: 回答シートから管理台帳を設計する

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 原本と管理台帳を並べると、どの列を追加すべきかが一目でわかる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/sheets-two-layer-design.png`（取得先: ダミー環境のSheetsをスクリーンショット）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S28 / 回答シートと管理台帳.
Slide topic for context: 画面共有: 回答シートから管理台帳を設計する.
Concept to visualize: 原本と管理台帳を並べると、どの列を追加すべきかが一目でわかる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 原本と管理台帳を並べると、どの列を追加すべきかが一目でわかる
**内容ブロック①：画面共有の内容**
- ダミーの回答シート（原本）と管理台帳を並べて表示する
- 原本シートの列構成を確認する（フォームの設問 = 列名）
- 管理台帳に「ステータス・担当者・期限・受付番号・ログ列」を追加する操作を確認する
**内容ブロック②：確認ポイント**
- 原本シートと管理台帳が別シートになっているか
- 管理台帳の列名が統一されているか（スペース・大文字小文字の揺れがないか）
- ステータス・担当者がプルダウンになっているか（入力規則の設定）
**内容ブロック③：演習への接続**
- 次のワーク（台帳列設計ワーク）でこの画面を参考にしてください
Visible text candidates to use when useful, without reducing the source density: 画面共有の内容 / 確認ポイント / 演習への接続 / ダミーの回答シートと管理台帳を並べて表示する / 原本シートの列構成を確認する / 原本シートと管理台帳が別シートになっているか / 管理台帳の列名が統一されているか / ステータス・担当者がプルダウンになっているか / 次のワークでこの画面を参考にしてください.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S29 台帳列設計ワーク（10分）

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 列ごとに入力元と更新者を決めると、GASを入れたときに台帳が崩れない
- 推奨図解パターン: work-instruction
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S29 / 回答シートと管理台帳.
Slide topic for context: 台帳列設計ワーク（10分）.
Concept to visualize: 列ごとに入力元と更新者を決めると、GASを入れたときに台帳が崩れない.
Suggested visual pattern: work-instruction.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 列ごとに入力元と更新者を決めると、GASを入れたときに台帳が崩れない
**内容ブロック①：ワーク手順**
1. 動画を一時停止して、ワークシートの「台帳列定義表」を開いてください
2. フォーム設計表で書いた設問を台帳列として転記する
3. 各列に: 列名 / 入力元（フォーム・担当者・GAS・AI） / 更新者 / 用途 / 注意点 を記入する
4. GAS追加予定列（受付番号・ログ列）も記入しておく
**内容ブロック②：記入例（ダミーデータ・問い合わせ受付台帳）**
| 列名 | 入力元 | 更新者 | 用途 | 注意点 |
|---|---|---|---|---|
| タイムスタンプ | フォーム | 変更不可 | 受付日時の記録 | 列名・順序を変えない |
| 申請者名 | フォーム | 変更不可 | 依頼者の特定 | 原本から参照 |
| カテゴリ | フォーム（プルダウン） | 変更不可 | GAS分岐・集計 | マスタと一致させる |
| 受付番号 | GAS（自動） | 変更不可 | 案件の一意キー | 第3回で実装 |
| ステータス | 担当者（プルダウン） | 担当者 | 進捗管理 | 選択肢を固定する |
| 担当者 | 担当者（プルダウン） | 担当者 | 責任明確化 | 担当者マスタと一致 |
| 対応期限 | 担当者（日付型） | 担当者 | 期限管理・アラート | YYYY/MM/DD統一 |
**内容ブロック③：自己レビュー**
- フォーム列・GAS列・担当者列が分けて設計されているか
- GAS追加予定列が記入されているか
- 今日設計した台帳を第3回のGAS実装に使います
Visible text candidates to use when useful, without reducing the source density: ワーク手順 / 記入例 / 自己レビュー / タイムスタンプ / フォーム / 申請者名 / カテゴリ / 受付番号 / GAS / ステータス / 担当者 / 対応期限 / フォーム列・GAS列・担当者列が分けて設計されているか / GAS追加予定列が記入されているか / 今日設計した台帳を第3回のGAS実装に使います / 台帳列定義表.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S30 台帳列設計まとめ

- セクション: 回答シートと管理台帳
- スライド側ヘッドライン（画像内には原則入れない）: 列の入力元を設計した段階で、GASが読める台帳の土台が完成する
- 推奨図解パターン: summary-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S30 / 回答シートと管理台帳.
Slide topic for context: 台帳列設計まとめ.
Concept to visualize: 列の入力元を設計した段階で、GASが読める台帳の土台が完成する.
Suggested visual pattern: summary-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 列の入力元を設計した段階で、GASが読める台帳の土台が完成する
**内容ブロック①：まとめポイント**
- 原本シートは触らない・消さない・列名を変えない
- 管理台帳は処理用: ステータス・担当者・期限・ログ列を追加する
- 列を入力元（フォーム・GAS・担当者・AI）で色分けすると運用ミスが減る
- 受付番号とログ列は「GAS追加予定」として記入しておく
**内容ブロック②：次のブロックへ**
- 次は「台帳の品質を保つための入力規則・条件付き書式・集計」を設定します
Visible text candidates to use when useful, without reducing the source density: まとめポイント / 次のブロックへ / 原本シートは触らない・消さない・列名を変えない / 管理台帳は処理用 / 列を入力元で色分けすると運用ミスが減る / 受付番号とログ列は「GAS追加予定」として記入しておく / GAS追加予定.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S31 章見出し/現在位置 4/5: 台帳の品質管理

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 台帳の品質管理では、1行1件・1列1項目を守り、入力規則・集計列で台帳崩壊を防ぐことで、台帳品質チェックにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S31 / 台帳の品質管理.
Slide topic for context: 章見出し/現在位置 4/5: 台帳の品質管理.
Concept to visualize: 台帳の品質管理では、1行1件・1列1項目を守り、入力規則・集計列で台帳崩壊を防ぐことで、台帳品質チェックにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 4/5 台帳の品質管理
現在章の範囲: S31-S38
時間目安: 20分
前の章: 回答シートと管理台帳
次の章: 権限・保存設計と演習レビュー
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 台帳の品質管理では、1行1件・1列1項目を守り、入力規則・集計列で台帳崩壊を防ぐことで、台帳品質チェックにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 4/5 / 台帳の品質管理 / S31-S38 / 20分 / 前の章 / 回答シートと管理台帳 / 次の章 / 権限・保存設計と演習レビュー.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S32 Excelスタイルの表設計を直す: 台帳を崩す4つのパターン

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 結合セル・空行・複数情報セルを使うと、関数もGASもAIも正しく読めなくなる
- 推奨図解パターン: before-after-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S32 / 台帳の品質管理.
Slide topic for context: Excelスタイルの表設計を直す: 台帳を崩す4つのパターン.
Concept to visualize: 結合セル・空行・複数情報セルを使うと、関数もGASもAIも正しく読めなくなる.
Suggested visual pattern: before-after-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 結合セル・空行・複数情報セルを使うと、関数もGASもAIも正しく読めなくなる
**内容ブロック①：Before/After 対比**
| NG（Excelスタイル） | OK（Sheetsデータ台帳） |
|---|---|
| セルの結合（見出し・月名） | 結合しない（1行1件を徹底） |
| 見た目のための空行 | 空行を入れない |
| 1セルに「担当者/期限」 | 担当者列と期限列を分ける |
| 列名なし・行番号で管理 | 1行目に列名を固定する |
| 月ごとにシートを分ける | 1シートで年通し管理・年月列を作る |
**内容ブロック②：よくある間違いの業種別例**
| 業種 | NGパターン | 修正方法 |
|---|---|---|
| サービス業 | 月別にシートを分けて管理 | 1シートに「月」列を追加 |
| 製造業 | ロット・機種を1セルに入力 | ロット列・機種列を分ける |
| 士業 | 顧客名と担当者を結合セルで表示 | 顧客列・担当者列に分ける |
**内容ブロック③：整備前と整備後のGASの動き**
- 整備前: 結合セル・空行でGASが行数を正確に読めず誤作動する
- 整備後: 1行1件・列名固定でGASがデータを確実に処理できる
Visible text candidates to use when useful, without reducing the source density: Before/After 対比 / よくある間違いの業種別例 / 整備前と整備後のGASの動き / NG / OK / セルの結合 / 結合しない / 見た目のための空行 / 空行を入れない / 1セルに「担当者/期限」 / 担当者列と期限列を分ける / 列名なし・行番号で管理 / 1行目に列名を固定する / 月ごとにシートを分ける / 1シートで年通し管理・年月列を作る / NGパターン / サービス業 / 月別にシートを分けて管理 / 製造業 / ロット・機種を1セルに入力.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S33 1行1件・1列1項目の原則: 集計・GAS・AI貼り付けに強い台帳を作る

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 1行1件・1列1項目で整えたデータは、関数・GAS・AIが最速で処理できる
- 推奨図解パターン: basic-principle-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S33 / 台帳の品質管理.
Slide topic for context: 1行1件・1列1項目の原則: 集計・GAS・AI貼り付けに強い台帳を作る.
Concept to visualize: 1行1件・1列1項目で整えたデータは、関数・GAS・AIが最速で処理できる.
Suggested visual pattern: basic-principle-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 1行1件・1列1項目で整えたデータは、関数・GAS・AIが最速で処理できる
**内容ブロック①：原則と効果**
| 原則 | 守ると使えるもの | 守らないと困ること |
|---|---|---|
| 1行1件 | FILTER・QUERY・GASのgetValues() | 行の読み取りがズレてエラー |
| 1列1項目 | VLOOKUP・INDEX/MATCH・GASのgetRange() | 列番号がズレてデータ混在 |
| 列名を1行目に固定 | GASのgetHeaderValues()・AI貼り付け | ヘッダーなしでAIが誤分類 |
| 選択肢を統一する | COUNTIF・ピボット・GASのif分岐 | 「対応中」「対応中 」で別カウント |
**内容ブロック②：データ整備のチェック手順**
1. A列から最終列まで列名が1行目に入っているか確認する
2. 空行・結合セルを探して削除・解除する
3. ステータス・カテゴリ列に表記ゆれがないか確認する（COUNTIF等で確認）
4. 日付列がYYYY/MM/DD形式で統一されているか確認する
**内容ブロック③：整備の順序**
- まず: 1行1件・列名固定を確認する
- 次に: ステータス・カテゴリをプルダウンで入力規則を設定する
- 最後: 期限列の形式を統一する
Visible text candidates to use when useful, without reducing the source density: 原則と効果 / データ整備のチェック手順 / 整備の順序 / 守ると使えるもの / 1行1件 / FILTER・QUERY・GASのgetValues / 1列1項目 / VLOOKUP・INDEX/MATCH・GASのgetRange / 列名を1行目に固定 / GASのgetHeaderValues・AI貼り付け / 選択肢を統一する / COUNTIF・ピボット・GASのif分岐 / まず / 次に / 最後 / 対応中.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S34 マスタシートを作る: カテゴリ・担当者・ステータスを別シートで管理する

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: マスタシートを持つと、選択肢の変更が全台帳に一括反映される
- 推奨図解パターン: layer-structure
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S34 / 台帳の品質管理.
Slide topic for context: マスタシートを作る: カテゴリ・担当者・ステータスを別シートで管理する.
Concept to visualize: マスタシートを持つと、選択肢の変更が全台帳に一括反映される.
Suggested visual pattern: layer-structure.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: マスタシートを持つと、選択肢の変更が全台帳に一括反映される
**内容ブロック①：マスタシートの構成**
| シート名 | 内容 | 使う場所 |
|---|---|---|
| マスタ_カテゴリ | カテゴリ名一覧（製品・サービス・請求・その他） | フォームの選択肢・台帳のプルダウン |
| マスタ_担当者 | 担当者名・メールアドレス・部署 | 台帳の担当者プルダウン・GASの送信先 |
| マスタ_ステータス | ステータス一覧と説明 | 台帳のステータスプルダウン |
| マスタ_優先度 | 優先度と対応期限の目安 | 台帳の優先度・期限設定の基準 |
**内容ブロック②：マスタシートのメリット**
- 選択肢を変えるときはマスタシートを更新するだけ（台帳のプルダウンに自動反映）
- GASがマスタシートを参照してメール送信先を決める（第3回で実装）
- Geminiの分類ラベルをマスタと一致させると、集計がそのまま使える
**内容ブロック③：マスタシートの命名規則**
- シート名: 「マスタ_」プレフィックスをつける（マスタ_カテゴリ・マスタ_担当者）
- 列名: 「コード」「名称」「説明」の3列を基本にする
- 更新日列を入れておくと変更履歴が残る
Visible text candidates to use when useful, without reducing the source density: マスタシートの構成 / マスタシートのメリット / マスタシートの命名規則 / シート名 / マスタ_カテゴリ / カテゴリ名一覧 / マスタ_担当者 / 担当者名・メールアドレス・部署 / マスタ_ステータス / ステータス一覧と説明 / マスタ_優先度 / 優先度と対応期限の目安 / 選択肢を変えるときはマスタシートを更新するだけ / GASがマスタシートを参照してメール送信先を決める / Geminiの分類ラベルをマスタと一致させると、集計がそのまま使える / 更新日列を入れておくと変更履歴が残る / マスタ_ / コード / 名称 / 説明.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S35 入力規則とプルダウン: ステータス・担当者・カテゴリを選択式にする

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 入力規則で選択式にするだけで、表記ゆれゼロの台帳が自動で維持される
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/sheets-data-validation.png`（取得先: https://support.google.com/docs/answer/186103?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S35 / 台帳の品質管理.
Slide topic for context: 入力規則とプルダウン: ステータス・担当者・カテゴリを選択式にする.
Concept to visualize: 入力規則で選択式にするだけで、表記ゆれゼロの台帳が自動で維持される.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 入力規則で選択式にするだけで、表記ゆれゼロの台帳が自動で維持される
**内容ブロック①：Sheetsの入力規則の設定方法**
1. プルダウンにしたい列を選択する
2. メニュー「データ」→「データの入力規則」を開く
3. 「リストを直接指定」または「リストをセル範囲から取得」を選ぶ
4. 「リストをセル範囲から取得」でマスタシートを参照すると自動連動する
**内容ブロック②：入力規則を使う列の例**
| 列 | 入力規則の種類 | 選択肢の参照先 |
|---|---|---|
| ステータス | リストをセル範囲から取得 | マスタ_ステータス!A列 |
| 担当者 | リストをセル範囲から取得 | マスタ_担当者!A列（氏名） |
| カテゴリ | リストをセル範囲から取得 | マスタ_カテゴリ!A列 |
| 優先度 | リストを直接指定 | 高・中・低 |
| 対応期限 | 日付（YYYY/MM/DD以外は拒否） | 入力形式を制限 |
**内容ブロック③：入力規則の注意点**
- リストを直接指定した場合、選択肢が変わったら手動で更新が必要
- マスタシート参照を使うと、マスタを更新すれば全列のプルダウンに反映される
- GASがデータを書き込む列には入力規則を設定しない（GASの書き込みが無効化される場合がある）
Visible text candidates to use when useful, without reducing the source density: Sheetsの入力規則の設定方法 / 入力規則を使う列の例 / 入力規則の注意点 / 列 / 入力規則の種類 / ステータス / リストをセル範囲から取得 / 担当者 / カテゴリ / 優先度 / リストを直接指定 / 対応期限 / 日付 / リストを直接指定した場合、選択肢が変わったら手動で更新が必要 / GASがデータを書き込む列には入力規則を設定しない / データ / データの入力規則.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S36 条件付き書式で見える化する: 期限超過・未対応・差戻しを色で通知する

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 色で状態が一目でわかると、目視確認の時間がゼロになる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/sheets-conditional-formatting.png`（取得先: https://support.google.com/docs/answer/78413?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S36 / 台帳の品質管理.
Slide topic for context: 条件付き書式で見える化する: 期限超過・未対応・差戻しを色で通知する.
Concept to visualize: 色で状態が一目でわかると、目視確認の時間がゼロになる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 色で状態が一目でわかると、目視確認の時間がゼロになる
**内容ブロック①：条件付き書式の設定例**
| 条件 | 色設定 | 数式または条件 |
|---|---|---|
| 期限超過 | 赤（背景） | `=AND(E2<>"完了", D2<TODAY())` |
| 期限3日前 | 黄（背景） | `=AND(E2<>"完了", D2<=TODAY()+3)` |
| ステータス「新規」 | 薄青（背景） | `=E2="新規"` |
| ステータス「保留」 | オレンジ（背景） | `=E2="保留"` |
| 担当者未割当 | グレー（背景） | `=F2=""` |
**内容ブロック②：条件付き書式の設定手順**
1. 色をつけたい列・行を選択する
2. メニュー「表示形式」→「条件付き書式」を開く
3. 「カスタム数式」を選んで条件式を入力する
4. 色を設定して「完了」をクリック
**内容ブロック③：業種別の条件付き書式の活用例**
| 業種 | 条件 | 色 |
|---|---|---|
| 士業 | 申告期限30日前 | オレンジ |
| 製造業 | 異常値フラグが「あり」 | 赤 |
| 小売業 | 在庫数が基準値以下 | 黄 |
Visible text candidates to use when useful, without reducing the source density: 条件付き書式の設定例 / 条件付き書式の設定手順 / 業種別の条件付き書式の活用例 / 条件 / 色設定 / 期限超過 / 赤 / 期限3日前 / 黄 / ステータス「新規」 / 薄青 / ステータス「保留」 / オレンジ / 担当者未割当 / グレー / 士業 / 申告期限30日前 / 製造業 / 異常値フラグが「あり」 / 小売業.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S37 集計列の使い方: COUNTIF・FILTER・QUERYで未対応を一瞬で把握する

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 集計式を1本入れるだけで、担当者別・カテゴリ別の状況が自動更新される
- 推奨図解パターン: function-comparison-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S37 / 台帳の品質管理.
Slide topic for context: 集計列の使い方: COUNTIF・FILTER・QUERYで未対応を一瞬で把握する.
Concept to visualize: 集計式を1本入れるだけで、担当者別・カテゴリ別の状況が自動更新される.
Suggested visual pattern: function-comparison-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 集計式を1本入れるだけで、担当者別・カテゴリ別の状況が自動更新される
**内容ブロック①：よく使う集計関数の使い分け**
| 関数 | 用途 | 例 |
|---|---|---|
| COUNTIF | 条件に一致する件数を数える | `=COUNTIF(E:E,"新規")` → 新規件数 |
| COUNTIFS | 複数条件で件数を数える | `=COUNTIFS(F:F,"担当者A",E:E,"対応中")` |
| SUMIF | 条件に一致する数値を合計する | `=SUMIF(D:D,TODAY(),G:G)` → 今日の件数 |
| FILTER | 条件を満たす行を抽出する | `=FILTER(A:G,E:E="新規")` → 新規のみ表示 |
| QUERY | SQL風に条件・並び替え・集計をする | 複雑な抽出に使う |
| ピボット | ドラッグで集計軸を切り替える | カテゴリ×ステータスの件数表 |
**内容ブロック②：ダッシュボードシートの設計例**
- A列: KPI名（新規件数・対応中・期限超過・完了件数）
- B列: COUNTIF式を入れる
- C列: 目標値を手入力する
- D列: 達成状況を条件付き書式で色表示する
**内容ブロック③：集計は「最初から作りすぎない」**
- 最初は「件数・未対応・期限超過・担当者別」の4指標で十分
- 第5回（運用設計）でKPIダッシュボードを整える
- 集計式は台帳の列名に依存するため、列名を変えると壊れる点に注意
Visible text candidates to use when useful, without reducing the source density: よく使う集計関数の使い分け / ダッシュボードシートの設計例 / 集計は「最初から作りすぎない」 / 関数 / 用途 / COUNTIF / 条件に一致する件数を数える / COUNTIFS / 複数条件で件数を数える / SUMIF / 条件に一致する数値を合計する / FILTER / 条件を満たす行を抽出する / QUERY / SQL風に条件・並び替え・集計をする / ピボット / ドラッグで集計軸を切り替える / A列 / B列 / C列.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S38 画面共有: 入力規則・条件付き書式・集計式を確認する

- セクション: 台帳の品質管理
- スライド側ヘッドライン（画像内には原則入れない）: 実際の設定画面を見ると、3つの機能が10分以内に設定できることがわかる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/sheets-validation-formatting-formula.png`（取得先: ダミー環境のSheetsをスクリーンショット）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S38 / 台帳の品質管理.
Slide topic for context: 画面共有: 入力規則・条件付き書式・集計式を確認する.
Concept to visualize: 実際の設定画面を見ると、3つの機能が10分以内に設定できることがわかる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 実際の設定画面を見ると、3つの機能が10分以内に設定できることがわかる
**内容ブロック①：画面共有の内容**
- ダミー台帳にステータス列のプルダウン（入力規則）を設定する操作を確認する
- 期限超過行に条件付き書式で赤色をつける設定を確認する
- ダッシュボードシートにCOUNTIFで件数集計式を入れる操作を確認する
**内容ブロック②：確認ポイント**
- 入力規則: マスタシートを参照する設定の操作手順
- 条件付き書式: カスタム数式の入力方法
- COUNTIF: 列全体を参照する場合のセル範囲の書き方（`E:E` vs `E2:E100`）
**内容ブロック③：演習での活用**
- 今日のワーク後に、自分の台帳にこの設定を追加してみてください
- 実装は第3回のGAS実装と同時に行うことも可能
Visible text candidates to use when useful, without reducing the source density: 画面共有の内容 / 確認ポイント / 演習での活用 / ダミー台帳にステータス列のプルダウンを設定する操作を確認する / 期限超過行に条件付き書式で赤色をつける設定を確認する / 入力規則 / 条件付き書式 / COUNTIF / 今日のワーク後に、自分の台帳にこの設定を追加してみてください / 実装は第3回のGAS実装と同時に行うことも可能.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S39 章見出し/現在位置 5/5: 権限・保存設計と演習レビュー

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 権限・保存設計と演習レビューでは、Drive権限・保存場所・命名規則を決め、第3回のGAS準備を終えることで、権限・保存設計メモにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S39 / 権限・保存設計と演習レビュー.
Slide topic for context: 章見出し/現在位置 5/5: 権限・保存設計と演習レビュー.
Concept to visualize: 権限・保存設計と演習レビューでは、Drive権限・保存場所・命名規則を決め、第3回のGAS準備を終えることで、権限・保存設計メモにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 5/5 権限・保存設計と演習レビュー
現在章の範囲: S39-S45
時間目安: 25分
前の章: 台帳の品質管理
次の章: まとめ・実務への接続
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 権限・保存設計と演習レビューでは、Drive権限・保存場所・命名規則を決め、第3回のGAS準備を終えることで、権限・保存設計メモにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 5/5 / 権限・保存設計と演習レビュー / S39-S45 / 25分 / 前の章 / 台帳の品質管理 / 次の章 / まとめ・実務へ接続.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S40 権限設計の基本: 閲覧・コメント・編集・オーナーの4段階を使い分ける

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 権限を設計してから共有すると、データの誤操作と情報漏洩を同時に防げる
- 推奨図解パターン: permission-table
- 参照素材・スクリーンショット: `スクリーンショット/drive-share-settings.png`（取得先: https://support.google.com/drive/answer/2494822?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S40 / 権限・保存設計と演習レビュー.
Slide topic for context: 権限設計の基本: 閲覧・コメント・編集・オーナーの4段階を使い分ける.
Concept to visualize: 権限を設計してから共有すると、データの誤操作と情報漏洩を同時に防げる.
Suggested visual pattern: permission-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 権限を設計してから共有すると、データの誤操作と情報漏洩を同時に防げる
**内容ブロック①：Google Drive・Sheetsの権限一覧**
| 権限レベル | できること | 向いている相手 |
|---|---|---|
| 閲覧者 | ファイルを読むだけ | 報告を受け取るだけの管理者・他部署 |
| コメント可 | コメントを追加・返信 | フィードバックが欲しい場合 |
| 編集者 | ファイルを編集・追加 | 台帳を更新する担当者 |
| オーナー | 削除・権限変更・完全管理 | フォーム・台帳の管理担当者のみ |
**内容ブロック②：業務別の権限設計例**
| ファイル | 閲覧者 | 編集者 | オーナー |
|---|---|---|---|
| フォーム | 回答者（権限不要） | 共同編集者 | フォーム作成担当者 |
| 原本シート | 管理者のみ | 権限付与しない | フォーム作成担当者 |
| 管理台帳 | 関係者全員 | 対応担当者 | 台帳管理担当者 |
| マスタシート | 全員（閲覧） | 管理者のみ | 管理者 |
**内容ブロック③：「全員に公開」にしない理由**
- 個人情報・業務データが外部に流出するリスクがある
- 誰でも編集できると台帳の列名・ステータスが書き変えられる
- リンクを知っていれば誰でも見られる設定は、公開情報のみに使う
Visible text candidates to use when useful, without reducing the source density: Google Drive・Sheetsの権限一覧 / 業務別の権限設計例 / 「全員に公開」にしない理由 / 権限レベル / できること / 閲覧者 / ファイルを読むだけ / コメント可 / コメントを追加・返信 / 編集者 / ファイルを編集・追加 / オーナー / 削除・権限変更・完全管理 / ファイル / フォーム / 回答者 / 原本シート / 管理者のみ / 管理台帳 / 関係者全員.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S41 フォーム共同編集と回答先Sheetsの注意点

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: フォームの共同編集者は回答先Sheetsにもアクセスできるため、権限管理を明示する
- 推奨図解パターン: warning-card
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S41 / 権限・保存設計と演習レビュー.
Slide topic for context: フォーム共同編集と回答先Sheetsの注意点.
Concept to visualize: フォームの共同編集者は回答先Sheetsにもアクセスできるため、権限管理を明示する.
Suggested visual pattern: warning-card.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: フォームの共同編集者は回答先Sheetsにもアクセスできるため、権限管理を明示する
**内容ブロック①：フォーム共同編集の仕組み**
- フォームに共同編集者を追加すると、その人もフォームの設問を編集できる
- フォームの共同編集者は、回答先スプレッドシートにもアクセスできる場合がある
- 回答データを見せたくない人をフォームの共同編集者に追加してはいけない
**内容ブロック②：回答先Sheetsの権限管理**
- 回答先スプレッドシートは、フォームとは別に権限を設定する
- 原本シートは「管理者のみ閲覧」に設定し、担当者には管理台帳のみ編集権限を与える
- シートタブの保護機能で原本シートへのアクセスを制限する
**内容ブロック③：確認すべきチェックリスト**
- フォームの共同編集者に、回答データを見せたくない人が含まれていないか
- 回答先Sheetsのリンク共有設定が「組織内のみ」「特定の人のみ」になっているか
- 出典: Google Docs Editors Help（Googleフォームの共同編集者と回答先シートの扱い）
Visible text candidates to use when useful, without reducing the source density: フォーム共同編集の仕組み / 回答先Sheetsの権限管理 / 確認すべきチェックリスト / フォームに共同編集者を追加すると、その人もフォームの設問を編集できる / 回答データを見せたくない人をフォームの共同編集者に追加してはいけない / 回答先スプレッドシートは、フォームとは別に権限を設定する / シートタブの保護機能で原本シートへのアクセスを制限する / フォームの共同編集者に、回答データを見せたくない人が含まれていないか / 出典 / 管理者のみ閲覧 / 組織内のみ / 特定の人のみ.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S42 Drive保存場所と命名規則: フォルダ構造と名前のルールで迷子ファイルをなくす

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 命名規則を決めてから保存すると、GASが自動でファイルを探せるようになる
- 推奨図解パターン: structure-layer
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S42 / 権限・保存設計と演習レビュー.
Slide topic for context: Drive保存場所と命名規則: フォルダ構造と名前のルールで迷子ファイルをなくす.
Concept to visualize: 命名規則を決めてから保存すると、GASが自動でファイルを探せるようになる.
Suggested visual pattern: structure-layer.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 命名規則を決めてから保存すると、GASが自動でファイルを探せるようになる
**内容ブロック①：推奨フォルダ構造（問い合わせ受付の例）**
コード例:
業務フォルダ/
  ├── フォーム/          ← Googleフォームを保存
  ├── 台帳/             ← 回答先Sheets・管理台帳を保存
  ├── 添付/             ← ファイルアップロードの保存先
  │   └── 2026年/
  │       └── 05月/
  └── アーカイブ/        ← 完了案件の添付ファイルを移動
コード例:
**内容ブロック②：命名規則の例**
| ファイルの種類 | 命名規則の例 | 例 |
|---|---|---|
| フォーム | [業務名]_フォーム_[年月] | 問い合わせ受付_フォーム_202606 |
| 回答先シート | [業務名]_回答_[年月] | 問い合わせ受付_回答_202606 |
| 管理台帳 | [業務名]_台帳 | 問い合わせ受付_台帳 |
| 添付ファイル | [受付番号]_[ファイル名] | INQ-20260604-001_見積依頼.pdf |
**内容ブロック③：GASとの連動**
- GASはフォルダIDまたはフォルダ名でDriveのフォルダを参照する
- フォルダ名を変えるとGASが添付ファイルを見つけられなくなる
- 今日のメモに「フォルダ名」と「GAS追加予定」を記録しておく
Visible text candidates to use when useful, without reducing the source density: 推奨フォルダ構造 / 命名規則の例 / GASとの連動 / ファイルの種類 / フォーム / [業務名]_フォーム_[年月] / 回答先シート / [業務名]_回答_[年月] / 管理台帳 / [業務名]_台帳 / 添付ファイル / [受付番号]_[ファイル名] / GASはフォルダIDまたはフォルダ名でDriveのフォルダを参照する / フォルダ名を変えるとGASが添付ファイルを見つけられなくなる / 今日のメモに「フォルダ名」と「GAS追加予定」を記録しておく / フォルダ名 / GAS追加予定.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S43 画面共有: Drive権限と保存場所を確認する

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 権限設定と保存場所の確認は5分で終わるが、後から直すと1時間かかる
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/drive-permission-check.png`（取得先: https://support.google.com/drive/answer/2494822?hl=ja）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S43 / 権限・保存設計と演習レビュー.
Slide topic for context: 画面共有: Drive権限と保存場所を確認する.
Concept to visualize: 権限設定と保存場所の確認は5分で終わるが、後から直すと1時間かかる.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 権限設定と保存場所の確認は5分で終わるが、後から直すと1時間かかる
**内容ブロック①：画面共有の内容**
- ダミーのDriveフォルダを開き、フォルダ構造を確認する
- フォームのファイル権限設定を確認する（共有→リンク共有の範囲）
- 管理台帳のファイル権限設定を確認する（共有→編集者の範囲）
**内容ブロック②：確認ポイント**
- 「全員（リンクを知っている人全員）」になっていないか
- 「組織内のみ」または「特定のユーザー」に限定されているか
- オーナーが管理担当者になっているか
**内容ブロック③：権限設計メモへの記入**
- 次のワーク（権限・保存設計メモ）でこの画面を参考にしてください
- 設定後に「誰が閲覧・編集できるか」をメモとして残しておく
Visible text candidates to use when useful, without reducing the source density: 画面共有の内容 / 確認ポイント / 権限設計メモへの記入 / ダミーのDriveフォルダを開き、フォルダ構造を確認する / フォームのファイル権限設定を確認する / 管理台帳のファイル権限設定を確認する / 「全員」になっていないか / 「組織内のみ」または「特定のユーザー」に限定されているか / オーナーが管理担当者になっているか / 次のワークでこの画面を参考にしてください / 設定後に「誰が閲覧・編集できるか」をメモとして残しておく / 組織内のみ / 特定のユーザー / 誰が閲覧・編集できるか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S44 演習レビュー: GASで扱いやすい台帳になっているか確認する

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 6点のチェックを通過した台帳は、第3回のGAS実装をそのまま始められる
- 推奨図解パターン: checklist-confirmation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S44 / 権限・保存設計と演習レビュー.
Slide topic for context: 演習レビュー: GASで扱いやすい台帳になっているか確認する.
Concept to visualize: 6点のチェックを通過した台帳は、第3回のGAS実装をそのまま始められる.
Suggested visual pattern: checklist-confirmation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 6点のチェックを通過した台帳は、第3回のGAS実装をそのまま始められる
**内容ブロック①：GAS準備チェックリスト**
| チェック項目 | 確認内容 |
|---|---|
| 原本分離 | 回答シートと管理台帳が別シートになっているか |
| 列名固定 | 1行目に列名が入り、以降に変更していないか |
| 選択肢統一 | ステータス・担当者・カテゴリがプルダウンになっているか |
| 受付番号列 | 受付番号列が「GAS追加予定」として設計されているか |
| 期限列 | 対応期限が日付型（YYYY/MM/DD）で統一されているか |
| 権限設計 | フォーム・原本シート・管理台帳の共有範囲が設定されているか |
**内容ブロック②：チェックを通過しなかった場合の対処**
- 原本シートと台帳が同じシート → 今日中に別シートに分ける
- ステータスがテキスト入力 → 入力規則でプルダウンに変更する
- 期限がテキスト（「月末」など）→ 日付型に入力し直す
**内容ブロック③：第3回（GAS実装）への引き継ぎ**
- このチェックリストが通過したら、第3回の実装準備が完了
- 台帳列定義表と権限・保存設計メモを第3回の設計書として使う
Visible text candidates to use when useful, without reducing the source density: GAS準備チェックリスト / チェックを通過しなかった場合の対処 / 第3回への引き継ぎ / チェック項目 / 確認内容 / 原本分離 / 回答シートと管理台帳が別シートになっているか / 列名固定 / 1行目に列名が入り、以降に変更していないか / 選択肢統一 / ステータス・担当者・カテゴリがプルダウンになっているか / 受付番号列 / 受付番号列が「GAS追加予定」として設計されているか / 期限列 / 対応期限が日付型で統一されているか / 権限設計 / フォーム・原本シート・管理台帳の共有範囲が設定されているか / 原本シートと台帳が同じシート → 今日中に別シートに分ける / ステータスがテキスト入力 → 入力規則でプルダウンに変更する / 期限がテキスト→ 日付型に入力し直す.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S45 まとめと次回準備

- セクション: 権限・保存設計と演習レビュー
- スライド側ヘッドライン（画像内には原則入れない）: 今日設計したフォーム・台帳・権限が、第3回のGAS自動化の設計書になる
- 推奨図解パターン: summary-closing
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第2回 業務データ基盤の設計 / S45 / 権限・保存設計と演習レビュー.
Slide topic for context: まとめと次回準備.
Concept to visualize: 今日設計したフォーム・台帳・権限が、第3回のGAS自動化の設計書になる.
Suggested visual pattern: summary-closing.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 今日設計したフォーム・台帳・権限が、第3回のGAS自動化の設計書になる
**内容ブロック①：第2回の学習内容まとめ**
- Googleフォームの設問設計（1設問1情報・必須・選択肢・入力形式）
- 回答先スプレッドシートの2層設計（原本シート + 管理台帳）
- 管理台帳の基本列（受付番号・ステータス・担当者・期限・ログ列）
- 入力規則・条件付き書式・集計式で台帳の品質を保つ方法
- Drive権限設計と命名規則・フォルダ構造
**内容ブロック②：成果物の確認**
| 成果物 | 完成状況 | 第3回での使い道 |
|---|---|---|
| フォーム設計表 | 第2回ワークで完成 | Googleフォームの実装に使う |
| 管理台帳列定義 | 第2回ワークで完成 | GASがデータを書き込む列の定義になる |
| 権限・保存設計メモ | 第2回ワークで完成 | Driveフォルダ設定・GASの保存先設定に使う |
**内容ブロック③：第3回までの準備**
| 準備物 | 内容 |
|---|---|
| フォームの実装 | 設計表をもとにGoogleフォームを実際に作成する |
| 台帳列の確定 | 管理台帳の列名・プルダウンを設定する |
| サンプルデータ3〜5行 | 台帳にダミーデータを入力しておく |
- 第3回は「この台帳にGASで自動通知・自動集計を追加する」実装を行います
Visible text candidates to use when useful, without reducing the source density: 第2回の学習内容まとめ / 成果物の確認 / 第3回までの準備 / Googleフォームの設問設計 / 回答先スプレッドシートの2層設計 / 管理台帳の基本列 / 入力規則・条件付き書式・集計式で台帳の品質を保つ方法 / Drive権限設計と命名規則・フォルダ構造 / 完成状況 / フォーム設計表 / 第2回ワークで完成 / 管理台帳列定義 / 権限・保存設計メモ / 準備物 / フォームの実装 / 設計表をもとにGoogleフォームを実際に作成する / 台帳列の確定 / 管理台帳の列名・プルダウンを設定する / サンプルデータ3〜5行 / 台帳にダミーデータを入力しておく.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```
