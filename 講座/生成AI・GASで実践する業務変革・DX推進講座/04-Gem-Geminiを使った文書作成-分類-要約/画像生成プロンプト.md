# 04-Gem-Geminiを使った文書作成-分類-要約 画像生成プロンプト（1枚まるごとラスター・ワイヤーフレーム参照版）

対象講座: 生成AI・GASで実践する業務変革・DX推進講座
対象セッション: 第4回 スライド構成案: Gem/Geminiを使った文書作成・分類・要約
対象スライド: S01-S30
採用テンプレート: `isometric-corporate-clean` + 固定レイアウトワイヤーフレーム参照
参照レイアウト画像（毎回必ず添付する）: `/Users/deguchishouma/Desktop/AI法人研修/skills/corporate-training-course-builder/references/assets/講座スライドレイアウトワイヤフレーム.png`

このファイルは、編集可能Google Slidesテンプレート方式ではなく、1スライド=1枚の完成ラスター画像を毎回ワイヤーフレーム参照画像付きで生成する標準に基づく。講師コメントがなくても、スライドだけで内容を想像できる密度を維持しつつ、レイアウトの再現性をワイヤーフレーム参照で担保する。

## 共通ルール

- Codex App Server / GPT image 2 / `imagegen` スキルで1枚まるごとの完成ラスター画像として生成する。
- 生成のたびに参照レイアウト画像 `講座スライドレイアウトワイヤフレーム.png` を添付し、帯構成・比率・罫線をそのまま踏襲する。テキストで説明するだけでなく、画像そのものを参照入力として渡す。
- SVG、HTML/CSS、canvas、ブラウザスクリーンショット、PIL/Pillow、ImageMagick、PDF/PPTX書き出し、ローカル変換、後載せテキスト合成は使わない。
- 1枚ごとに、講座タイトル帯、スライド番号/全体の枚数帯、スライド見出し帯、図解エリア（3〜6個の本文ブロック・具体例・成果物・確認観点）、今回のまとめ帯、目次見出しストリップを必ず入れる。
- 画像内テキストは、スライド案にある語句を優先し、勝手な要約や別概念を足さない。長すぎる本文は、意味を保ってカード・表・フローに分割する。
- 目次見出しストリップは、現在の章のチップだけを塗りつぶしてハイライトし、他のチップは白背景・薄い枠線にする。
- 実在ロゴや実在UIは参照素材がある場合だけ使う。参照素材なしにGoogleロゴやGoogle画面を描かせない。
- 会社名、顧客名、社員名、メールアドレス、価格、連絡先、契約情報、APIキー、社内固有情報は入れない。
- `素材配置枠`、`公式ロゴ`、空の破線枠、透かし、QRコード、架空UI、架空ロゴを入れない。

## 共通ベースプロンプト

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
```

## ワイヤーフレーム参照指示（共通・毎回付与）

```text
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
```

## 共通ネガティブプロンプト

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## 目次見出しストリップ（章構成）

- 1 役割分担（S01-S07）
- 2 Gemini-Gem（S08-S12）
- 3 Gem設計（S13-S18）
- 4 事例（S19-S24）
- 5 レビュー（S25-S27）
- 6 演習（S28-S30）

## セッション全体の時間配分

画像生成時の文脈として参照する。時間配分そのものを全スライドに入れる必要はない。

```markdown
## 120分の時間配分

| ブロック | 時間 | スライド範囲 | ねらい |
|---|---:|---|---|
| 導入と役割分担 | 15分 | S01-S07 | GAS・Sheets・Gem/Gemini・人の確認を切り分ける |
| Gemini/Gemの使いどころ | 20分 | S08-S12 | Workspace連携と代替手順・Gemを作る意味を理解する |
| Gem設計の型 | 25分 | S13-S18 | 役割・入力・出力・禁止事項・判断基準を設計する |
| 業務別ユースケース | 25分 | S19-S24 | 問い合わせ分類・日報要約・Meet文字起こし・コード読解を扱う |
| AI出力レビューと運用 | 20分 | S25-S27 | 事実確認・台帳反映・共有・履歴・リスクを確認する |
| 演習とまとめ | 15分 | S28-S30 | 自社業務用Gem設計書を作り、第5回へつなぐ |
```

## Slide S01. Gem/Geminiで文書作成・分類・要約を業務に組み込む ─ 第4回

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S01 / 30
- スライド見出し帯: Gem/Geminiで文書作成・分類・要約を業務に組み込む ─ 第4回
- 今回のまとめ帯（要点・結論・次のアクション）: Gem/Geminiを業務フローに組み込むと、毎回プロンプトを考えなくても安定した下書きと分類が得られる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
今日の到達点
問い合わせ分類・日報要約・Meet文字起こし要約に使えるGem設計書を1つ完成させる
AI出力を事実・推測・要確認・利用不可に分けてレビューできる
Gemini連携が使えない環境でも代替手順を選べる
今日の成果物
| 成果物 | 内容 |
|---|---|
| Gem設計書 | 役割・入力・出力形式・禁止事項・判断基準 |
| AI活用プロンプト | 用途・入力・出力形式・確認観点 |
| 出力レビュー表 | 事実・推測・要確認・利用不可の4分類 |
| Meet要約メモ | 決定事項・宿題・期限・担当者の抽出テンプレート |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S01 / 30
スライド見出し帯（そのまま表示）: Gem/Geminiで文書作成・分類・要約を業務に組み込む ─ 第4回
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Gem/Geminiを業務フローに組み込むと、毎回プロンプトを考えなくても安定した下書きと分類が得られる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
今日の到達点
問い合わせ分類・日報要約・Meet文字起こし要約に使えるGem設計書を1つ完成させる
AI出力を事実・推測・要確認・利用不可に分けてレビューできる
Gemini連携が使えない環境でも代替手順を選べる
今日の成果物
| 成果物 | 内容 |
|---|---|
| Gem設計書 | 役割・入力・出力形式・禁止事項・判断基準 |
| AI活用プロンプト | 用途・入力・出力形式・確認観点 |
| 出力レビュー表 | 事実・推測・要確認・利用不可の4分類 |
| Meet要約メモ | 決定事項・宿題・期限・担当者の抽出テンプレート |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S02. 今日の2時間で作るものと演習データ

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S02 / 30
- スライド見出し帯: 今日の2時間で作るものと演習データ
- 今回のまとめ帯（要点・結論・次のアクション）: この2時間で、明日から自分の業務で試せるAI活用設計書を完成させる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:演習データ
| ファイル名 | 内容 | 使う場面 |
|---|---|---|
| `問い合わせAI分類入力.csv` | 架空の問い合わせ10件 | 分類Gemの演習 |
| `日報要約入力.csv` | 架空の日報5件 | 日報要約Gem |
| `Meet文字起こしサンプル.md` | 架空会議の文字起こし | 決定事項・宿題抽出 |
| `AI出力レビュー練習.csv` | 事実・推測・要確認混在 | レビュー観点の演習 |
内容ブロック2:使う機能の前提
GeminiのWorkspace連携・Gem共有・Sheets AI関数・Meet文字起こしは、プラン・管理者設定・ロールアウトに依存する
必須演習は貼り付け運用でも成立する形にする
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S02 / 30
スライド見出し帯（そのまま表示）: 今日の2時間で作るものと演習データ
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: この2時間で、明日から自分の業務で試せるAI活用設計書を完成させる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:演習データ
| ファイル名 | 内容 | 使う場面 |
|---|---|---|
| `問い合わせAI分類入力.csv` | 架空の問い合わせ10件 | 分類Gemの演習 |
| `日報要約入力.csv` | 架空の日報5件 | 日報要約Gem |
| `Meet文字起こしサンプル.md` | 架空会議の文字起こし | 決定事項・宿題抽出 |
| `AI出力レビュー練習.csv` | 事実・推測・要確認混在 | レビュー観点の演習 |
内容ブロック2:使う機能の前提
GeminiのWorkspace連携・Gem共有・Sheets AI関数・Meet文字起こしは、プラン・管理者設定・ロールアウトに依存する
必須演習は貼り付け運用でも成立する形にする
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S03. 第4回の目次/全体像

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S03 / 30
- スライド見出し帯: 第4回の目次/全体像
- 今回のまとめ帯（要点・結論・次のアクション）: AIの役割を決め、設計書を作り、出力をレビューして台帳へ戻す流れを扱う
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:目次と成果物
| No. | 目次項目 | 時間 | スライド範囲 | 支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入と役割分担 | 15分 | S01-S07 | AIを入れる位置の判断メモ |
| 2 | Gemini/Gemの使いどころ | 20分 | S08-S12 | 利用可否と代替手順メモ |
| 3 | Gem設計の型 | 25分 | S13-S18 | Gem設計書 |
| 4 | 業務別ユースケース | 25分 | S19-S24 | AI活用プロンプトと出力例 |
| 5 | AI出力レビューと運用 | 20分 | S25-S27 | 出力レビュー表 |
| 6 | 演習とまとめ | 15分 | S28-S30 | Gem設計書・プロンプト・レビュー表 |
内容ブロック2:デモ/演習の位置
| スライド | ここで行うこと |
|---|---|
| S12 | 画面共有: Gemini/Gemの入口を確認 |
| S17 | 画面共有: Gem設計書を1つ作る |
| S24 | 画面共有: 問い合わせ分類を台帳に戻す |
| S29 | 演習: Gem設計書・プロンプト・レビュー表を作る |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S03 / 30
スライド見出し帯（そのまま表示）: 第4回の目次/全体像
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AIの役割を決め、設計書を作り、出力をレビューして台帳へ戻す流れを扱う
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:目次と成果物
| No. | 目次項目 | 時間 | スライド範囲 | 支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入と役割分担 | 15分 | S01-S07 | AIを入れる位置の判断メモ |
| 2 | Gemini/Gemの使いどころ | 20分 | S08-S12 | 利用可否と代替手順メモ |
| 3 | Gem設計の型 | 25分 | S13-S18 | Gem設計書 |
| 4 | 業務別ユースケース | 25分 | S19-S24 | AI活用プロンプトと出力例 |
| 5 | AI出力レビューと運用 | 20分 | S25-S27 | 出力レビュー表 |
| 6 | 演習とまとめ | 15分 | S28-S30 | Gem設計書・プロンプト・レビュー表 |
内容ブロック2:デモ/演習の位置
| スライド | ここで行うこと |
|---|---|
| S12 | 画面共有: Gemini/Gemの入口を確認 |
| S17 | 画面共有: Gem設計書を1つ作る |
| S24 | 画面共有: 問い合わせ分類を台帳に戻す |
| S29 | 演習: Gem設計書・プロンプト・レビュー表を作る |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S04. 章見出し/現在位置 1/6: 導入と役割分担

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 1/6 導入と役割分担
- 前の章: なし
- 次の章: Gemini/Gemの使いどころ
- 章の一言(画像内は小さく): ここから「1. 導入と役割分担」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「1」「導入と役割分担」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「1 / 6」。
前の章がないため、前の章の表示は省略する。
現在の章の右下または下に、次の章「Gemini/Gemの使いどころ」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S04」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「1. 導入と役割分担」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は15分、対象スライド範囲はS01-S07。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S05. AIはどこに入れるか

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S05 / 30
- スライド見出し帯: AIはどこに入れるか
- 今回のまとめ帯（要点・結論・次のアクション）: GASが処理できない曖昧な文章をGemini/Gemに渡し、確定判断は人に戻す
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
役割カード
| 担当 | 得意 | 苦手 |
|---|---|---|
| GAS | 抽出・集計・転記 | 曖昧な文章判断 |
| Sheets | データ保管・閲覧 | 文章の意味判断 |
| Gemini/Gem | 分類・要約・下書き | 事実確定・承認 |
| 人 | 最終確認・例外判断 | 大量の繰り返し処理 |
業種別例
| 業種 | GAS | Gemini/Gem | 人 |
|---|---|---|---|
| サービス業 | 未対応抽出 | 返信案 | 送信判断 |
| 士業 | 期限超過抽出 | 依頼内容要約 | 法的判断 |
| 製造業 | 未出荷集計 | 納期回答案 | 在庫判断 |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S05 / 30
スライド見出し帯（そのまま表示）: AIはどこに入れるか
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: GASが処理できない曖昧な文章をGemini/Gemに渡し、確定判断は人に戻す
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
役割カード
| 担当 | 得意 | 苦手 |
|---|---|---|
| GAS | 抽出・集計・転記 | 曖昧な文章判断 |
| Sheets | データ保管・閲覧 | 文章の意味判断 |
| Gemini/Gem | 分類・要約・下書き | 事実確定・承認 |
| 人 | 最終確認・例外判断 | 大量の繰り返し処理 |
業種別例
| 業種 | GAS | Gemini/Gem | 人 |
|---|---|---|---|
| サービス業 | 未対応抽出 | 返信案 | 送信判断 |
| 士業 | 期限超過抽出 | 依頼内容要約 | 法的判断 |
| 製造業 | 未出荷集計 | 納期回答案 | 在庫判断 |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S06. 自動化しすぎない判断軸

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S06 / 30
- スライド見出し帯: 自動化しすぎない判断軸
- 今回のまとめ帯（要点・結論・次のアクション）: AIに任せてよいことと、AIに任せてはいけないことを分けると、導入後の事故を防げる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:判断マトリクス
| 処理 | GAS | Gemini/Gem | 人の確認 |
|---|---|---|---|
| 条件抽出 | ○ | - | ログ確認 |
| 文書分類 | - | 候補 | 最終確定 |
| 返信案 | - | 下書き | 送信前確認 |
| 承認・決裁 | × | × | 必須 |
| 外部送信 | × | × | 必須 |
内容ブロック2:段階設計
レベル1: 下書きだけAI、人が全文見直し
レベル2: 分類候補をAI、人が確認列を埋める
レベル3: 要約をAI、人が重要事項を確認
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S06 / 30
スライド見出し帯（そのまま表示）: 自動化しすぎない判断軸
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AIに任せてよいことと、AIに任せてはいけないことを分けると、導入後の事故を防げる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:判断マトリクス
| 処理 | GAS | Gemini/Gem | 人の確認 |
|---|---|---|---|
| 条件抽出 | ○ | - | ログ確認 |
| 文書分類 | - | 候補 | 最終確定 |
| 返信案 | - | 下書き | 送信前確認 |
| 承認・決裁 | × | × | 必須 |
| 外部送信 | × | × | 必須 |
内容ブロック2:段階設計
レベル1: 下書きだけAI、人が全文見直し
レベル2: 分類候補をAI、人が確認列を埋める
レベル3: 要約をAI、人が重要事項を確認
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S07. 情報管理の前提

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S07 / 30
- スライド見出し帯: 情報管理の前提
- 今回のまとめ帯（要点・結論・次のアクション）: AIへ入力する前に個人情報・機密情報・実名をダミー化し、出力も人が確認する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 役割分担
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:入れてよい/いけない
| 入れてよい | 入れてはいけない |
|---|---|
| 匿名化済みテキスト | 実在顧客名・社員名 |
| 架空の担当者名 | メールアドレス・電話番号 |
| 業務手順 | 未公開資料・契約内容 |
| 公式URL・公開情報 | 個人が特定できる組み合わせ |
内容ブロック2:演習ルール
すべて架空データを使う
実業務へ使う場合は社内ルールと契約条件を確認する
出力は「下書き」であり、確定情報ではない
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S07 / 30
スライド見出し帯（そのまま表示）: 情報管理の前提
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AIへ入力する前に個人情報・機密情報・実名をダミー化し、出力も人が確認する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 役割分担
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:入れてよい/いけない
| 入れてよい | 入れてはいけない |
|---|---|
| 匿名化済みテキスト | 実在顧客名・社員名 |
| 架空の担当者名 | メールアドレス・電話番号 |
| 業務手順 | 未公開資料・契約内容 |
| 公式URL・公開情報 | 個人が特定できる組み合わせ |
内容ブロック2:演習ルール
すべて架空データを使う
実業務へ使う場合は社内ルールと契約条件を確認する
出力は「下書き」であり、確定情報ではない
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S08. 章見出し/現在位置 2/6: Gemini/Gemの使いどころ

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 2/6 Gemini/Gemの使いどころ
- 前の章: 導入と役割分担
- 次の章: Gem設計の型
- 章の一言(画像内は小さく): ここから「2. Gemini/Gemの使いどころ」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「2」「Gemini/Gemの使いどころ」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「2 / 6」。
画面の隅に、前の章「導入と役割分担」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「Gem設計の型」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S08」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「2. Gemini/Gemの使いどころ」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は20分、対象スライド範囲はS08-S12。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S09. GeminiとGemの使い分け

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S09 / 30
- スライド見出し帯: GeminiとGemの使い分け
- 今回のまとめ帯（要点・結論・次のアクション）: 毎回違う質問はGemini、同じ業務を繰り返すならGemとして指示を固定する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 Gemini-Gem
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:使い分け表
| 使い方 | 向く場面 | 成果物 |
|---|---|---|
| Gemini単発利用 | その場の要約・下書き | 一回限りの出力 |
| Gem | 同じ分類・要約を毎週行う | 業務用指示書 |
| Docs/Drive連携 | 資料要約・文書確認 | 要約メモ |
| Meet文字起こし | 会議要点整理 | 決定事項・宿題一覧 |
内容ブロック2:導入時の注意
機能提供状況、利用条件、料金、管理者設定は変わるため、導入時に最新の公式情報を確認する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S09 / 30
スライド見出し帯（そのまま表示）: GeminiとGemの使い分け
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 毎回違う質問はGemini、同じ業務を繰り返すならGemとして指示を固定する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 Gemini-Gem
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:使い分け表
| 使い方 | 向く場面 | 成果物 |
|---|---|---|
| Gemini単発利用 | その場の要約・下書き | 一回限りの出力 |
| Gem | 同じ分類・要約を毎週行う | 業務用指示書 |
| Docs/Drive連携 | 資料要約・文書確認 | 要約メモ |
| Meet文字起こし | 会議要点整理 | 決定事項・宿題一覧 |
内容ブロック2:導入時の注意
機能提供状況、利用条件、料金、管理者設定は変わるため、導入時に最新の公式情報を確認する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S10. Workspace連携と代替手順

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S10 / 30
- スライド見出し帯: Workspace連携と代替手順
- 今回のまとめ帯（要点・結論・次のアクション）: 連携機能が使えない環境でも、貼り付け運用で同じ業務目的を達成できる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 Gemini-Gem
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:連携と代替
| 目的 | 連携できる場合 | 代替手順 |
|---|---|---|
| Docs要約 | Geminiで文書要約 | テキストをコピーして入力 |
| Meet要約 | 文字起こしから要約 | 配布用サンプルを貼り付け |
| Sheets分類 | AI関数・連携 | CSVを貼り付けて分類候補を作る |
| Drive資料確認 | ファイル参照 | 必要箇所のみ抜粋して入力 |
内容ブロック2:必須にしない理由
企業のプラン・管理者設定・ロールアウトで差がある
研修では業務設計と確認観点を主目的にする
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S10 / 30
スライド見出し帯（そのまま表示）: Workspace連携と代替手順
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 連携機能が使えない環境でも、貼り付け運用で同じ業務目的を達成できる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 Gemini-Gem
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:連携と代替
| 目的 | 連携できる場合 | 代替手順 |
|---|---|---|
| Docs要約 | Geminiで文書要約 | テキストをコピーして入力 |
| Meet要約 | 文字起こしから要約 | 配布用サンプルを貼り付け |
| Sheets分類 | AI関数・連携 | CSVを貼り付けて分類候補を作る |
| Drive資料確認 | ファイル参照 | 必要箇所のみ抜粋して入力 |
内容ブロック2:必須にしない理由
企業のプラン・管理者設定・ロールアウトで差がある
研修では業務設計と確認観点を主目的にする
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S11. Gem共有で確認すること

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S11 / 30
- スライド見出し帯: Gem共有で確認すること
- 今回のまとめ帯（要点・結論・次のアクション）: 業務用Gemは便利だが、共有範囲・入力禁止情報・更新責任を決めないと危険になる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 Gemini-Gem
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:共有前チェック
| 観点 | 確認すること |
|---|---|
| 共有範囲 | 誰が使えるか |
| 入力禁止 | 個人情報・機密情報の扱い |
| 出力確認 | 誰が最終確認するか |
| 更新責任 | 判断基準を誰が直すか |
| 履歴 | 変更内容をどこに記録するか |
内容ブロック2:運用メモ
Gemは業務の指示書に近い
使う人が増えるほど、説明と更新ルールが必要になる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S11 / 30
スライド見出し帯（そのまま表示）: Gem共有で確認すること
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 業務用Gemは便利だが、共有範囲・入力禁止情報・更新責任を決めないと危険になる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 Gemini-Gem
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:共有前チェック
| 観点 | 確認すること |
|---|---|
| 共有範囲 | 誰が使えるか |
| 入力禁止 | 個人情報・機密情報の扱い |
| 出力確認 | 誰が最終確認するか |
| 更新責任 | 判断基準を誰が直すか |
| 履歴 | 変更内容をどこに記録するか |
内容ブロック2:運用メモ
Gemは業務の指示書に近い
使う人が増えるほど、説明と更新ルールが必要になる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S12. 画面共有: Gemini/Gemの入口を確認する

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S12 / 30
- スライド見出し帯: 画面共有: Gemini/Gemの入口を確認する
- 今回のまとめ帯（要点・結論・次のアクション）: 機能の入口を確認し、使える場合と使えない場合の進め方を分ける
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 Gemini-Gem
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実演手順
1. Gemini/Gemの入口を確認する
2. 利用できる機能・できない機能を分ける
3. 貼り付け運用でも演習が進められることを確認する
内容ブロック2:見るポイント
管理者設定やプランで表示が違う
表示が違っても、設計書とプロンプトは作れる
実在データは入力しない
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S12 / 30
スライド見出し帯（そのまま表示）: 画面共有: Gemini/Gemの入口を確認する
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 機能の入口を確認し、使える場合と使えない場合の進め方を分ける
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 Gemini-Gem
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実演手順
1. Gemini/Gemの入口を確認する
2. 利用できる機能・できない機能を分ける
3. 貼り付け運用でも演習が進められることを確認する
内容ブロック2:見るポイント
管理者設定やプランで表示が違う
表示が違っても、設計書とプロンプトは作れる
実在データは入力しない
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S13. 章見出し/現在位置 3/6: Gem設計の型

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 3/6 Gem設計の型
- 前の章: Gemini/Gemの使いどころ
- 次の章: 業務別ユースケース
- 章の一言(画像内は小さく): ここから「3. Gem設計の型」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「3」「Gem設計の型」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「3 / 6」。
画面の隅に、前の章「Gemini/Gemの使いどころ」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「業務別ユースケース」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S13」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「3. Gem設計の型」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は25分、対象スライド範囲はS13-S18。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S14. Gem設計の7項目

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S14 / 30
- スライド見出し帯: Gem設計の7項目
- 今回のまとめ帯（要点・結論・次のアクション）: Gemはプロンプトではなく、役割・入力・出力・禁止事項・判断基準を持つ業務用指示書として設計する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Gem設計
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:7項目
| 項目 | 書くこと |
|---|---|
| 目的 | 何の業務を支援するか |
| 役割 | AIに期待する立場 |
| 入力 | 受け取るデータ |
| 出力形式 | 表・箇条書き・分類列 |
| 禁止事項 | 推測、外部送信、個人情報利用 |
| 判断基準 | 分類・要約の基準 |
| 確認観点 | 人が見るポイント |
内容ブロック2:設計例
問い合わせ分類Gem: カテゴリ候補・優先度・要確認理由を出す
日報要約Gem: 実績・課題・次アクションを抽出する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S14 / 30
スライド見出し帯（そのまま表示）: Gem設計の7項目
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Gemはプロンプトではなく、役割・入力・出力・禁止事項・判断基準を持つ業務用指示書として設計する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Gem設計
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:7項目
| 項目 | 書くこと |
|---|---|
| 目的 | 何の業務を支援するか |
| 役割 | AIに期待する立場 |
| 入力 | 受け取るデータ |
| 出力形式 | 表・箇条書き・分類列 |
| 禁止事項 | 推測、外部送信、個人情報利用 |
| 判断基準 | 分類・要約の基準 |
| 確認観点 | 人が見るポイント |
内容ブロック2:設計例
問い合わせ分類Gem: カテゴリ候補・優先度・要確認理由を出す
日報要約Gem: 実績・課題・次アクションを抽出する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S15. 良い指示は役割・文脈・出力形式から始める

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S15 / 30
- スライド見出し帯: 良い指示は役割・文脈・出力形式から始める
- 今回のまとめ帯（要点・結論・次のアクション）: AIに何者として、何を見て、どの形式で返すかを先に伝えると、出力のぶれが減る
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Gem設計
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:プロンプト骨格
1. あなたの役割: 業務改善担当の補助者
2. 文脈: 問い合わせ台帳を分類する
3. 入力: 問い合わせ本文・受付日・担当部署
4. 出力: カテゴリ、優先度、要確認理由の表
5. 禁止: 実在情報の推測、送信判断、断定
内容ブロック2:悪い例/良い例
| 悪い例 | 良い例 |
|---|---|
| これ分類して | 問い合わせ本文を4カテゴリに分類し、要確認理由を1文で出す |
| いい感じに要約 | 決定事項・宿題・期限・担当者に分けて要約 |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S15 / 30
スライド見出し帯（そのまま表示）: 良い指示は役割・文脈・出力形式から始める
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AIに何者として、何を見て、どの形式で返すかを先に伝えると、出力のぶれが減る
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Gem設計
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:プロンプト骨格
1. あなたの役割: 業務改善担当の補助者
2. 文脈: 問い合わせ台帳を分類する
3. 入力: 問い合わせ本文・受付日・担当部署
4. 出力: カテゴリ、優先度、要確認理由の表
5. 禁止: 実在情報の推測、送信判断、断定
内容ブロック2:悪い例/良い例
| 悪い例 | 良い例 |
|---|---|
| これ分類して | 問い合わせ本文を4カテゴリに分類し、要確認理由を1文で出す |
| いい感じに要約 | 決定事項・宿題・期限・担当者に分けて要約 |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S16. 入力データ・出力形式・禁止事項を先に決める

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S16 / 30
- スライド見出し帯: 入力データ・出力形式・禁止事項を先に決める
- 今回のまとめ帯（要点・結論・次のアクション）: AIの品質は、入力の整理と出力形式の指定で大きく変わる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Gem設計
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:入力の整え方
| 入力 | 整えること |
|---|---|
| 問い合わせ本文 | 個人名を担当者Aへ置換 |
| 日報 | 日付・部署・実績・課題を分ける |
| 文字起こし | 発言者名を役割名へ置換 |
| GASコード | 対象シート名と処理目的を添える |
内容ブロック2:禁止事項の例
実在顧客名を出さない
確認していない事実を断定しない
外部送信用の確定文にしない
不明点は「要確認」と書く
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S16 / 30
スライド見出し帯（そのまま表示）: 入力データ・出力形式・禁止事項を先に決める
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AIの品質は、入力の整理と出力形式の指定で大きく変わる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Gem設計
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:入力の整え方
| 入力 | 整えること |
|---|---|
| 問い合わせ本文 | 個人名を担当者Aへ置換 |
| 日報 | 日付・部署・実績・課題を分ける |
| 文字起こし | 発言者名を役割名へ置換 |
| GASコード | 対象シート名と処理目的を添える |
内容ブロック2:禁止事項の例
実在顧客名を出さない
確認していない事実を断定しない
外部送信用の確定文にしない
不明点は「要確認」と書く
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S17. 画面共有: Gem設計書を1つ作る

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S17 / 30
- スライド見出し帯: 画面共有: Gem設計書を1つ作る
- 今回のまとめ帯（要点・結論・次のアクション）: 問い合わせ分類を題材に、設計書の7項目を埋めて業務用Gemの形にする
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Gem設計
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実演手順
1. `問い合わせAI分類入力.csv` を確認
2. 分類カテゴリを4つに絞る
3. 出力形式を表で指定
4. 禁止事項と確認観点を追記
5. サンプル入力で出力を確認
内容ブロック2:見せるポイント
毎回の思いつきプロンプトをやめる
設計書にしてから使う
出力は候補であり、人が確定する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S17 / 30
スライド見出し帯（そのまま表示）: 画面共有: Gem設計書を1つ作る
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 問い合わせ分類を題材に、設計書の7項目を埋めて業務用Gemの形にする
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Gem設計
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実演手順
1. `問い合わせAI分類入力.csv` を確認
2. 分類カテゴリを4つに絞る
3. 出力形式を表で指定
4. 禁止事項と確認観点を追記
5. サンプル入力で出力を確認
内容ブロック2:見せるポイント
毎回の思いつきプロンプトをやめる
設計書にしてから使う
出力は候補であり、人が確定する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S18. 判断基準を明文化する

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S18 / 30
- スライド見出し帯: 判断基準を明文化する
- 今回のまとめ帯（要点・結論・次のアクション）: カテゴリや要約の基準を言葉にすると、AI出力を人がレビューしやすくなる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Gem設計
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:分類基準例
| カテゴリ | 判断基準 | 要確認になる条件 |
|---|---|---|
| 予約変更 | 日時変更・キャンセル | 希望日時が複数ある |
| 苦情 | 不満・不具合 | 感情的表現が強い |
| 見積依頼 | 金額・条件確認 | 契約条件に触れる |
| その他 | 上記以外 | 判断不能 |
内容ブロック2:レビューしやすい出力
カテゴリだけでなく理由も出す
自信が低いものは要確認にする
台帳へ戻す列名と同じ名前で出力する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S18 / 30
スライド見出し帯（そのまま表示）: 判断基準を明文化する
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: カテゴリや要約の基準を言葉にすると、AI出力を人がレビューしやすくなる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Gem設計
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:分類基準例
| カテゴリ | 判断基準 | 要確認になる条件 |
|---|---|---|
| 予約変更 | 日時変更・キャンセル | 希望日時が複数ある |
| 苦情 | 不満・不具合 | 感情的表現が強い |
| 見積依頼 | 金額・条件確認 | 契約条件に触れる |
| その他 | 上記以外 | 判断不能 |
内容ブロック2:レビューしやすい出力
カテゴリだけでなく理由も出す
自信が低いものは要確認にする
台帳へ戻す列名と同じ名前で出力する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S19. 章見出し/現在位置 4/6: 業務別ユースケース

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 4/6 業務別ユースケース
- 前の章: Gem設計の型
- 次の章: AI出力レビューと運用
- 章の一言(画像内は小さく): ここから「4. 業務別ユースケース」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「4」「業務別ユースケース」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「4 / 6」。
画面の隅に、前の章「Gem設計の型」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「AI出力レビューと運用」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S19」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「4. 業務別ユースケース」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は25分、対象スライド範囲はS19-S24。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S20. 問い合わせ分類Gem

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S20 / 30
- スライド見出し帯: 問い合わせ分類Gem
- 今回のまとめ帯（要点・結論・次のアクション）: 問い合わせ分類は、AIが候補を出し、人が確定して台帳へ戻す半自動運用から始める
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 事例
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:処理フロー
Forms/メール → Sheets台帳 → Gemini/Gemで分類候補 → 人が確認 → カテゴリ列へ反映 → GASで担当者通知
内容ブロック2:出力列
| 列名 | 内容 |
|---|---|
| AIカテゴリ候補 | 予約変更・苦情・見積依頼など |
| 優先度候補 | 高・中・低 |
| 要確認理由 | 判断に迷う理由 |
| 人の確定カテゴリ | 担当者が最終入力 |
内容ブロック3:リスク
苦情や契約判断はAIだけで確定しない
返信案は必ず人が確認する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S20 / 30
スライド見出し帯（そのまま表示）: 問い合わせ分類Gem
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 問い合わせ分類は、AIが候補を出し、人が確定して台帳へ戻す半自動運用から始める
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 事例
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:処理フロー
Forms/メール → Sheets台帳 → Gemini/Gemで分類候補 → 人が確認 → カテゴリ列へ反映 → GASで担当者通知
内容ブロック2:出力列
| 列名 | 内容 |
|---|---|
| AIカテゴリ候補 | 予約変更・苦情・見積依頼など |
| 優先度候補 | 高・中・低 |
| 要確認理由 | 判断に迷う理由 |
| 人の確定カテゴリ | 担当者が最終入力 |
内容ブロック3:リスク
苦情や契約判断はAIだけで確定しない
返信案は必ず人が確認する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S21. 日報要約・Meet文字起こし要約

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S21 / 30
- スライド見出し帯: 日報要約・Meet文字起こし要約
- 今回のまとめ帯（要点・結論・次のアクション）: 長い文章は、実績・課題・決定事項・宿題・期限に分けると業務に戻しやすい
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 事例
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:要約テンプレート
| 入力 | 抽出する項目 | 出力先 |
|---|---|---|
| 日報 | 実績・課題・明日の予定 | 日報要約表 |
| Meet文字起こし | 決定事項・宿題・期限・担当者 | アクション台帳 |
| 議事録 | 合意事項・未決事項 | 会議メモ |
内容ブロック2:確認観点
重要な決定が抜けていないか
担当者・期限が推測で補われていないか
文字起こしに含まれない内容を追加していないか
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S21 / 30
スライド見出し帯（そのまま表示）: 日報要約・Meet文字起こし要約
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 長い文章は、実績・課題・決定事項・宿題・期限に分けると業務に戻しやすい
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 事例
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:要約テンプレート
| 入力 | 抽出する項目 | 出力先 |
|---|---|---|
| 日報 | 実績・課題・明日の予定 | 日報要約表 |
| Meet文字起こし | 決定事項・宿題・期限・担当者 | アクション台帳 |
| 議事録 | 合意事項・未決事項 | 会議メモ |
内容ブロック2:確認観点
重要な決定が抜けていないか
担当者・期限が推測で補われていないか
文字起こしに含まれない内容を追加していないか
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S22. Drive/Docs資料要約と根拠確認

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S22 / 30
- スライド見出し帯: Drive/Docs資料要約と根拠確認
- 今回のまとめ帯（要点・結論・次のアクション）: 資料要約では、要点だけでなく根拠箇所と利用可否を確認する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 事例
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:要約時の出力形式
| 項目 | 内容 |
|---|---|
| 要点 | 3〜5個の短い箇条書き |
| 根拠箇所 | どの段落・見出しに基づくか |
| 要確認 | 不明点・確認が必要な点 |
| 利用不可 | 根拠が弱い、未確認、推測の内容 |
内容ブロック2:使ってよい場面
公開資料・社内で利用許可された資料
ダミー化済みの教材・演習用文章
実業務では権限と利用条件を確認する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S22 / 30
スライド見出し帯（そのまま表示）: Drive/Docs資料要約と根拠確認
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 資料要約では、要点だけでなく根拠箇所と利用可否を確認する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 事例
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:要約時の出力形式
| 項目 | 内容 |
|---|---|
| 要点 | 3〜5個の短い箇条書き |
| 根拠箇所 | どの段落・見出しに基づくか |
| 要確認 | 不明点・確認が必要な点 |
| 利用不可 | 根拠が弱い、未確認、推測の内容 |
内容ブロック2:使ってよい場面
公開資料・社内で利用許可された資料
ダミー化済みの教材・演習用文章
実業務では権限と利用条件を確認する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S23. GASコード読解Gem

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S23 / 30
- スライド見出し帯: GASコード読解Gem
- 今回のまとめ帯（要点・結論・次のアクション）: コードの意味をAIに説明させる場合も、実行判断と修正は人が確認する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 事例
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:読解依頼の型
このGASコードの目的を説明してください
読み取るシート名・更新する列・ログ出力の有無を表にしてください
危険な上書きや外部送信があれば指摘してください
内容ブロック2:出力させる表
| 観点 | AIに確認させること |
|---|---|
| 入力 | どのシート・範囲を読むか |
| 処理 | どの条件で分岐するか |
| 出力 | どこへ書き込むか |
| リスク | 上書き・送信・権限の注意 |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S23 / 30
スライド見出し帯（そのまま表示）: GASコード読解Gem
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: コードの意味をAIに説明させる場合も、実行判断と修正は人が確認する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 事例
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:読解依頼の型
このGASコードの目的を説明してください
読み取るシート名・更新する列・ログ出力の有無を表にしてください
危険な上書きや外部送信があれば指摘してください
内容ブロック2:出力させる表
| 観点 | AIに確認させること |
|---|---|
| 入力 | どのシート・範囲を読むか |
| 処理 | どの条件で分岐するか |
| 出力 | どこへ書き込むか |
| リスク | 上書き・送信・権限の注意 |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S24. 画面共有: 問い合わせ分類を台帳に戻す

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S24 / 30
- スライド見出し帯: 画面共有: 問い合わせ分類を台帳に戻す
- 今回のまとめ帯（要点・結論・次のアクション）: AI分類候補をそのまま確定せず、人の確認列を挟んで台帳へ戻す
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 事例
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実演手順
1. 問い合わせCSVを確認
2. Gem設計に沿って分類候補を出す
3. AIカテゴリ候補・要確認理由を表で確認
4. 人の確定カテゴリ列に戻す
5. GAS通知につなげる想定を確認
内容ブロック2:見るポイント
AI出力を候補列として扱う
人の確定列を別に持つ
台帳へ戻す前にレビュー表で確認する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S24 / 30
スライド見出し帯（そのまま表示）: 画面共有: 問い合わせ分類を台帳に戻す
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AI分類候補をそのまま確定せず、人の確認列を挟んで台帳へ戻す
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 事例
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実演手順
1. 問い合わせCSVを確認
2. Gem設計に沿って分類候補を出す
3. AIカテゴリ候補・要確認理由を表で確認
4. 人の確定カテゴリ列に戻す
5. GAS通知につなげる想定を確認
内容ブロック2:見るポイント
AI出力を候補列として扱う
人の確定列を別に持つ
台帳へ戻す前にレビュー表で確認する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S25. 章見出し/現在位置 5/6: AI出力レビューと運用

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 5/6 AI出力レビューと運用
- 前の章: 業務別ユースケース
- 次の章: 演習とまとめ
- 章の一言(画像内は小さく): ここから「5. AI出力レビューと運用」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「5」「AI出力レビューと運用」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「5 / 6」。
画面の隅に、前の章「業務別ユースケース」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「演習とまとめ」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S25」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「5. AI出力レビューと運用」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は20分、対象スライド範囲はS25-S27。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S26. AI出力レビューの4分類

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S26 / 30
- スライド見出し帯: AI出力レビューの4分類
- 今回のまとめ帯（要点・結論・次のアクション）: AI出力を事実・推測・要確認・利用不可に分けると、使ってよい部分と止める部分が明確になる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 5 レビュー
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:4分類表
| 分類 | 意味 | 扱い |
|---|---|---|
| 事実 | 入力や資料に根拠がある | 利用候補 |
| 推測 | 可能性として書かれている | 根拠確認後に利用 |
| 要確認 | 担当者・期限・判断が必要 | 人が確認 |
| 利用不可 | 根拠なし・危険・機密混入 | 使わない |
内容ブロック2:レビュー列
根拠
確認者
確定結果
台帳反映日
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S26 / 30
スライド見出し帯（そのまま表示）: AI出力レビューの4分類
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: AI出力を事実・推測・要確認・利用不可に分けると、使ってよい部分と止める部分が明確になる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 5 レビュー
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:4分類表
| 分類 | 意味 | 扱い |
|---|---|---|
| 事実 | 入力や資料に根拠がある | 利用候補 |
| 推測 | 可能性として書かれている | 根拠確認後に利用 |
| 要確認 | 担当者・期限・判断が必要 | 人が確認 |
| 利用不可 | 根拠なし・危険・機密混入 | 使わない |
内容ブロック2:レビュー列
根拠
確認者
確定結果
台帳反映日
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S27. 共有・履歴・フィードバックを運用する

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S27 / 30
- スライド見出し帯: 共有・履歴・フィードバックを運用する
- 今回のまとめ帯（要点・結論・次のアクション）: Gemやプロンプトは作って終わりではなく、分類ミスや確認結果を反映して更新する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 5 レビュー
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:改善ループ
使う → 出力をレビュー → ミスを記録 → 判断基準を更新 → 共有範囲に反映
内容ブロック2:運用ルール
| 観点 | 決めること |
|---|---|
| 更新責任 | 誰がGem設計書を直すか |
| 履歴 | 何をいつ変えたか |
| 共有範囲 | 誰が利用できるか |
| 禁止情報 | 入力してはいけない情報 |
内容ブロック3:第5回への接続
次回は、AI/GAS自動化を現場で止めないための要件定義・運用設計へ進む
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S27 / 30
スライド見出し帯（そのまま表示）: 共有・履歴・フィードバックを運用する
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Gemやプロンプトは作って終わりではなく、分類ミスや確認結果を反映して更新する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 5 レビュー
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:改善ループ
使う → 出力をレビュー → ミスを記録 → 判断基準を更新 → 共有範囲に反映
内容ブロック2:運用ルール
| 観点 | 決めること |
|---|---|
| 更新責任 | 誰がGem設計書を直すか |
| 履歴 | 何をいつ変えたか |
| 共有範囲 | 誰が利用できるか |
| 禁止情報 | 入力してはいけない情報 |
内容ブロック3:第5回への接続
次回は、AI/GAS自動化を現場で止めないための要件定義・運用設計へ進む
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S28. 章見出し/現在位置 6/6: 演習とまとめ

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 6/6 演習とまとめ
- 前の章: AI出力レビューと運用
- 次の章: なし
- 章の一言(画像内は小さく): ここから「6. 演習とまとめ」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「6」「演習とまとめ」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「6 / 6」。
画面の隅に、前の章「AI出力レビューと運用」を小さく薄い色のチップとして添える(あれば)。
最後の章のため、次の章の表示は省略し、代わりに「まとめへ」のような一言を小さく添えてもよい。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S28」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「6. 演習とまとめ」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は15分、対象スライド範囲はS28-S30。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S29. 演習: Gem設計書・プロンプト・レビュー表を作る

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S29 / 30
- スライド見出し帯: 演習: Gem設計書・プロンプト・レビュー表を作る
- 今回のまとめ帯（要点・結論・次のアクション）: 業務用Gemは、用途を1つに絞り、入力・出力・禁止事項・確認観点を具体化すると明日から試せる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 6 演習
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:動画一時停止ワーク
動画を一時停止して、10分ほど取り組んでください。取り組めたら再生してください。
内容ブロック2:作るもの
| 成果物 | 書く内容 |
|---|---|
| Gem設計書 | 目的・役割・入力・出力形式・禁止事項 |
| AI活用プロンプト | そのまま使える指示文 |
| レビュー表 | 事実・推測・要確認・利用不可の確認列 |
内容ブロック3:自己レビュー
実在情報を入れない設計になっているか
AIだけで確定しない列があるか
出力を台帳へ戻す手順が見えるか
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S29 / 30
スライド見出し帯（そのまま表示）: 演習: Gem設計書・プロンプト・レビュー表を作る
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 業務用Gemは、用途を1つに絞り、入力・出力・禁止事項・確認観点を具体化すると明日から試せる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 6 演習
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:動画一時停止ワーク
動画を一時停止して、10分ほど取り組んでください。取り組めたら再生してください。
内容ブロック2:作るもの
| 成果物 | 書く内容 |
|---|---|
| Gem設計書 | 目的・役割・入力・出力形式・禁止事項 |
| AI活用プロンプト | そのまま使える指示文 |
| レビュー表 | 事実・推測・要確認・利用不可の確認列 |
内容ブロック3:自己レビュー
実在情報を入れない設計になっているか
AIだけで確定しない列があるか
出力を台帳へ戻す手順が見えるか
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S30. 第4回まとめと第5回への橋渡し

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S30 / 30
- スライド見出し帯: 第4回まとめと第5回への橋渡し
- 今回のまとめ帯（要点・結論・次のアクション）: Gem/Geminiの出力を業務に戻すには、次回の要件定義・運用設計が欠かせない
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 6 演習
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:今日のまとめ
| 学んだこと | 持ち帰る成果物 |
|---|---|
| AIの役割分担 | AIを入れる位置の判断メモ |
| Gemini/Gemの使いどころ | 利用可否と代替手順メモ |
| Gem設計の型 | Gem設計書 |
| 出力レビュー | 出力レビュー表 |
内容ブロック2:次回準備
自分の業務でAI/GASを使うとしたら、誰が確認し、誰が直し、止まった時に誰が復旧するかを考えておく
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S30 / 30
スライド見出し帯（そのまま表示）: 第4回まとめと第5回への橋渡し
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Gem/Geminiの出力を業務に戻すには、次回の要件定義・運用設計が欠かせない
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 役割分担 / 2 Gemini-Gem / 3 Gem設計 / 4 事例 / 5 レビュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 6 演習
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:今日のまとめ
| 学んだこと | 持ち帰る成果物 |
|---|---|
| AIの役割分担 | AIを入れる位置の判断メモ |
| Gemini/Gemの使いどころ | 利用可否と代替手順メモ |
| Gem設計の型 | Gem設計書 |
| 出力レビュー | 出力レビュー表 |
内容ブロック2:次回準備
自分の業務でAI/GASを使うとしたら、誰が確認し、誰が直し、止まった時に誰が復旧するかを考えておく
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```
