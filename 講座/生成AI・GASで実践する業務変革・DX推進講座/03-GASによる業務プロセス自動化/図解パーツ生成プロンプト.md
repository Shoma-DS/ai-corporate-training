# 第3回 図解パーツ生成プロンプト

- 生成日時: 2026-07-05 20:01:01 JST
- 講座名: 生成AI・GASで実践する業務変革・DX推進講座
- セッション/テキスト名: 第3回 GASによる業務プロセス自動化
- 用途: HTML/Google Slides上で編集可能な講座名・回・S番号・タイトル・ヘッドラインを残したうえで、スライド本文の情報量を落とさない横長資料画像を1枚ずつ生成する。
- 生成サイズ: 全スライド共通で1536×1024（横3:2）。1枚ごとにサイズや縦横比を変えない。全ページ同一の図解ゾーン（600×322pt・約1.86:1）へ一括配置されるため、横に少し引き伸ばされる前提で平たく横長の構図にする。
- 上部余白: 画像上部約20%は白余白として空ける。テンプレートが編集可能なタイトルとヘッドラインをその位置に重ねる。下端にも小さな余白を残す（すぐ下に目次ストリップが来る）。
- 保存先: 生成・検品済みPNGを `図解パーツ/Sxx.png` として保存し、HTML/Google Slidesテンプレートへ添付する。
- 重要: 講座名、セッション名、S番号、セクション名、スピーカーノート、長い段落は画像内に固定しない。比較表、プロセス表、判断表、業種別例など元スライドの中核となる短いセル文は画像内に入れてよい。
- 画像内テキスト: 文字なし固定は禁止。役割名・プロセス名・成果物名・判断軸・手順・確認観点・具体例を、見出し、表セル、カード内説明として入れる。
- 密度基準: 元の全面スライド画像/スライド案にある判断軸、成果物、手順、リスク、確認観点、具体例を落とさない。S04のようなスライドでは、Before/After表、階層図、業種別例を1枚に併置し、2〜4領域・8〜20個程度の読める見出し/セル/短文を入れる。短いラベルだけの抽象図にしない。
- 例外: `章見出し/現在位置` または `章見出し` の目次切り替えスライドだけは、ユーザー指示により情報量を減らす。現在の目次項目を最も大きく、次の目次項目を次に大きく、前後関係と進捗だけを見せる。通常解説用の比較表・詳細カード・業種別例は入れない。
- 添付ルール: 生成後は `--embed-diagram-parts` でテンプレートへ挿入する。未生成・未添付・文字なしの飾り画像は完成扱いにしない。
- 共通画風: `isometric-corporate-clean`。白背景、ネイビー/ティール、薄いグレー罫線、法人研修向け、公式ロゴや実在UIを想像生成しない。

## 共通ネガティブプロンプト

```text
Do not create the editable template chrome. Do not render course title, session title, slide number, section header, speaker notes, long paragraph dumps, fake Google logos, fake Google UI, real personal data, email addresses, phone numbers, prices, QR codes, placeholder boxes, unreadable or incorrect Japanese, recruitment-ad or poster layouts, dramatic manga advertising, decorative dark backgrounds, or text areas meant to be filled later. The output is a dense supplemental diagram/reference-sheet image. Do not make it text-free, icon-only, or short-label-only; use readable Japanese headings, table cells, card text, and examples. If a comparison table or example table is the core source information, preserve it as visible structured text.
```

## S01 GASで繰り返し表作業を自動化する ─ 第3回

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: GASという「事務ロボット」を使えるようになることで、毎週同じ手順を繰り返す表作業から解放される
- 推奨図解パターン: module-block（表紙カード）
- 参照素材・スクリーンショット: なし（表紙スライド）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S01 / 導入とGASの位置づけ.
Slide topic for context: GASで繰り返し表作業を自動化する ─ 第3回.
Concept to visualize: GASという「事務ロボット」を使えるようになることで、毎週同じ手順を繰り返す表作業から解放される.
Suggested visual pattern: module-block（表紙カード）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: GASという「事務ロボット」を使えるようになることで、毎週同じ手順を繰り返す表作業から解放される
**内容ブロック①：今日の到達点（3つ）**
- 未対応・未請求行を自動で別シートへ抽出できる
- 処理件数・エラーをログシートに自動記録できる
- カスタムメニューから現場担当者が実行できる形を作れる
**内容ブロック②：今日の成果物**
- 自動処理スクリプト（`.gs` ファイル）
- 抽出結果シート（出力先）
- 処理ログシート
- コード読み替えメモ（ワークシート）
**内容ブロック③：受講者への問いかけ**
- 「今週、Excelかスプレッドシートで同じ操作を何回繰り返しましたか？」
Visible text candidates to use when useful, without reducing the source density: 今日の到達点 / 今日の成果物 / 受講者への問いかけ / 未対応・未請求行を自動で別シートへ抽出できる / 処理件数・エラーをログシートに自動記録できる / カスタムメニューから現場担当者が実行できる形を作れる / 自動処理スクリプト / 抽出結果シート / 処理ログシート / コード読み替えメモ / 事務ロボット.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S02 今日の2時間で作るもの

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: この2時間で「コードが読める」より「自分の業務に当てはめられる」状態を目指す
- 推奨図解パターン: checklist-confirmation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S02 / 導入とGASの位置づけ.
Slide topic for context: 今日の2時間で作るもの.
Concept to visualize: この2時間で「コードが読める」より「自分の業務に当てはめられる」状態を目指す.
Suggested visual pattern: checklist-confirmation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: この2時間で「コードが読める」より「自分の業務に当てはめられる」状態を目指す
**内容ブロック①：成果物カード（4枚）**
- スクリプトファイル: 未請求抽出・重複チェック・ログ追記が動く `.gs` ファイル
- 抽出結果シート: 未対応行・未請求行だけを別シートへ自動書き出し
- 処理ログシート: 実行日時・処理名・件数・エラー数を記録
- コード読み替えメモ: シート名・ヘッダー・条件・出力先を自分の業務に置き換えるメモ
**内容ブロック②：今日使う演習データ**
| ファイル名 | 内容 | 使う場面 |
|---|---|---|
| `CSV取込_売上明細_5月1週.csv` | 架空の売上明細50行 | 未請求抽出・ヘッダー確認 |
| `問い合わせ処理台帳_GAS演習.csv` | 架空の問い合わせ台帳30行 | 未対応抽出・ステータス更新 |
| `タスク期限リマインド_GAS演習.csv` | 架空のタスク一覧20行 | 期限超過確認・担当者別集計 |
Visible text candidates to use when useful, without reducing the source density: 成果物カード / 今日使う演習データ / スクリプトファイル / 抽出結果シート / 処理ログシート / コード読み替えメモ / ファイル名 / CSV取込_売上明細_5月1週.csv / 架空の売上明細50行 / 問い合わせ処理台帳_GAS演習.csv / 架空の問い合わせ台帳30行 / タスク期限リマインド_GAS演習.csv / 架空のタスク一覧20行 / コードが読める / 自分の業務に当てはめられる.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S03 第3回の目次/全体像

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: 今日は6つの章を順番に進み、成果物を作りながら次の回へ接続する
- 推奨図解パターン: roadmap-timeline（目次/全体像）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S03 / 導入とGASの位置づけ.
Slide topic for context: 第3回の目次/全体像.
Concept to visualize: 今日は6つの章を順番に進み、成果物を作りながら次の回へ接続する.
Suggested visual pattern: roadmap-timeline（目次/全体像）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 今日は6つの章を順番に進み、成果物を作りながら次の回へ接続する
**内容ブロック①：目次と成果物の全体像**
| No. | 目次項目 | 時間 | スライド範囲 | この章で支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入とGASの位置づけ | 15分 | S01–S09 | 自動処理の対象範囲メモ |
| 2 | JavaScript最小限 | 15分 | S10–S17 | コード読み替えメモの基礎 |
| 3 | SpreadsheetApp基礎 | 20分 | S18–S26 | SpreadsheetApp操作メモ |
| 4 | Excel的処理の自動化 | 25分 | S27–S36 | 自動処理スクリプトとログシート |
| 5 | カスタムメニューとエラー処理 | 20分 | S37–S43 | カスタムメニュー・テスト観点 |
| 6 | 演習とレビュー | 25分 | S44–S50 | 業務適用版の読み替えメモ |
**内容ブロック②：デモ/演習の位置**
| スライド | ここで行うこと |
|---|---|
| S25 | 画面共有：まず読み取ってログに出す |
| S36 | 画面共有：未請求抽出・転記・ログ追記を実行する |
| S45 | 演習：未対応行の抽出・転記・ログ追記 |
| S47 | 読み替える5項目 ─ ワーク |
**内容ブロック③：見方のルール**
- 各章の開始では「章見出し/現在位置」スライドで、今どこを学ぶかを確認する
- 通常スライドでは右上のセクション名とS番号で現在位置を追う
- 章ごとの成果物が、最終的に第6回の提案書へつながる
Visible text candidates to use when useful, without reducing the source density: 目次と成果物の全体像 / デモ/演習の位置 / 見方のルール / No. / 目次項目 / 1 / 導入とGASの位置づけ / 2 / JavaScript最小限 / 3 / SpreadsheetApp基礎 / 4 / Excel的処理の自動化 / 5 / カスタムメニューとエラー処理 / 6 / 演習とレビュー / スライド / ここで行うこと / S25.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S04 章見出し/現在位置 1/6: 導入とGASの位置づけ

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: 導入とGASの位置づけでは、GASで減らす作業と、人が判断する作業を切り分けることで、自動処理の対象範囲メモにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S04 / 導入とGASの位置づけ.
Slide topic for context: 章見出し/現在位置 1/6: 導入とGASの位置づけ.
Concept to visualize: 導入とGASの位置づけでは、GASで減らす作業と、人が判断する作業を切り分けることで、自動処理の対象範囲メモにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 1/6 導入とGASの位置づけ
現在章の範囲: S01-S09
時間目安: 15分
次の章: JavaScript最小限
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 導入とGASの位置づけでは、GASで減らす作業と、人が判断する作業を切り分けることで、自動処理の対象範囲メモにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 1/6 / 導入とGASの位置づけ / S01-S09 / 15分 / 次の章 / JavaScript最小限.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S05 第1回・第2回からのつながり

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: 業務選定（第1回）→台帳設計（第2回）→GAS実装（第3回）の順で積み上げることで、自動化は安定して動く
- 推奨図解パターン: process-flow（3ステップ）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S05 / 導入とGASの位置づけ.
Slide topic for context: 第1回・第2回からのつながり.
Concept to visualize: 業務選定（第1回）→台帳設計（第2回）→GAS実装（第3回）の順で積み上げることで、自動化は安定して動く.
Suggested visual pattern: process-flow（3ステップ）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 業務選定（第1回）→台帳設計（第2回）→GAS実装（第3回）の順で積み上げることで、自動化は安定して動く
**内容ブロック①：3ステップ図**
| ステップ | 回 | やったこと | 今回との接続 |
|---|---|---|---|
| 業務を選ぶ | 第1回 | 手作業の洗い出し・自動化優先度の整理 | 「どの列を抽出するか」の根拠 |
| 台帳を設計する | 第2回 | Formsの入力項目・Sheetsのヘッダー設計 | GASが読み取る列名・ステータス値 |
| 処理を自動化する | 第3回（今回） | 抽出・転記・集計・更新・ログ | 機械的な手作業を代替する |
**内容ブロック②：メタファー**
- 「GASは事務担当ロボットです。でも、渡す書類（台帳）の形が整っていないと、ロボットは毎回エラーを起こします。第2回で台帳を整えたのは、このロボットに渡す書類の規格を決めるためでした。」
- 「料理ロボットを動かす前に冷蔵庫の中身を揃えた、あのイメージです。」
**内容ブロック③：今日GASで扱う台帳の列構成（例）**
| 列名 | 内容 | GASが使う操作 |
|---|---|---|
| 受付日 | フォーム送信日時 | 期限計算の基準 |
| ステータス | 未対応 / 処理中 / 完了 | 抽出条件 |
| 担当者 | 部門Aなど | 担当者別集計の軸 |
| 請求フラグ | 未請求 / 請求済み | 未請求抽出の条件 |
| GAS更新日 | スクリプトが書き込む列 | 人は触らない |
Visible text candidates to use when useful, without reducing the source density: 3ステップ図 / メタファー / 今日GASで扱う台帳の列構成 / 業務を選ぶ / 第1回 / 台帳を設計する / 第2回 / 処理を自動化する / 第3回 / 「GASは事務担当ロボットです / 「料理ロボットを動かす前に冷蔵庫の中身を揃えた、あのイメージです / 受付日 / フォーム送信日時 / ステータス / 未対応 / 処理中 / 完了 / 担当者 / 部門Aなど / 請求フラグ / 未請求 / 請求済み / GAS更新日.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S06 GASは「表作業の事務ロボット」と覚える

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: GASを使うと、毎週手で繰り返していたフィルタ・コピー・貼り付け・集計の手順がスクリプト1回の実行に置き換わる
- 推奨図解パターン: before-after-transformation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S06 / 導入とGASの位置づけ.
Slide topic for context: GASは「表作業の事務ロボット」と覚える.
Concept to visualize: GASを使うと、毎週手で繰り返していたフィルタ・コピー・貼り付け・集計の手順がスクリプト1回の実行に置き換わる.
Suggested visual pattern: before-after-transformation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: GASを使うと、毎週手で繰り返していたフィルタ・コピー・貼り付け・集計の手順がスクリプト1回の実行に置き換わる
**内容ブロック①：Before/After（業種別）**
| 業種 | Before（手作業） | After（GAS） |
|---|---|---|
| 製造・卸売り | FAX/メールの注文を手書き台帳に転記 → 月末Excelに集計 | 台帳の新規行を自動集計・未請求だけ別シートへ転記 |
| サービス業 | 問い合わせメールをコピーして管理表に貼り付け | フォーム→Sheets台帳→未対応行を自動抽出 |
| 士業 | 各担当者のExcelで管理 → 月末に手動集計 | ステータス「未請求」行を一括抽出・件数ログ |
| 建設業 | 現場日報を紙→事務が転記 | GAS自動集計・担当者別件数を集計シートへ |
**内容ブロック②：GASが「得意なこと」と「不得意なこと」**
| 得意 | 不得意 |
|---|---|
| 決まった条件での行抽出 | 曖昧な内容の判断 |
| 定期的な集計・転記 | 例外が多い承認フロー |
| 重複・空欄チェック | 新しいパターンへの対応 |
| ログの自動記録 | 外部API・複雑な連携（発展） |
**内容ブロック③：覚えるキーワード（事務ロボットの仕事内容）**
- 読む（getValues）→ 条件で選ぶ（filter / if）→ 書く（setValues / appendRow）
- 上記の流れが基本。今日はこれだけ押さえれば十分。
Visible text candidates to use when useful, without reducing the source density: Before/After / GASが「得意なこと」と「不得意なこと」 / 覚えるキーワード / Before / 製造・卸売り / FAX/メールの注文を手書き台帳に転記 → 月末Excelに集計 / サービス業 / 問い合わせメールをコピーして管理表に貼り付け / 士業 / 各担当者のExcelで管理 → 月末に手動集計 / 建設業 / 現場日報を紙→事務が転記 / 得意 / 不得意 / 決まった条件での行抽出 / 曖昧な内容の判断 / 定期的な集計・転記 / 例外が多い承認フロー / 重複・空欄チェック / 新しいパターンへの対応.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S07 GASでやること・やらないこと・Geminiに任せること

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: 処理の種類を「GAS・Gemini・人」に振り分けることで、自動化の範囲が安全に広がる
- 推奨図解パターン: module-block
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S07 / 導入とGASの位置づけ.
Slide topic for context: GASでやること・やらないこと・Geminiに任せること.
Concept to visualize: 処理の種類を「GAS・Gemini・人」に振り分けることで、自動化の範囲が安全に広がる.
Suggested visual pattern: module-block.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 処理の種類を「GAS・Gemini・人」に振り分けることで、自動化の範囲が安全に広がる
**内容ブロック①：役割分担表**
| 担当 | 向いている処理 | 例 |
|---|---|---|
| GAS | 条件が決まっている機械的処理 | 未対応行の抽出、ステータス更新、ログ追記 |
| Gemini/Gem | 文章の分類・要約・下書き | 問い合わせカテゴリ分け、返信案、日報要約 |
| 人（担当者） | 承認・例外判断・最終確認 | 「要確認」フラグ行の確認、対外送信の判断 |
| Sheets | データ保持・閲覧・共有 | 台帳・集計表・ログの閲覧 |
**内容ブロック②：判断の流れ（図）**
コード例:
データ入力（Forms）
  ↓
台帳に蓄積（Sheets）
  ↓
機械的な仕分け・集計（GAS）
  ↓
曖昧な文章は整理（Gemini/Gem）
  ↓
確認・承認・送信（人）
コード例:
**内容ブロック③：「自動化しすぎない」3つの判断基準**
- 承認が必要なアクション（メール送信・外部転送）はGASに任せない
- 例外パターンが多い処理はGASでなく人が判断する
- ログを残さない自動実行は設計しない
Visible text candidates to use when useful, without reducing the source density: 役割分担表 / 判断の流れ / 「自動化しすぎない」3つの判断基準 / 担当 / 向いている処理 / GAS / 条件が決まっている機械的処理 / Gemini/Gem / 文章の分類・要約・下書き / 人 / 承認・例外判断・最終確認 / Sheets / データ保持・閲覧・共有 / 承認が必要なアクションはGASに任せない / 例外パターンが多い処理はGASでなく人が判断する / ログを残さない自動実行は設計しない / GAS・Gemini・人 / 要確認 / 自動化しすぎない.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S08 Apps Scriptエディタの基本画面

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: エディタ・実行ログ・トリガー・承認画面の4箇所を知っておくと、GASで困った時に自分で調べられる
- 推奨図解パターン: screen-annotation
- 参照素材・スクリーンショット: `スクリーンショット/gas-editor-overview.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S08 / 導入とGASの位置づけ.
Slide topic for context: Apps Scriptエディタの基本画面.
Concept to visualize: エディタ・実行ログ・トリガー・承認画面の4箇所を知っておくと、GASで困った時に自分で調べられる.
Suggested visual pattern: screen-annotation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: エディタ・実行ログ・トリガー・承認画面の4箇所を知っておくと、GASで困った時に自分で調べられる
**内容ブロック①：画面の構成（注釈付き）**
| エリア | 場所 | 役割 |
|---|---|---|
| コードエディタ | 中央 | スクリプトを書く・読む |
| 実行ボタン | ツールバー上部 | 選択した関数を手動実行する |
| 実行ログ | 下部パネル | Logger.log の出力を確認する |
| トリガー | 左サイドバー（時計アイコン） | 時間起動・フォーム送信時の自動実行を設定する |
| プロジェクト設定 | 左サイドバー（歯車アイコン） | スクリプトID・タイムゾーンを確認する |
**内容ブロック②：初回実行時の承認画面**
- 「このアプリはGoogleで確認されていません」が出たら → 「詳細」→「安全でないページに移動」
- 権限の意味を確認してから承認する（スプレッドシートへのアクセス権を許可する）
- 「何でも許可すればよい」という説明はしない。スコープの意味を確認する。
**内容ブロック③：よく使うショートカット**
| 操作 | ショートカット |
|---|---|
| 保存 | Ctrl+S / Cmd+S |
| 実行 | ツールバーの▶ボタン |
| ログを見る | 画面下部「実行ログ」タブ |
| 自動フォーマット | Ctrl+Shift+I / Cmd+Shift+I |
  （取得先: https://developers.google.com/apps-script/overview）
Visible text candidates to use when useful, without reducing the source density: 画面の構成 / 初回実行時の承認画面 / よく使うショートカット / エリア / 場所 / コードエディタ / 中央 / 実行ボタン / ツールバー上部 / 実行ログ / 下部パネル / トリガー / 左サイドバー / プロジェクト設定 / 権限の意味を確認してから承認する / 「何でも許可すればよい」という説明はしない / 操作 / ショートカット / 保存 / Ctrl+S / Cmd+S.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S09 情報管理と実行権限 ─ 始める前に確認する

- セクション: 導入とGASの位置づけ
- スライド側ヘッドライン（画像内には原則入れない）: GASを実行する前に「どのデータにアクセスするか」「誰が実行するか」「ログはどこに残るか」を確認することで、意図しないデータ漏えいを防げる
- 推奨図解パターン: governance-risk
- 参照素材・スクリーンショット: `スクリーンショット/gas-auth-screen.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S09 / 導入とGASの位置づけ.
Slide topic for context: 情報管理と実行権限 ─ 始める前に確認する.
Concept to visualize: GASを実行する前に「どのデータにアクセスするか」「誰が実行するか」「ログはどこに残るか」を確認することで、意図しないデータ漏えいを防げる.
Suggested visual pattern: governance-risk.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: GASを実行する前に「どのデータにアクセスするか」「誰が実行するか」「ログはどこに残るか」を確認することで、意図しないデータ漏えいを防げる
**内容ブロック①：スコープと共有範囲**
| 確認項目 | 内容 | 注意点 |
|---|---|---|
| スプレッドシートのスコープ | スクリプトがどのシートを読み書きするか | 不要なシートへのアクセスは削除する |
| スクリプトの共有 | 誰がスクリプトを実行・編集できるか | オーナー以外への編集権限は最小限に |
| 実データの入力禁止 | 演習中に実際の顧客名・メールを使わない | 架空データ（部門A・担当者B）のみ使う |
| 実行ログの扱い | Logger.log に実データが出力されていないか | ログに個人情報が混入しないよう設計する |
**内容ブロック②：演習での情報管理ルール**
- 演習データは `演習データ/` フォルダの架空CSVのみ使う
- 実在顧客名・社員名・メールアドレスを演習シートに入力しない
- GASのコード読解をGemini/AIに依頼する場合も、実データを貼り付けない
**内容ブロック③：権限の種類（参考）**
| 権限 | 意味 |
|---|---|
| spreadsheets（読み取り） | スプレッドシートを閲覧できる |
| spreadsheets（読み書き） | スプレッドシートを変更できる |
| gmail.send | GmailでメールをGASから送信できる |
| drive | Google Driveのファイルにアクセスできる |
  （取得先: https://developers.google.com/apps-script/guides/services/authorization）
Visible text candidates to use when useful, without reducing the source density: スコープと共有範囲 / 演習での情報管理ルール / 権限の種類 / 確認項目 / スプレッドシートのスコープ / スクリプトがどのシートを読み書きするか / スクリプトの共有 / 誰がスクリプトを実行・編集できるか / 実データの入力禁止 / 演習中に実際の顧客名・メールを使わない / 実行ログの扱い / Logger.log に実データが出力されていないか / 演習データは 演習データ/ フォルダの架空CSVのみ使う / 実在顧客名・社員名・メールアドレスを演習シートに入力しない / 権限 / 意味 / spreadsheets / スプレッドシートを閲覧できる / スプレッドシートを変更できる / gmail.send.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S10 章見出し/現在位置 2/6: JavaScript最小限

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: JavaScript最小限では、コード読解に必要な変数・配列・条件分岐・ループだけを押さえることで、コード読み替えメモの基礎につなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S10 / JavaScript最小限.
Slide topic for context: 章見出し/現在位置 2/6: JavaScript最小限.
Concept to visualize: JavaScript最小限では、コード読解に必要な変数・配列・条件分岐・ループだけを押さえることで、コード読み替えメモの基礎につなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 2/6 JavaScript最小限
現在章の範囲: S10-S17
時間目安: 15分
前の章: 導入とGASの位置づけ
次の章: SpreadsheetApp基礎
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: JavaScript最小限では、コード読解に必要な変数・配列・条件分岐・ループだけを押さえることで、コード読み替えメモの基礎につなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 2/6 / JavaScript最小限 / S10-S17 / 15分 / 前の章 / 導入とGASの位置づけ / 次の章 / SpreadsheetApp基礎.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S11 コードは「読む順番」があると速い

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: GASコードは「設定値 → 入力 → 条件 → 処理 → 出力 → ログ」の順に読むと、全体の意図がつかみやすい
- 推奨図解パターン: code-reading-flow（カラーハイライト）
- 参照素材・スクリーンショット: なし（コードサンプルで代替）

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S11 / JavaScript最小限.
Slide topic for context: コードは「読む順番」があると速い.
Concept to visualize: GASコードは「設定値 → 入力 → 条件 → 処理 → 出力 → ログ」の順に読むと、全体の意図がつかみやすい.
Suggested visual pattern: code-reading-flow（カラーハイライト）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: GASコードは「設定値 → 入力 → 条件 → 処理 → 出力 → ログ」の順に読むと、全体の意図がつかみやすい
**内容ブロック①：コードを読む5つのポイント**
| 読む場所 | 見るもの | 例 |
|---|---|---|
| ① 設定ブロック | シート名・列名・条件値 | `const TARGET_SHEET = '売上CSV'` |
| ② 入力 | どのシートのどの範囲を読むか | `sheet.getDataRange().getValues()` |
| ③ 条件 | どの行を処理するか | `if (row[statusCol] === '未請求')` |
| ④ 処理・出力 | 何をどこに書くか | `outputSheet.appendRow(row)` |
| ⑤ ログ | 何件処理されたか | `Logger.log(\`${count}件\`)` |
**内容ブロック②：読み替えの考え方**
- コードを変えるのは「設定ブロック」だけで済むよう設計する
- シート名・ヘッダー名・条件値を定数（const）に出しておくと変更が1箇所で済む
**内容ブロック③：色分けで見るコード全体像（概念図）**
コード例:
[設定]  const TARGET_SHEET = '売上CSV';
        const STATUS_COL = '請求ステータス';
[入力]  const values = sheet.getDataRange().getValues();
[条件]  if (row[colMap[STATUS_COL]] === '未請求') {
[処理]    result.push(row);
[ログ]  Logger.log(`抽出: ${result.length}件`);
コード例:
Visible text candidates to use when useful, without reducing the source density: コードを読む5つのポイント / 読み替えの考え方 / 色分けで見るコード全体像 / 読む場所 / 見るもの / ① 設定ブロック / シート名・列名・条件値 / ② 入力 / どのシートのどの範囲を読むか / ③ 条件 / どの行を処理するか / ④ 処理・出力 / 何をどこに書くか / ⑤ ログ / 何件処理されたか / コードを変えるのは「設定ブロック」だけで済むよう設計する / シート名・ヘッダー名・条件値を定数に出しておくと変更が1箇所で済む / 読む順番 / 設定ブロック.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S12 変数と定数 ─ 設定ブロックをまとめる理由

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: シート名・条件値を定数（const）にまとめることで、業務条件が変わったとき1箇所だけ直せばよくなる
- 推奨図解パターン: code-annotation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S12 / JavaScript最小限.
Slide topic for context: 変数と定数 ─ 設定ブロックをまとめる理由.
Concept to visualize: シート名・条件値を定数（const）にまとめることで、業務条件が変わったとき1箇所だけ直せばよくなる.
Suggested visual pattern: code-annotation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: シート名・条件値を定数（const）にまとめることで、業務条件が変わったとき1箇所だけ直せばよくなる
**内容ブロック①：変数と定数の違い**
| 種類 | キーワード | 使い方 | 例 |
|---|---|---|---|
| 定数（変わらない値） | `const` | シート名、列名、条件値 | `const SHEET_NAME = '売上CSV'` |
| 変数（途中で変わる値） | `let` | カウンター、一時的な結果 | `let count = 0` |
**内容ブロック②：設定ブロックのサンプル（実務パターン）**
コード例:
// ─── 設定ブロック（変えてよいのはここだけ）──────────
const INPUT_SHEET  = '売上CSV';       // 入力シート名
const OUTPUT_SHEET = '未請求一覧';    // 出力シート名
const LOG_SHEET    = '処理ログ';      // ログシート名
const STATUS_COL   = '請求ステータス'; // 抽出条件の列名
const TARGET_VAL   = '未請求';        // 抽出条件の値
// ────────────────────────────────────────────────────
コード例:
**内容ブロック③：業種別の読み替え例**
| 業種 | INPUT_SHEET の値 | STATUS_COL の値 | TARGET_VAL の値 |
|---|---|---|---|
| 製造・卸売り | 注文台帳 | 出荷ステータス | 未出荷 |
| 士業 | 依頼管理 | 処理状況 | 未対応 |
| サービス業 | 問い合わせ台帳 | 対応状況 | 未対応 |
| 建設業 | 日報台帳 | 確認済み | 未確認 |
Visible text candidates to use when useful, without reducing the source density: 変数と定数の違い / 設定ブロックのサンプル / 業種別の読み替え例 / 種類 / キーワード / 定数 / const / 変数 / let / INPUT_SHEET の値 / 製造・卸売り / 注文台帳 / 士業 / 依頼管理 / サービス業 / 問い合わせ台帳 / 建設業 / 日報台帳.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S13 配列と2次元配列 ─ スプレッドシートをコードで見ると

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: `getValues()` を使うとスプレッドシートが「行の配列」として読み込まれる。この構造を理解するとコードが読める
- 推奨図解パターン: table-to-array-visual
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S13 / JavaScript最小限.
Slide topic for context: 配列と2次元配列 ─ スプレッドシートをコードで見ると.
Concept to visualize: `getValues()` を使うとスプレッドシートが「行の配列」として読み込まれる。この構造を理解するとコードが読める.
Suggested visual pattern: table-to-array-visual.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `getValues()` を使うとスプレッドシートが「行の配列」として読み込まれる。この構造を理解するとコードが読める
**内容ブロック①：スプレッドシートと配列の対応（図）**
コード例:
スプレッドシート:
  行1: 受付日 | ステータス | 担当者
  行2: 5/1  | 未対応  | 部門A
  行3: 5/2  | 完了    | 部門B
getValues() の結果（2次元配列）:
  [
    ['受付日', 'ステータス', '担当者'],  ← values[0] ヘッダー行
    ['5/1',   '未対応',   '部門A'],    ← values[1]
    ['5/2',   '完了',     '部門B'],    ← values[2]
  ]
コード例:
**内容ブロック②：ヘッダー行とデータ行の切り分け**
コード例:
const values  = sheet.getDataRange().getValues();
const headers = values[0];          // 先頭行 = ヘッダー
const rows    = values.slice(1);    // 2行目以降 = データ行
Logger.log(`${rows.length}件のデータがあります`);
コード例:
**内容ブロック③：列番号のイメージ（0始まり）**
| スプレッドシートの列 | 配列インデックス | `row[n]` で取得できる値 |
|---|---|---|
| A列（受付日） | 0 | `row[0]` |
| B列（ステータス） | 1 | `row[1]` |
| C列（担当者） | 2 | `row[2]` |
Visible text candidates to use when useful, without reducing the source density: スプレッドシートと配列の対応 / ヘッダー行とデータ行の切り分け / 列番号のイメージ / スプレッドシートの列 / 配列インデックス / A列 / 0 / B列 / 1 / C列 / 2 / 行の配列.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S14 ヘッダー名から列番号を探す（headerMap）

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: 列番号を直接書かず、ヘッダー名から探す設計にすることで、列の追加・並び替えでコードが壊れなくなる
- 推奨図解パターン: code-comparison（NG/OK対比）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S14 / JavaScript最小限.
Slide topic for context: ヘッダー名から列番号を探す（headerMap）.
Concept to visualize: 列番号を直接書かず、ヘッダー名から探す設計にすることで、列の追加・並び替えでコードが壊れなくなる.
Suggested visual pattern: code-comparison（NG/OK対比）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 列番号を直接書かず、ヘッダー名から探す設計にすることで、列の追加・並び替えでコードが壊れなくなる
**内容ブロック①：列番号を直接書く問題**
コード例:
// NG: 列の並びが変わったら全部直さないといけない
const status = row[3];   // B列が追加されると壊れる
コード例:
**内容ブロック②：headerMapで解決する**
コード例:
// OK: ヘッダー名から列番号を自動的に探す
function createHeaderMap(headers) {
  return Object.fromEntries(
    headers.map((name, index) => [name, index])
  );
}
// 使い方
const colMap = createHeaderMap(headers);
const status = row[colMap['請求ステータス']];  // 列が移動しても壊れない
コード例:
**内容ブロック③：headerMapの実務メリット**
| 状況 | 固定列番号 | headerMap |
|---|---|---|
| 列を1列追加した | 全行の列番号を直す必要 | 自動対応（変更不要） |
| ヘッダー名を変えた | 番号で管理しているので影響なし | ヘッダー名を1箇所直す |
| 新しいシートに使い回す | ヘッダーが違えば全部直す | シート名と条件だけ変える |
Visible text candidates to use when useful, without reducing the source density: 列番号を直接書く問題 / headerMapで解決する / headerMapの実務メリット / 状況 / 固定列番号 / 列を1列追加した / 全行の列番号を直す必要 / ヘッダー名を変えた / 番号で管理しているので影響なし / 新しいシートに使い回す / ヘッダーが違えば全部直す.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S15 条件分岐 ─ 「この行だけ処理する」を書く

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: `if` 文で「どの行を処理するか」を決めることで、全件ではなく対象行だけを正確に選べる
- 推奨図解パターン: code-flow-branch
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S15 / JavaScript最小限.
Slide topic for context: 条件分岐 ─ 「この行だけ処理する」を書く.
Concept to visualize: `if` 文で「どの行を処理するか」を決めることで、全件ではなく対象行だけを正確に選べる.
Suggested visual pattern: code-flow-branch.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `if` 文で「どの行を処理するか」を決めることで、全件ではなく対象行だけを正確に選べる
**内容ブロック①：基本の if 文パターン（実務4例）**
コード例:
// パターン1: 未請求行を選ぶ
if (row[colMap['請求ステータス']] === '未請求') { ... }
// パターン2: 担当者が空欄の行をスキップ
if (!row[colMap['担当者']] || row[colMap['担当者']] === '') continue;
// パターン3: 期限を過ぎた行
const deadline = new Date(row[colMap['期限日']]);
if (deadline < new Date()) { ... }
// パターン4: 複数条件（未対応 かつ 期限超過）
if (status === '未対応' && deadline < today) { ... }
コード例:
**内容ブロック②：条件の業種別読み替え**
| 業種 | 列名 | 条件値 | 処理内容 |
|---|---|---|---|
| 製造・卸売り | 出荷ステータス | 未出荷 | 未出荷行を別シートへ転記 |
| 士業 | 処理状況 | 未対応 | 未対応行のみ担当者通知候補へ |
| サービス業 | 対応状況 | 要確認 | 要確認フラグ行を抽出 |
| 建設業 | 確認済み | 未確認 | 未確認日報行を管理者シートへ |
**内容ブロック③：注意：条件値は完全一致**
- スプレッドシートの「未対応 」（末尾スペース）と「未対応」は不一致になる
- `.trim()` で空白を除去してから比較する: `row[col].toString().trim() === '未対応'`
Visible text candidates to use when useful, without reducing the source density: 基本の if 文パターン / 条件の業種別読み替え / 注意 / 製造・卸売り / 出荷ステータス / 士業 / 処理状況 / サービス業 / 対応状況 / 建設業 / 確認済み / スプレッドシートの「未対応 」と「未対応」は不一致になる / .trim で空白を除去してから比較する / この行だけ処理する / どの行を処理するか / 未対応.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S16 ループ ─ 全行を1件ずつ確認する

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: `for...of` でデータ行を1行ずつ確認することで、「目で探す」作業がコードに置き換わる
- 推奨図解パターン: process-flow（ループ図）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S16 / JavaScript最小限.
Slide topic for context: ループ ─ 全行を1件ずつ確認する.
Concept to visualize: `for...of` でデータ行を1行ずつ確認することで、「目で探す」作業がコードに置き換わる.
Suggested visual pattern: process-flow（ループ図）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `for...of` でデータ行を1行ずつ確認することで、「目で探す」作業がコードに置き換わる
**内容ブロック①：ループの基本形**
コード例:
const rows = values.slice(1);  // ヘッダー行を除くデータ行
for (const row of rows) {
  // ここに「1行に対してやりたい処理」を書く
  const status = row[colMap['請求ステータス']];
  if (status === '未請求') {
    result.push(row);  // 条件を満たした行を配列に追加
  }
}
コード例:
**内容ブロック②：ループで処理できる「繰り返し手作業」**
| 手作業 | GASのループ処理 |
|---|---|
| 1行ずつ目でステータスを確認する | `for...of` でステータス列をチェック |
| 「未対応」をCtrl+Fで探す | `if` 条件で一致行を集める |
| 期限を1件ずつ今日の日付と比較する | 日付オブジェクトで比較 |
| 月末に件数を目で数える | `count++` でカウント |
**内容ブロック③：結果をまとめて書き出す（push＋setValues）**
コード例:
const result = [];   // 結果を入れる配列
for (const row of rows) {
  if (row[colMap['請求ステータス']] === '未請求') {
    result.push(row);
  }
}
// 全件処理が終わってから1回だけ書き出す（効率的）
if (result.length > 0) {
  outputSheet.getRange(2, 1, result.length, result[0].length)
             .setValues(result);
}
コード例:
Visible text candidates to use when useful, without reducing the source density: ループの基本形 / ループで処理できる「繰り返し手作業」 / 結果をまとめて書き出す / 手作業 / GASのループ処理 / 1行ずつ目でステータスを確認する / for...of でステータス列をチェック / 「未対応」をCtrl+Fで探す / if 条件で一致行を集める / 期限を1件ずつ今日の日付と比較する / 日付オブジェクトで比較 / 月末に件数を目で数える / count++ でカウント / 目で探す / 1行に対してやりたい処理 / 繰り返し手作業 / 未対応.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S17 日付・文字列・空欄の扱い ─ 実務の注意点

- セクション: JavaScript最小限
- スライド側ヘッドライン（画像内には原則入れない）: 日付のズレ・金額文字列・空欄の3つを知っておくと、現場データ特有のエラーを事前に防げる
- 推奨図解パターン: caution-card-set
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S17 / JavaScript最小限.
Slide topic for context: 日付・文字列・空欄の扱い ─ 実務の注意点.
Concept to visualize: 日付のズレ・金額文字列・空欄の3つを知っておくと、現場データ特有のエラーを事前に防げる.
Suggested visual pattern: caution-card-set.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 日付のズレ・金額文字列・空欄の3つを知っておくと、現場データ特有のエラーを事前に防げる
**内容ブロック①：日付の扱い**
| 状況 | コード | 注意点 |
|---|---|---|
| 今日と比較する | `new Date()` | タイムゾーンは設定で確認する |
| セルの日付を取得 | `new Date(row[col])` | 文字列の場合は変換が必要 |
| 日付差を日数で出す | `(today - date) / 86400000` | ミリ秒で返るため86400000で割る |
**内容ブロック②：金額・数値文字列の注意**
コード例:
// スプレッドシートに「¥1,200」と入っていると数値として扱えない
const amount = Number(String(row[colMap['金額']]).replace(/[¥,]/g, ''));
// → 1200（数値）
コード例:
**内容ブロック③：空欄チェックのパターン**
コード例:
// 空欄・null・undefined・空白スペースをまとめて確認
function isEmpty(val) {
  return val === '' || val === null || val === undefined
    || String(val).trim() === '';
}
// 使い方
if (isEmpty(row[colMap['担当者']])) {
  Logger.log(`担当者未設定: 行${i + 2}`);
  continue;  // この行はスキップ
}
コード例:
Visible text candidates to use when useful, without reducing the source density: 日付の扱い / 金額・数値文字列の注意 / 空欄チェックのパターン / 状況 / コード / 今日と比較する / new Date / セルの日付を取得 / 日付差を日数で出す / / 86400000 / String.trim === ''; / ¥1,200.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S18 章見出し/現在位置 3/6: SpreadsheetApp基礎

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: SpreadsheetApp基礎では、シート・範囲・値・ヘッダーを扱い、台帳を壊さず読み書きすることで、SpreadsheetApp操作メモにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S18 / SpreadsheetApp基礎.
Slide topic for context: 章見出し/現在位置 3/6: SpreadsheetApp基礎.
Concept to visualize: SpreadsheetApp基礎では、シート・範囲・値・ヘッダーを扱い、台帳を壊さず読み書きすることで、SpreadsheetApp操作メモにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 3/6 SpreadsheetApp基礎
現在章の範囲: S18-S26
時間目安: 20分
前の章: JavaScript最小限
次の章: Excel的処理の自動化
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: SpreadsheetApp基礎では、シート・範囲・値・ヘッダーを扱い、台帳を壊さず読み書きすることで、SpreadsheetApp操作メモにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 3/6 / SpreadsheetApp基礎 / S18-S26 / 20分 / 前の章 / JavaScript最小限 / 次の章 / Excel的処理の自動化.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S19 SpreadsheetAppで触れる範囲 ─ 4つの階層

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: Spreadsheet→Sheet→Range→Valuesの4階層を理解することで、「どこのデータを操作しているか」がコードを読んだときに一目でわかる
- 推奨図解パターン: hierarchy-diagram
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S19 / SpreadsheetApp基礎.
Slide topic for context: SpreadsheetAppで触れる範囲 ─ 4つの階層.
Concept to visualize: Spreadsheet→Sheet→Range→Valuesの4階層を理解することで、「どこのデータを操作しているか」がコードを読んだときに一目でわかる.
Suggested visual pattern: hierarchy-diagram.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: Spreadsheet→Sheet→Range→Valuesの4階層を理解することで、「どこのデータを操作しているか」がコードを読んだときに一目でわかる
**内容ブロック①：4階層の関係図**
コード例:
SpreadsheetApp（Googleスプレッドシート全体）
  └─ Spreadsheet（1つのスプレッドシートファイル）
       └─ Sheet（1つのシートタブ）
            └─ Range（セル範囲）
                 └─ Values（配列データ）
コード例:
**内容ブロック②：各階層で使うメソッド**
| 階層 | よく使うメソッド | 意味 |
|---|---|---|
| SpreadsheetApp | `getActive()` | 現在開いているスプレッドシートを取得 |
| Spreadsheet | `getSheetByName('シート名')` | シート名でシートを取得 |
| Sheet | `getDataRange()` | データが入っている全範囲を取得 |
| Range | `getValues()` | 2次元配列として値を取得 |
| Range | `setValues(array)` | 2次元配列を書き込む |
**内容ブロック③：よくある間違い**
| 間違い | 正しい書き方 |
|---|---|
| `getSheet()` | `getSheetByName('シート名')` |
| `getRange().getValue()` | `getDataRange().getValues()` |
| シートがないのに `getSheetByName()` | `if (!sheet)` でnullチェックする |
Visible text candidates to use when useful, without reducing the source density: 4階層の関係図 / 各階層で使うメソッド / よくある間違い / 階層 / よく使うメソッド / SpreadsheetApp / getActive / Spreadsheet / getSheetByName / Sheet / getDataRange / Range / getValues / setValues / 間違い / 正しい書き方 / getSheet / getRange.getValue / getDataRange.getValues / シートがないのに getSheetByName.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S20 シートを取得する

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: `getSheetByName()` でシート名を指定することで、スプレッドシート内の目的のシートだけを正確に操作できる
- 推奨図解パターン: code-annotation
- 参照素材・スクリーンショット: `スクリーンショット/gas-sheet-tabs.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S20 / SpreadsheetApp基礎.
Slide topic for context: シートを取得する.
Concept to visualize: `getSheetByName()` でシート名を指定することで、スプレッドシート内の目的のシートだけを正確に操作できる.
Suggested visual pattern: code-annotation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `getSheetByName()` でシート名を指定することで、スプレッドシート内の目的のシートだけを正確に操作できる
**内容ブロック①：シート取得の基本**
コード例:
const ss    = SpreadsheetApp.getActive();
const sheet = ss.getSheetByName('売上CSV');
// シートが存在しない場合のnullチェック（必須）
if (!sheet) {
  Logger.log('エラー: シート「売上CSV」が見つかりません');
  return;
}
コード例:
**内容ブロック②：`getActive()` と `openById()` の違い**
| メソッド | 使う場面 | 注意 |
|---|---|---|
| `getActive()` | スクリプトと同じスプレッドシート | 最もよく使う |
| `openById('ID')` | 別のスプレッドシートを操作 | DriveのURLからIDを取得する |
**内容ブロック③：複数シートを扱う実務パターン**
コード例:
const inputSheet  = ss.getSheetByName('問い合わせ台帳');   // 入力元
const outputSheet = ss.getSheetByName('未対応一覧');        // 出力先
const logSheet    = ss.getSheetByName('処理ログ');          // ログ
// いずれかがなければ即時終了
[inputSheet, outputSheet, logSheet].forEach((s, i) => {
  if (!s) throw new Error(`シートが見つかりません: インデックス${i}`);
});
コード例:
  （取得先: https://developers.google.com/apps-script/reference/spreadsheet/spreadsheet-app）
Visible text candidates to use when useful, without reducing the source density: シート取得の基本 / getActive と openById の違い / 複数シートを扱う実務パターン / メソッド / 使う場面 / getActive / スクリプトと同じスプレッドシート / openById / 別のスプレッドシートを操作 / 売上CSV.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S21 範囲を取得する

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: `getDataRange()` でデータのある全範囲を自動取得することで、行数が増えても修正不要のコードが書ける
- 推奨図解パターン: range-diagram（セル範囲の視覚化）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S21 / SpreadsheetApp基礎.
Slide topic for context: 範囲を取得する.
Concept to visualize: `getDataRange()` でデータのある全範囲を自動取得することで、行数が増えても修正不要のコードが書ける.
Suggested visual pattern: range-diagram（セル範囲の視覚化）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `getDataRange()` でデータのある全範囲を自動取得することで、行数が増えても修正不要のコードが書ける
**内容ブロック①：よく使う範囲取得メソッド**
| メソッド | 意味 | 使いどころ |
|---|---|---|
| `getDataRange()` | データが入っている最大範囲 | 台帳全体を読む時 |
| `getRange(row, col, numRows, numCols)` | 行・列を数値指定 | 特定の出力先セルを指定する時 |
| `getLastRow()` | データが入っている最終行 | appendRowの前に確認する時 |
| `getLastColumn()` | データが入っている最終列 | 列数が動的に変わる時 |
**内容ブロック②：実務でよく使う組み合わせ**
コード例:
// パターン1: 全データを読む（最も多い）
const values = sheet.getDataRange().getValues();
// パターン2: 出力先の先頭から書き込む（転記）
const lastRow = outputSheet.getLastRow();
outputSheet.getRange(lastRow + 1, 1, result.length, result[0].length)
           .setValues(result);
コード例:
**内容ブロック③：注意：`getDataRange()` はヘッダー行も含む**
- `values[0]` = ヘッダー行
- `values.slice(1)` = データ行のみ
- ヘッダー行をデータとして処理しないよう必ず `slice(1)` する
Visible text candidates to use when useful, without reducing the source density: よく使う範囲取得メソッド / 実務でよく使う組み合わせ / 注意 / メソッド / 意味 / getDataRange / データが入っている最大範囲 / getRange / 行・列を数値指定 / getLastRow / データが入っている最終行 / getLastColumn / データが入っている最終列 / values[0] = ヘッダー行 / values.slice = データ行のみ / ヘッダー行をデータとして処理しないよう必ず slice する.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S22 読む・処理する・まとめて書く ─ 3ステップの基本

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: 「全件読む→条件で絞る→まとめて書く」の3ステップを守ることで、処理が速く、上書きミスが起きにくくなる
- 推奨図解パターン: 3-step-process-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S22 / SpreadsheetApp基礎.
Slide topic for context: 読む・処理する・まとめて書く ─ 3ステップの基本.
Concept to visualize: 「全件読む→条件で絞る→まとめて書く」の3ステップを守ることで、処理が速く、上書きミスが起きにくくなる.
Suggested visual pattern: 3-step-process-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 「全件読む→条件で絞る→まとめて書く」の3ステップを守ることで、処理が速く、上書きミスが起きにくくなる
**内容ブロック①：3ステップの全体構造**
コード例:
// Step 1: まとめて読む
const values = inputSheet.getDataRange().getValues();
const headers = values[0];
const rows    = values.slice(1);
const colMap  = createHeaderMap(headers);
// Step 2: 条件で絞る（メモリ内で処理）
const result = [];
for (const row of rows) {
  if (row[colMap['請求ステータス']].toString().trim() === '未請求') {
    result.push(row);
  }
}
// Step 3: まとめて書く（1回だけ）
if (result.length > 0) {
  outputSheet.getRange(2, 1, result.length, result[0].length)
             .setValues(result);
}
Logger.log(`未請求: ${result.length}件`);
コード例:
**内容ブロック②：「1行ずつ書く」より「まとめて書く」が速い理由**
| 方法 | 速度 | リスク |
|---|---|---|
| ループ内で `appendRow()` を毎回呼ぶ | 遅い（100行で数十秒） | 実行時間超過エラーになることがある |
| `setValues()` で一括書き込み | 速い | 形が合っていないとエラーになる |
| `appendRow()` はログ追記のみ | 用途限定 | 1行ずつ追記するときだけ使う |
Visible text candidates to use when useful, without reducing the source density: 3ステップの全体構造 / 「1行ずつ書く」より「まとめて書く」が速い理由 / 方法 / 速度 / ループ内で appendRow を毎回呼ぶ / 遅い / setValues で一括書き込み / 速い / appendRow はログ追記のみ / 用途限定 / 1行ずつ書く / まとめて書く.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S23 `setValues()` の形を合わせる ─ よくあるエラーと対処

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: `setValues()` は「渡す配列の行数・列数」と「書き込むRange」が一致していないとエラーになる
- 推奨図解パターン: error-check-cards
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S23 / SpreadsheetApp基礎.
Slide topic for context: `setValues()` の形を合わせる ─ よくあるエラーと対処.
Concept to visualize: `setValues()` は「渡す配列の行数・列数」と「書き込むRange」が一致していないとエラーになる.
Suggested visual pattern: error-check-cards.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `setValues()` は「渡す配列の行数・列数」と「書き込むRange」が一致していないとエラーになる
**内容ブロック①：形のチェックポイント**
コード例:
// setValues() に渡す配列は必ず2次元配列
// NG: 1次元配列
outputSheet.getRange(2,1,1,3).setValues(['a','b','c']);  // エラー
// OK: 2次元配列
outputSheet.getRange(2,1,1,3).setValues([['a','b','c']]);
// 複数行の場合
outputSheet.getRange(2, 1, result.length, result[0].length)
           .setValues(result);  // result は [[...],[...]] の形
コード例:
**内容ブロック②：結果が0件の場合の扱い**
コード例:
if (result.length === 0) {
  Logger.log('対象行なし。処理を終了します。');
  return;  // 0件でも正常終了（エラーではない）
}
コード例:
**内容ブロック③：よくあるエラーとメッセージ**
| エラーメッセージ | 原因 | 対処 |
|---|---|---|
| `Incorrect range height` | rowの数とrangeのheightが不一致 | `result.length` を確認する |
| `Incorrect range width` | colの数が不一致 | `result[0].length` を使う |
| `Cannot read property '0'` | resultが空でsetValuesを呼んだ | `result.length > 0` を確認する |
Visible text candidates to use when useful, without reducing the source density: 形のチェックポイント / 結果が0件の場合の扱い / よくあるエラーとメッセージ / エラーメッセージ / 原因 / Incorrect range height / rowの数とrangeのheightが不一致 / Incorrect range width / colの数が不一致 / Cannot read property '0' / resultが空でsetValuesを呼んだ / 渡す配列の行数・列数 / 書き込むRange.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S24 ヘッダー行を基準にする ─ 列の追加・並び替えに強いコード

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: ヘッダー名を基準に列を探す設計にすることで、台帳の列構成が変わってもコード修正が最小限になる
- 推奨図解パターン: comparison-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S24 / SpreadsheetApp基礎.
Slide topic for context: ヘッダー行を基準にする ─ 列の追加・並び替えに強いコード.
Concept to visualize: ヘッダー名を基準に列を探す設計にすることで、台帳の列構成が変わってもコード修正が最小限になる.
Suggested visual pattern: comparison-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: ヘッダー名を基準に列を探す設計にすることで、台帳の列構成が変わってもコード修正が最小限になる
**内容ブロック①：固定列番号 vs ヘッダーMap（比較）**
| 比較軸 | 固定列番号（`row[2]`） | headerMap（`row[colMap['担当者']]`） |
|---|---|---|
| 列追加 | 全行のインデックスを直す | 変更不要 |
| 列の並び替え | 全行のインデックスを直す | 変更不要 |
| 可読性 | `row[2]` が何かわからない | `colMap['担当者']` で意図が明確 |
| デメリット | ヘッダー名変更で壊れる | 設定が1行増える |
**内容ブロック②：createHeaderMap の完全版**
コード例:
function createHeaderMap(headers) {
  const map = {};
  headers.forEach((name, i) => {
    if (name !== '') map[name] = i;  // 空欄ヘッダーは登録しない
  });
  return map;
}
コード例:
**内容ブロック③：ワークシートでの読み替え練習**
受講者記入欄（ワークシートに対応）:
- 自分の台帳の列名: ___
- GASで使う colMap キー名: ___
- 条件として使う値: ___
Visible text candidates to use when useful, without reducing the source density: 固定列番号 vs ヘッダーMap / createHeaderMap の完全版 / ワークシートでの読み替え練習 / 比較軸 / 固定列番号 / 列追加 / 全行のインデックスを直す / 列の並び替え / 可読性 / row[2] が何かわからない / デメリット / ヘッダー名変更で壊れる / 自分の台帳の列名 / GASで使う colMap キー名 / 条件として使う値.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S25 画面共有：まず読み取ってログに出す

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: 最初のステップはデータを読み取ってログに出力するだけ。処理前に「何が読めているか」を確認する習慣が重要
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/gas-execution-log.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S25 / SpreadsheetApp基礎.
Slide topic for context: 画面共有：まず読み取ってログに出す.
Concept to visualize: 最初のステップはデータを読み取ってログに出力するだけ。処理前に「何が読めているか」を確認する習慣が重要.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 最初のステップはデータを読み取ってログに出力するだけ。処理前に「何が読めているか」を確認する習慣が重要
**内容ブロック①：画面共有の手順**
1. Apps Scriptエディタを開く（`script.google.com`）
2. `checkRows` 関数を実行（▶ボタン）
3. 実行ログで「ヘッダー行」「データ件数」を確認する
4. ヘッダー名のスペルが合っているか確認する
**内容ブロック②：確認用コード（全員で使う）**
コード例:
function checkRows() {
  const sheet = SpreadsheetApp.getActive()
                              .getSheetByName('問い合わせ台帳');
  if (!sheet) { Logger.log('シートが見つかりません'); return; }
  const values  = sheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  Logger.log('ヘッダー: ' + JSON.stringify(headers));
  Logger.log(`データ: ${rows.length}件`);
  Logger.log('先頭行: ' + JSON.stringify(rows[0]));
}
コード例:
**内容ブロック③：確認するポイント**
- ヘッダー名に余分なスペースがないか
- 先頭行の各値が想定どおりの型か（日付・文字列・数値）
- データ件数が台帳の行数と一致しているか
  （取得先: https://developers.google.com/apps-script/guides/logging）
Visible text candidates to use when useful, without reducing the source density: 画面共有の手順 / 確認用コード / 確認するポイント / ヘッダー名に余分なスペースがないか / 先頭行の各値が想定どおりの型か / データ件数が台帳の行数と一致しているか / 何が読めているか / ヘッダー行 / データ件数.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S26 GASで更新する列・人が更新する列を分ける

- セクション: SpreadsheetApp基礎
- スライド側ヘッドライン（画像内には原則入れない）: 台帳の列を「GAS更新列」と「人更新列」に分けることで、スクリプトによる上書き事故と属人化を同時に防げる
- 推奨図解パターン: column-role-design
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S26 / SpreadsheetApp基礎.
Slide topic for context: GASで更新する列・人が更新する列を分ける.
Concept to visualize: 台帳の列を「GAS更新列」と「人更新列」に分けることで、スクリプトによる上書き事故と属人化を同時に防げる.
Suggested visual pattern: column-role-design.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 台帳の列を「GAS更新列」と「人更新列」に分けることで、スクリプトによる上書き事故と属人化を同時に防げる
**内容ブロック①：列の役割分担（設計例）**
| 列名 | 更新者 | タイミング | 備考 |
|---|---|---|---|
| 受付日 | フォーム自動 | 送信時 | 変更不可 |
| 問い合わせ内容 | 顧客入力 | 送信時 | 変更不可 |
| ステータス | 担当者（人） | 対応後 | 人が書く |
| 担当者名 | 担当者（人） | 割り当て後 | 人が書く |
| GAS処理日時 | GAS自動 | スクリプト実行時 | 人は触らない |
| 抽出フラグ | GAS自動 | スクリプト実行時 | 人は触らない |
| 確認メモ | 担当者（人） | 要確認時 | 人が書く |
**内容ブロック②：GAS更新列を設計する3つのルール**
- GASが書く列は末尾にまとめる（視覚的に分かりやすい）
- GASが書く列のヘッダーには「GAS_」などのプレフィックスをつける
- GAS更新列を人が誤って編集できないよう、列保護（シート保護）を検討する
Visible text candidates to use when useful, without reducing the source density: 列の役割分担 / GAS更新列を設計する3つのルール / 更新者 / 受付日 / フォーム自動 / 問い合わせ内容 / 顧客入力 / ステータス / 担当者 / 担当者名 / GAS処理日時 / GAS自動 / 抽出フラグ / 確認メモ / GASが書く列は末尾にまとめる / GASが書く列のヘッダーには「GAS_」などのプレフィックスをつける / GAS更新列を人が誤って編集できないよう、列保護を検討する / GAS更新列 / 人更新列 / GAS_.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S27 章見出し/現在位置 4/6: Excel的処理の自動化

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: Excel的処理の自動化では、抽出・転記・集計・重複チェック・更新・ログを業務処理に落とし込むことで、自動処理スクリプトとログシートにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S27 / Excel的処理の自動化.
Slide topic for context: 章見出し/現在位置 4/6: Excel的処理の自動化.
Concept to visualize: Excel的処理の自動化では、抽出・転記・集計・重複チェック・更新・ログを業務処理に落とし込むことで、自動処理スクリプトとログシートにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 4/6 Excel的処理の自動化
現在章の範囲: S27-S36
時間目安: 25分
前の章: SpreadsheetApp基礎
次の章: カスタムメニューとエラー処理
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: Excel的処理の自動化では、抽出・転記・集計・重複チェック・更新・ログを業務処理に落とし込むことで、自動処理スクリプトとログシートにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 4/6 / Excel的処理の自動化 / S27-S36 / 25分 / 前の章 / SpreadsheetApp基礎 / 次の章 / カスタムメニューとエラー処理.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S28 Excel的処理をGASに置き換える ─ 対応表

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: Excelで手動でやっていた処理のほとんどはGASの6パターンで代替できる
- 推奨図解パターン: replacement-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S28 / Excel的処理の自動化.
Slide topic for context: Excel的処理をGASに置き換える ─ 対応表.
Concept to visualize: Excelで手動でやっていた処理のほとんどはGASの6パターンで代替できる.
Suggested visual pattern: replacement-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: Excelで手動でやっていた処理のほとんどはGASの6パターンで代替できる
**内容ブロック①：手作業とGASの置き換え対応表**
| Excel手作業 | GASの処理 | 使うメソッド |
|---|---|---|
| フィルタで絞り込んでコピー | 条件でループして配列に追加→setValues | `filter` / `push` / `setValues` |
| 別シートに貼り付け | 出力シートにまとめて書き込み | `getSheetByName` / `setValues` |
| COUNTIF で集計 | Mapオブジェクトで件数を集計 | `Map` / `forEach` |
| VLOOKUP でマスター参照 | Mapに変換してlookup | `new Map(values.map(...))` |
| 重複チェック（Ctrl+F） | Setで重複を検出 | `Set` |
| 手書きで更新する列を入力 | 対象行の特定列に値をセット | `sheet.getRange().setValue()` |
**内容ブロック②：業種別の「置き換え候補」リスト**
| 業種 | 置き換えたい手作業 | GASパターン |
|---|---|---|
| 製造・卸売り | 月末の未出荷集計 | 抽出＋集計 |
| 士業 | 担当者別・進捗別件数の集計 | Map集計 |
| サービス業 | 週次の未対応件数確認 | 抽出＋ログ |
| 建設業 | 現場ごとの日報集計 | 担当者別Map集計 |
Visible text candidates to use when useful, without reducing the source density: 手作業とGASの置き換え対応表 / 業種別の「置き換え候補」リスト / Excel手作業 / GASの処理 / フィルタで絞り込んでコピー / 条件でループして配列に追加→setValues / 別シートに貼り付け / 出力シートにまとめて書き込み / COUNTIF で集計 / Mapオブジェクトで件数を集計 / VLOOKUP でマスター参照 / Mapに変換してlookup / 重複チェック / Setで重複を検出 / 手書きで更新する列を入力 / 対象行の特定列に値をセット / 置き換えたい手作業 / 製造・卸売り / 月末の未出荷集計 / 士業.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S29 対象行を抽出する ─ 実務4パターン

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 条件の組み合わせを変えるだけで「未請求抽出」「未対応抽出」「期限超過」「担当者別」に応用できる
- 推奨図解パターン: condition-card-set（4パターン）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S29 / Excel的処理の自動化.
Slide topic for context: 対象行を抽出する ─ 実務4パターン.
Concept to visualize: 条件の組み合わせを変えるだけで「未請求抽出」「未対応抽出」「期限超過」「担当者別」に応用できる.
Suggested visual pattern: condition-card-set（4パターン）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 条件の組み合わせを変えるだけで「未請求抽出」「未対応抽出」「期限超過」「担当者別」に応用できる
**内容ブロック①：パターン1 ─ ステータスで絞る（最頻出）**
コード例:
const result = rows.filter(row =>
  row[colMap['ステータス']].toString().trim() === '未対応'
);
コード例:
**内容ブロック②：パターン2 ─ 期限超過で絞る**
コード例:
const today = new Date();
today.setHours(0, 0, 0, 0);  // 時刻を0時に揃える
const result = rows.filter(row => {
  const deadline = new Date(row[colMap['期限日']]);
  return !isNaN(deadline) && deadline < today
    && row[colMap['ステータス']] !== '完了';
});
コード例:
**内容ブロック③：パターン3 ─ 担当者別で絞る**
コード例:
const targetPerson = '部門A担当';  // ← 設定ブロックに出しておく
const result = rows.filter(row =>
  row[colMap['担当者']].toString().trim() === targetPerson
);
コード例:
**内容ブロック④：パターン4 ─ 複数条件（AND）**
コード例:
const result = rows.filter(row => {
  const status   = row[colMap['ステータス']].toString().trim();
  const deadline = new Date(row[colMap['期限日']]);
  return status === '未対応' && deadline < today;
});
コード例:
Visible text candidates to use when useful, without reducing the source density: パターン1 ─ ステータスで絞る / パターン2 ─ 期限超過で絞る / パターン3 ─ 担当者別で絞る / パターン4 ─ 複数条件 / 未請求抽出 / 未対応抽出 / 期限超過 / 担当者別.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S30 別シートへ転記する

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 抽出結果を専用の出力シートへ書き出すことで、元データを汚さずに「今日の未対応一覧」が自動で完成する
- 推奨図解パターン: before-after-transformation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S30 / Excel的処理の自動化.
Slide topic for context: 別シートへ転記する.
Concept to visualize: 抽出結果を専用の出力シートへ書き出すことで、元データを汚さずに「今日の未対応一覧」が自動で完成する.
Suggested visual pattern: before-after-transformation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 抽出結果を専用の出力シートへ書き出すことで、元データを汚さずに「今日の未対応一覧」が自動で完成する
**内容ブロック①：転記の完全コード**
コード例:
function extractAndTransfer() {
  const ss          = SpreadsheetApp.getActive();
  const inputSheet  = ss.getSheetByName('問い合わせ台帳');
  const outputSheet = ss.getSheetByName('未対応一覧');
  if (!inputSheet || !outputSheet) {
    Logger.log('シートが見つかりません'); return;
  }
  const values  = inputSheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  const colMap  = createHeaderMap(headers);
  // 出力シートをヘッダー行以外クリア
  const lastRow = outputSheet.getLastRow();
  if (lastRow > 1) outputSheet.getRange(2, 1, lastRow - 1, headers.length).clearContent();
  // 抽出
  const result = rows.filter(row =>
    row[colMap['ステータス']].toString().trim() === '未対応'
  );
  // 書き込み
  if (result.length > 0) {
    outputSheet.getRange(2, 1, result.length, headers.length)
               .setValues(result);
  }
  Logger.log(`転記完了: ${result.length}件`);
}
コード例:
**内容ブロック②：Before/After**
| 比較軸 | Before（手作業） | After（GAS） |
|---|---|---|
| 操作 | フィルタ→コピー→別シートに貼り付け | スクリプト実行1回 |
| 時間 | 5〜15分（件数による） | 数秒 |
| ミス | コピー漏れ・貼り付け先間違い | ゼロ（出力先固定） |
| 頻度 | 月1〜週1 | 毎日でも問題なし |
Visible text candidates to use when useful, without reducing the source density: 転記の完全コード / Before/After / 比較軸 / Before / 操作 / フィルタ→コピー→別シートに貼り付け / 時間 / 5〜15分 / ミス / コピー漏れ・貼り付け先間違い / 頻度 / 月1〜週1 / 今日の未対応一覧.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S31 集計表を作る ─ Map を使った件数集計

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: `Map` オブジェクトを使うと「担当者別・カテゴリ別・ステータス別の件数」をコードで自動集計できる
- 推奨図解パターン: data-aggregation-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S31 / Excel的処理の自動化.
Slide topic for context: 集計表を作る ─ Map を使った件数集計.
Concept to visualize: `Map` オブジェクトを使うと「担当者別・カテゴリ別・ステータス別の件数」をコードで自動集計できる.
Suggested visual pattern: data-aggregation-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `Map` オブジェクトを使うと「担当者別・カテゴリ別・ステータス別の件数」をコードで自動集計できる
**内容ブロック①：Map集計の基本パターン**
コード例:
function countByAssignee() {
  const ss    = SpreadsheetApp.getActive();
  const sheet = ss.getSheetByName('問い合わせ台帳');
  const values = sheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  const colMap  = createHeaderMap(headers);
  const countMap = new Map();
  for (const row of rows) {
    const person = row[colMap['担当者']].toString().trim() || '（未設定）';
    countMap.set(person, (countMap.get(person) || 0) + 1);
  }
  // ログに出力
  for (const [person, count] of countMap) {
    Logger.log(`${person}: ${count}件`);
  }
}
コード例:
**内容ブロック②：集計の応用パターン**
| 集計軸 | コード例（一部） | 出力例 |
|---|---|---|
| 担当者別 | `colMap['担当者']` をキー | 部門A: 12件 |
| ステータス別 | `colMap['ステータス']` をキー | 未対応: 8件 |
| カテゴリ別 | `colMap['カテゴリ']` をキー | 請求関連: 5件 |
| 月別 | `new Date().getMonth()` をキー | 5月: 30件 |
**内容ブロック③：集計結果をシートに書き出す**
コード例:
const summarySheet = ss.getSheetByName('集計');
summarySheet.clearContents();
summarySheet.appendRow(['担当者', '件数']);
for (const [person, count] of countMap) {
  summarySheet.appendRow([person, count]);
}
コード例:
Visible text candidates to use when useful, without reducing the source density: Map集計の基本パターン / 集計の応用パターン / 集計結果をシートに書き出す / 集計軸 / コード例 / 担当者別 / colMap['担当者'] をキー / ステータス別 / colMap['ステータス'] をキー / カテゴリ別 / colMap['カテゴリ'] をキー / 月別 / new Date.getMonth をキー.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S32 重複チェック ─ Set で重複を検出する

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: `Set` を使うと伝票番号・受付番号・メールアドレスの重複を瞬時に検出でき、目視確認を自動化できる
- 推奨図解パターン: duplicate-check-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S32 / Excel的処理の自動化.
Slide topic for context: 重複チェック ─ Set で重複を検出する.
Concept to visualize: `Set` を使うと伝票番号・受付番号・メールアドレスの重複を瞬時に検出でき、目視確認を自動化できる.
Suggested visual pattern: duplicate-check-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `Set` を使うと伝票番号・受付番号・メールアドレスの重複を瞬時に検出でき、目視確認を自動化できる
**内容ブロック①：重複チェックの基本**
コード例:
function checkDuplicates() {
  const sheet  = SpreadsheetApp.getActive().getSheetByName('売上CSV');
  const values = sheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  const colMap  = createHeaderMap(headers);
  const seen       = new Set();
  const duplicates = [];
  for (const row of rows) {
    const key = row[colMap['受付番号']].toString().trim();
    if (seen.has(key)) {
      duplicates.push({ key, row });
      Logger.log(`重複: ${key}`);
    } else {
      seen.add(key);
    }
  }
  Logger.log(`重複: ${duplicates.length}件 / 全体: ${rows.length}件`);
}
コード例:
**内容ブロック②：業種別の重複チェック対象**
| 業種 | チェック対象の列 | 重複時の対処 |
|---|---|---|
| 製造・卸売り | 注文番号・伝票番号 | 要確認フラグを立てる |
| 士業 | 申請番号・受付番号 | 担当者へ通知（発展） |
| サービス業 | 顧客メール・受付ID | 重複行を別シートへ転記 |
| 建設業 | 現場コード＋日付 | 重複は上書き候補として抽出 |
**内容ブロック③：複合キーで重複チェックする**
コード例:
// 「担当者 + 日付」の組み合わせで重複チェック
const key = `${row[colMap['担当者']]}_${row[colMap['日付']]}`;
コード例:
Visible text candidates to use when useful, without reducing the source density: 重複チェックの基本 / 業種別の重複チェック対象 / 複合キーで重複チェックする / チェック対象の列 / 製造・卸売り / 注文番号・伝票番号 / 士業 / 申請番号・受付番号 / サービス業 / 顧客メール・受付ID / 建設業 / 現場コード＋日付 / 担当者 + 日付.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S33 ステータスを更新する ─ 元シートへ書き戻す

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 転記が完了した行のステータスを自動で「転記済み」に変えることで、二重処理と見落としを防げる
- 推奨図解パターン: update-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S33 / Excel的処理の自動化.
Slide topic for context: ステータスを更新する ─ 元シートへ書き戻す.
Concept to visualize: 転記が完了した行のステータスを自動で「転記済み」に変えることで、二重処理と見落としを防げる.
Suggested visual pattern: update-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 転記が完了した行のステータスを自動で「転記済み」に変えることで、二重処理と見落としを防げる
**内容ブロック①：ステータス更新の基本パターン**
コード例:
function updateStatus() {
  const sheet  = SpreadsheetApp.getActive().getSheetByName('問い合わせ台帳');
  const values = sheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  const colMap  = createHeaderMap(headers);
  for (let i = 0; i < rows.length; i++) {
    const row    = rows[i];
    const status = row[colMap['ステータス']].toString().trim();
    if (status === '未対応') {
      // シートの行番号はデータ行インデックス+2（1始まり+ヘッダー分）
      const sheetRow = i + 2;
      const statusColIndex = colMap['ステータス'] + 1;  // getRange は1始まり
      sheet.getRange(sheetRow, statusColIndex).setValue('処理中');
    }
  }
  Logger.log('ステータス更新完了');
}
コード例:
**内容ブロック②：更新の注意点**
| 注意点 | 詳細 |
|---|---|
| 行番号はデータインデックス+2 | ヘッダー行（1行目）＋配列0始まりの補正 |
| 列番号はcolMap値+1 | getRange は1始まりのため |
| 更新前にバックアップ | 元シートを直接変更するため、実行前にコピーを取る |
| ループ内でgetRange呼び出し | 件数が多い場合は処理が遅くなる（発展: batchUpdate） |
Visible text candidates to use when useful, without reducing the source density: ステータス更新の基本パターン / 更新の注意点 / 注意点 / 詳細 / 行番号はデータインデックス+2 / ヘッダー行＋配列0始まりの補正 / 列番号はcolMap値+1 / getRange は1始まりのため / 更新前にバックアップ / 元シートを直接変更するため、実行前にコピーを取る / ループ内でgetRange呼び出し / 件数が多い場合は処理が遅くなる（発展 / 転記済み.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S34 ログを残す ─ `appendRow()` でログシートに記録

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 処理のたびにログシートへ実行日時・件数・エラー数を記録することで、「いつ・何件処理されたか」が後から確認できる
- 推奨図解パターン: log-table-design
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S34 / Excel的処理の自動化.
Slide topic for context: ログを残す ─ `appendRow()` でログシートに記録.
Concept to visualize: 処理のたびにログシートへ実行日時・件数・エラー数を記録することで、「いつ・何件処理されたか」が後から確認できる.
Suggested visual pattern: log-table-design.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 処理のたびにログシートへ実行日時・件数・エラー数を記録することで、「いつ・何件処理されたか」が後から確認できる
**内容ブロック①：ログ追記の基本**
コード例:
function appendLog(logSheet, processName, count, errorCount, note) {
  logSheet.appendRow([
    new Date(),       // 実行日時
    processName,      // 処理名（例: '未対応抽出'）
    count,            // 処理件数
    errorCount,       // エラー件数
    note || ''        // 備考（任意）
  ]);
}
// 呼び出し例
appendLog(logSheet, '未対応抽出', result.length, 0, '正常終了');
コード例:
**内容ブロック②：ログシートの列設計（推奨）**
| 列名 | 内容 | 例 |
|---|---|---|
| 実行日時 | `new Date()` | 2026/05/10 9:32:10 |
| 処理名 | 処理の名称 | 未対応抽出 |
| 処理件数 | 対象行数 | 12 |
| エラー件数 | try/catchで捕捉した数 | 0 |
| 備考 | エラー内容や特記事項 | 担当者空欄3件 |
**内容ブロック③：ログ活用のメリット**
- 「先週と今週で件数が大きく違う」変化に気づける
- エラーが続いていることを担当者が確認できる
- GASがいつ動いたかを後から証明できる
Visible text candidates to use when useful, without reducing the source density: ログ追記の基本 / ログシートの列設計 / ログ活用のメリット / 実行日時 / new Date / 処理名 / 処理の名称 / 処理件数 / 対象行数 / エラー件数 / try/catchで捕捉した数 / 備考 / エラー内容や特記事項 / 「先週と今週で件数が大きく違う」変化に気づける / エラーが続いていることを担当者が確認できる / GASがいつ動いたかを後から証明できる / いつ・何件処理されたか / 先週と今週で件数が大きく違う.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S35 失敗しても戻れる設計 ─ 原本保護と出力先の分離

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 元データを直接変更せず、出力先・ログシートを分けた設計にすることで、スクリプトが誤動作しても元に戻せる
- 推奨図解パターン: recovery-flow
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S35 / Excel的処理の自動化.
Slide topic for context: 失敗しても戻れる設計 ─ 原本保護と出力先の分離.
Concept to visualize: 元データを直接変更せず、出力先・ログシートを分けた設計にすることで、スクリプトが誤動作しても元に戻せる.
Suggested visual pattern: recovery-flow.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 元データを直接変更せず、出力先・ログシートを分けた設計にすることで、スクリプトが誤動作しても元に戻せる
**内容ブロック①：安全設計の3原則**
| 原則 | 具体的な対策 |
|---|---|
| 原本を直接変更しない | 回答シートはGASで変更しない。出力用シートを別途用意する |
| 出力先をクリアしてから書く | 毎回の実行前に出力シートのデータ行だけをclearContent |
| ログを必ず残す | 実行日時・件数・エラーをログシートに記録する |
**内容ブロック②：出力シートの準備パターン**
コード例:
// 実行のたびに出力シートをリセット（ヘッダーは残す）
function clearOutputSheet(sheet) {
  const lastRow = sheet.getLastRow();
  if (lastRow > 1) {
    sheet.getRange(2, 1, lastRow - 1, sheet.getLastColumn())
         .clearContent();
  }
}
コード例:
**内容ブロック③：復旧フローの設計**
コード例:
問題発生
  ↓
1. 実行ログを確認（Logger.log）
2. 出力シートの内容を確認
3. 元データ（回答シート）は変更されていないか確認
4. 問題箇所を特定して修正
5. 再実行（出力シートはリセット後）
コード例:
Visible text candidates to use when useful, without reducing the source density: 安全設計の3原則 / 出力シートの準備パターン / 復旧フローの設計 / 具体的な対策 / 原本を直接変更しない / 回答シートはGASで変更しない / 出力先をクリアしてから書く / 毎回の実行前に出力シートのデータ行だけをclearContent / ログを必ず残す / 実行日時・件数・エラーをログシートに記録する.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S36 画面共有：未請求抽出・転記・ログ追記を実行する

- セクション: Excel的処理の自動化
- スライド側ヘッドライン（画像内には原則入れない）: 実際のコードを動かして「読む→条件で絞る→書く→ログ」の3ステップが一気通貫で動くことを確認する
- 推奨図解パターン: screen-share-guide
- 参照素材・スクリーンショット: `スクリーンショット/gas-execution-log-transfer.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S36 / Excel的処理の自動化.
Slide topic for context: 画面共有：未請求抽出・転記・ログ追記を実行する.
Concept to visualize: 実際のコードを動かして「読む→条件で絞る→書く→ログ」の3ステップが一気通貫で動くことを確認する.
Suggested visual pattern: screen-share-guide.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 実際のコードを動かして「読む→条件で絞る→書く→ログ」の3ステップが一気通貫で動くことを確認する
**内容ブロック①：画面共有の手順**
1. Apps Scriptエディタを開く
2. `extractAndTransfer` 関数を実行
3. 実行ログで「転記完了: N件」を確認
4. 出力シート（未対応一覧）へ切り替えて転記されたデータを確認
5. ログシートへ切り替えて記録を確認
**内容ブロック②：使用するダミーデータ**
- 入力シート: `CSV取込_売上明細_5月1週.csv`（架空データ50行）
- 出力シート: `未請求一覧`（実行前にクリア）
- ログシート: `処理ログ`（実行のたびに1行追加）
**内容ブロック③：受講者が確認するポイント**
- 未請求フラグの行だけが出力シートに転記されているか
- ヘッダー行は出力シートの1行目に正しく入っているか
- ログシートに「実行日時・転記件数」が追記されているか
- 元シートのデータは変更されていないか
  （取得先: https://developers.google.com/apps-script/guides/logging）
Visible text candidates to use when useful, without reducing the source density: 画面共有の手順 / 使用するダミーデータ / 受講者が確認するポイント / 入力シート / 出力シート / ログシート / 未請求フラグの行だけが出力シートに転記されているか / ヘッダー行は出力シートの1行目に正しく入っているか / ログシートに「実行日時・転記件数」が追記されているか / 元シートのデータは変更されていないか / 読む→条件で絞る→書く→ログ / 転記完了 / 実行日時・転記件数.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S37 章見出し/現在位置 5/6: カスタムメニューとエラー処理

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: カスタムメニューとエラー処理では、現場担当者が押せる実行方法と、止まった時の見方を設計することで、カスタムメニュー・テスト観点につなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S37 / カスタムメニューとエラー処理.
Slide topic for context: 章見出し/現在位置 5/6: カスタムメニューとエラー処理.
Concept to visualize: カスタムメニューとエラー処理では、現場担当者が押せる実行方法と、止まった時の見方を設計することで、カスタムメニュー・テスト観点につなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 5/6 カスタムメニューとエラー処理
現在章の範囲: S37-S43
時間目安: 20分
前の章: Excel的処理の自動化
次の章: 演習とレビュー
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: カスタムメニューとエラー処理では、現場担当者が押せる実行方法と、止まった時の見方を設計することで、カスタムメニュー・テスト観点につなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 5/6 / カスタムメニューとエラー処理 / S37-S43 / 20分 / 前の章 / Excel的処理の自動化 / 次の章 / 演習とレビュー.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S38 現場担当者が押せるカスタムメニュー

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: カスタムメニューを使うと、Apps Scriptエディタを知らない担当者でもスプレッドシートのメニューからGASを実行できる
- 推奨図解パターン: ui-menu-mockup
- 参照素材・スクリーンショット: `スクリーンショット/gas-custom-menu.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S38 / カスタムメニューとエラー処理.
Slide topic for context: 現場担当者が押せるカスタムメニュー.
Concept to visualize: カスタムメニューを使うと、Apps Scriptエディタを知らない担当者でもスプレッドシートのメニューからGASを実行できる.
Suggested visual pattern: ui-menu-mockup.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: カスタムメニューを使うと、Apps Scriptエディタを知らない担当者でもスプレッドシートのメニューからGASを実行できる
**内容ブロック①：カスタムメニューの基本**
コード例:
function onOpen() {
  SpreadsheetApp.getUi()
    .createMenu('業務メニュー')         // メニュー名
    .addItem('未対応を抽出', 'extractAndTransfer')   // 表示名、関数名
    .addItem('重複をチェック', 'checkDuplicates')
    .addSeparator()                       // 区切り線
    .addItem('処理ログを確認', 'openLogSheet')
    .addToUi();
}
コード例:
**内容ブロック②：カスタムメニューのメリット**
| 比較軸 | Apps Scriptエディタ | カスタムメニュー |
|---|---|---|
| 実行できる人 | スクリプト編集者のみ | スプレッドシート閲覧者も可 |
| 操作の難易度 | 関数名を知っている必要あり | メニューをクリックするだけ |
| 誤実行リスク | 関数を間違えて実行する可能性 | メニュー名で意図が明確 |
| 現場への展開 | ハードルが高い | 現場担当者が使いやすい |
**内容ブロック③：業種別のカスタムメニュー例**
| 業種 | メニュー名 | 含める処理 |
|---|---|---|
| 製造・卸売り | 出荷管理 | 未出荷抽出・件数集計・ログ |
| 士業 | 進捗管理 | 未対応抽出・担当者別集計 |
| サービス業 | 問い合わせ管理 | 未対応抽出・重複チェック |
| 建設業 | 日報管理 | 未確認抽出・現場別集計 |
  （取得先: https://developers.google.com/workspace/add-ons/how-tos/building-editor-addons）
Visible text candidates to use when useful, without reducing the source density: カスタムメニューの基本 / カスタムメニューのメリット / 業種別のカスタムメニュー例 / 比較軸 / Apps Scriptエディタ / 実行できる人 / スクリプト編集者のみ / 操作の難易度 / 関数名を知っている必要あり / 誤実行リスク / 関数を間違えて実行する可能性 / 現場への展開 / ハードルが高い / メニュー名 / 製造・卸売り / 出荷管理 / 士業 / 進捗管理 / サービス業 / 問い合わせ管理.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S39 手動実行と自動実行を分ける

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: 最初は手動実行で動作確認し、安定してから時間起動トリガーへ移行することで、誤動作のリスクを段階的に下げられる
- 推奨図解パターン: comparison-table（手動/自動）
- 参照素材・スクリーンショット: `スクリーンショット/gas-trigger-settings.png`

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S39 / カスタムメニューとエラー処理.
Slide topic for context: 手動実行と自動実行を分ける.
Concept to visualize: 最初は手動実行で動作確認し、安定してから時間起動トリガーへ移行することで、誤動作のリスクを段階的に下げられる.
Suggested visual pattern: comparison-table（手動/自動）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 最初は手動実行で動作確認し、安定してから時間起動トリガーへ移行することで、誤動作のリスクを段階的に下げられる
**内容ブロック①：実行方法の比較**
| 実行方法 | 設定 | 向いている場面 | 注意 |
|---|---|---|---|
| 手動実行（▶ボタン） | なし | 開発中・確認時 | 担当者が手で押す |
| カスタムメニュー | onOpen関数 | 現場担当者に展開 | スプレッドシートを開いた時に設定 |
| 時間起動トリガー | トリガー設定 | 毎朝定時実行 | 設定者のアカウントで動く |
| フォーム送信時トリガー | トリガー設定 | フォーム回答直後 | フォームと紐付ける |
**内容ブロック②：トリガー設定の手順**
1. Apps Scriptエディタ左サイドバーの「トリガー」（時計アイコン）をクリック
2. 右下の「+ トリガーを追加」をクリック
3. 実行する関数・イベントのソース・時刻を設定
4. 保存（Googleアカウントで承認が必要）
**内容ブロック③：トリガー設計の3原則**
- 承認が必要なアクション（メール送信）はトリガー自動実行させない
- ログが残っている処理のみトリガー化する
- トリガー設定者が退職した場合の引き継ぎ方法を事前に決めておく
  （取得先: https://developers.google.com/apps-script/guides/triggers/installable）
Visible text candidates to use when useful, without reducing the source density: 実行方法の比較 / トリガー設定の手順 / トリガー設計の3原則 / 実行方法 / 設定 / 手動実行 / カスタムメニュー / onOpen関数 / 時間起動トリガー / トリガー設定 / フォーム送信時トリガー / 承認が必要なアクションはトリガー自動実行させない / ログが残っている処理のみトリガー化する / トリガー設定者が退職した場合の引き継ぎ方法を事前に決めておく / トリガー / + トリガーを追加.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S40 エラーは隠さず見える化する ─ try/catch

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: `try/catch` でエラーをキャッチしてログに記録することで、「なぜ止まったか」を誰でも確認できる状態を作れる
- 推奨図解パターン: error-flow（exception-path）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S40 / カスタムメニューとエラー処理.
Slide topic for context: エラーは隠さず見える化する ─ try/catch.
Concept to visualize: `try/catch` でエラーをキャッチしてログに記録することで、「なぜ止まったか」を誰でも確認できる状態を作れる.
Suggested visual pattern: error-flow（exception-path）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: `try/catch` でエラーをキャッチしてログに記録することで、「なぜ止まったか」を誰でも確認できる状態を作れる
**内容ブロック①：try/catch の基本パターン**
コード例:
function extractAndTransfer() {
  try {
    // ── ここにメイン処理を書く ──
    const sheet = SpreadsheetApp.getActive().getSheetByName('問い合わせ台帳');
    if (!sheet) throw new Error('シートが見つかりません: 問い合わせ台帳');
    // ...抽出・転記処理...
    appendLog(logSheet, '未対応抽出', result.length, 0, '正常終了');
  } catch (e) {
    // ── エラーが起きたらここに来る ──
    Logger.log('エラー発生: ' + e.message);
    appendLog(logSheet, '未対応抽出', 0, 1, 'エラー: ' + e.message);
  }
}
コード例:
**内容ブロック②：エラーを「見える化」する意味**
- エラーが起きても処理が静かに止まるだけだと、誰も気づかない
- ログシートにエラー内容を残すことで、担当者が翌朝確認できる
- エラー内容をGAS/Geminiで読み解く材料にもなる
**内容ブロック③：よくあるエラーと原因**
| エラーメッセージ | 原因 | 対処 |
|---|---|---|
| `Cannot read properties of null` | シートが見つからない | `if (!sheet)` でチェック |
| `Exception: Service invoked too many times` | 実行回数制限超え | 件数を減らすか分割する |
| `Execution timeout` | 実行時間6分超え | 処理を分割して段階実行 |
| `TypeError: is not a function` | スペルミス・スコープ外 | 関数名・定義を確認する |
Visible text candidates to use when useful, without reducing the source density: try/catch の基本パターン / エラーを「見える化」する意味 / よくあるエラーと原因 / エラーが起きても処理が静かに止まるだけだと、誰も気づかない / ログシートにエラー内容を残すことで、担当者が翌朝確認できる / エラー内容をGAS/Geminiで読み解く材料にもなる / エラーメッセージ / 原因 / Cannot read properties of null / シートが見つからない / Exception / 実行回数制限超え / Execution timeout / 実行時間6分超え / TypeError / スペルミス・スコープ外 / なぜ止まったか / 見える化.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S41 入力チェック ─ 処理前に台帳の状態を確認する

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: 処理の前に「必須列の存在」「空欄」「想定外の値」をチェックすることで、途中で止まらない安定したスクリプトになる
- 推奨図解パターン: checklist-confirmation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S41 / カスタムメニューとエラー処理.
Slide topic for context: 入力チェック ─ 処理前に台帳の状態を確認する.
Concept to visualize: 処理の前に「必須列の存在」「空欄」「想定外の値」をチェックすることで、途中で止まらない安定したスクリプトになる.
Suggested visual pattern: checklist-confirmation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 処理の前に「必須列の存在」「空欄」「想定外の値」をチェックすることで、途中で止まらない安定したスクリプトになる
**内容ブロック①：必須ヘッダーの存在確認**
コード例:
function validateHeaders(headers, requiredColumns) {
  const missing = requiredColumns.filter(col => !headers.includes(col));
  if (missing.length > 0) {
    throw new Error(`ヘッダーが見つかりません: ${missing.join(', ')}`);
  }
}
// 使い方
validateHeaders(headers, ['受付日', 'ステータス', '担当者', '請求ステータス']);
コード例:
**内容ブロック②：行単位のチェックパターン**
コード例:
for (let i = 0; i < rows.length; i++) {
  const row = rows[i];
  const sheetRow = i + 2;
  // 必須列が空の行はスキップしてログに記録
  if (isEmpty(row[colMap['受付日']])) {
    Logger.log(`行${sheetRow}: 受付日が空欄 → スキップ`);
    skipCount++;
    continue;
  }
  // 想定外のステータス値はログに記録
  const validStatuses = ['未対応', '処理中', '完了', '要確認'];
  const status = row[colMap['ステータス']].toString().trim();
  if (!validStatuses.includes(status)) {
    Logger.log(`行${sheetRow}: 想定外ステータス「${status}」`);
  }
}
コード例:
**内容ブロック③：チェックリスト（演習前に確認）**
- [ ] 必須列のヘッダー名がスクリプトと一致しているか
- [ ] ステータス列の値が想定どおりか（スペース・全角半角）
- [ ] 日付列は日付形式か（文字列「5/1」になっていないか）
- [ ] 担当者列に空欄行が想定以上にないか
Visible text candidates to use when useful, without reducing the source density: 必須ヘッダーの存在確認 / 行単位のチェックパターン / チェックリスト / [ ] 必須列のヘッダー名がスクリプトと一致しているか / [ ] ステータス列の値が想定どおりか / [ ] 日付列は日付形式か / [ ] 担当者列に空欄行が想定以上にないか / 必須列の存在 / 空欄 / 想定外の値 / ${status} / 5/1.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S42 対象行なし・重複ありの扱い ─ テストケース設計

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: 対象行がゼロのケースと重複があるケースを事前に想定してコードを書くことで、本番データでの予期しない動作を防げる
- 推奨図解パターン: test-case-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S42 / カスタムメニューとエラー処理.
Slide topic for context: 対象行なし・重複ありの扱い ─ テストケース設計.
Concept to visualize: 対象行がゼロのケースと重複があるケースを事前に想定してコードを書くことで、本番データでの予期しない動作を防げる.
Suggested visual pattern: test-case-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 対象行がゼロのケースと重複があるケースを事前に想定してコードを書くことで、本番データでの予期しない動作を防げる
**内容ブロック①：テストケース一覧**
| テストケース | 期待する動作 | 確認するポイント |
|---|---|---|
| 対象行あり（通常） | 抽出・転記・ログ追記 | 件数が正しいか |
| 対象行なし | 「0件」ログ、正常終了 | エラーにならないか |
| 担当者列に空欄あり | スキップしてログに記録 | 空欄行のシート行番号がログに出るか |
| 重複あり | 重複件数をログに記録 | どのキーが重複したかわかるか |
| ヘッダー名が違う | エラーメッセージでログに出る | 「ヘッダーが見つかりません」が出るか |
| 出力シートがない | エラーメッセージで終了 | 「シートが見つかりません」が出るか |
**内容ブロック②：0件対応のコード**
コード例:
if (result.length === 0) {
  Logger.log('対象行なし: 正常終了');
  appendLog(logSheet, '未対応抽出', 0, 0, '対象行なし');
  return;  // エラーではなく正常終了
}
コード例:
**内容ブロック③：自己レビューチェックポイント**
- 0件でもエラーにならず正常終了するか
- 空欄行が混在していてもスキップされるか
- 重複があった場合にログに記録されるか
Visible text candidates to use when useful, without reducing the source density: テストケース一覧 / 0件対応のコード / 自己レビューチェックポイント / テストケース / 期待する動作 / 対象行あり / 抽出・転記・ログ追記 / 対象行なし / 「0件」ログ、正常終了 / 担当者列に空欄あり / スキップしてログに記録 / 重複あり / 重複件数をログに記録 / ヘッダー名が違う / エラーメッセージでログに出る / 出力シートがない / エラーメッセージで終了 / 0件でもエラーにならず正常終了するか / 空欄行が混在していてもスキップされるか / 重複があった場合にログに記録されるか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S43 制限と通知 ─ GASの上限を知っておく

- セクション: カスタムメニューとエラー処理
- スライド側ヘッドライン（画像内には原則入れない）: GASには実行時間・サービス呼び出し回数などの上限があることを知っておくことで、本番運用で突然止まる事態を防げる
- 推奨図解パターン: caution-card-set
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S43 / カスタムメニューとエラー処理.
Slide topic for context: 制限と通知 ─ GASの上限を知っておく.
Concept to visualize: GASには実行時間・サービス呼び出し回数などの上限があることを知っておくことで、本番運用で突然止まる事態を防げる.
Suggested visual pattern: caution-card-set.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: GASには実行時間・サービス呼び出し回数などの上限があることを知っておくことで、本番運用で突然止まる事態を防げる
**内容ブロック①：主な制限（概要）**
| 制限の種類 | 概要 | 対処方法 |
|---|---|---|
| スクリプトの実行時間 | 1回の実行に上限あり（数分程度） | 処理を分割する・件数を減らす |
| メール送信数 | 1日あたりの送信数に上限あり | まとめて1通に集約する |
| URL Fetch呼び出し回数 | 外部APIへのアクセスに上限あり | キャッシュを使う（発展） |
| スプレッドシートの読み書き | 大量のgetRange呼び出しは遅くなる | まとめてgetValues/setValues |
> 注: 制限の具体的な数値は変更されることがあります。最新の公式情報を確認してください。
> 参照: https://developers.google.com/apps-script/guides/services/quotas
**内容ブロック②：メール通知の設計（発展・任意）**
コード例:
// エラーが起きた時だけメールを送る（件数が多い場合は1通に集約）
if (errorCount > 0) {
  GmailApp.sendEmail(
    'test-dummy@example.com',  // ← 本番では社内承認済みの通知先に変える
    'GAS処理エラー通知',
    `エラー: ${errorCount}件発生しました。ログシートを確認してください。`
  );
}
コード例:
**内容ブロック③：本番運用前のチェックリスト**
- [ ] テストデータで動作確認済みか
- [ ] 実行時間が制限に収まっているか（実行ログで確認）
- [ ] エラー通知の宛先が正しいか
- [ ] トリガー設定者のアカウントが正しいか
- [ ] ログシートが溜まり続ける設計になっていないか（定期クリーンアップの検討）
Visible text candidates to use when useful, without reducing the source density: 主な制限 / メール通知の設計 / 本番運用前のチェックリスト / 制限の種類 / 概要 / スクリプトの実行時間 / 1回の実行に上限あり / メール送信数 / 1日あたりの送信数に上限あり / URL Fetch呼び出し回数 / 外部APIへのアクセスに上限あり / スプレッドシートの読み書き / 大量のgetRange呼び出しは遅くなる / [ ] テストデータで動作確認済みか / [ ] 実行時間が制限に収まっているか / [ ] エラー通知の宛先が正しいか / [ ] トリガー設定者のアカウントが正しいか / [ ] ログシートが溜まり続ける設計になっていないか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S44 章見出し/現在位置 6/6: 演習とレビュー

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: 演習とレビューでは、サンプルコードを自分の台帳条件へ読み替え、動かす前にテスト観点を確認することで、業務適用版の読み替えメモにつなげる
- 推奨図解パターン: roadmap-timeline（章見出し/現在位置）
- 参照素材・スクリーンショット: なし

```text
Create one simple agenda-transition reference image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S44 / 演習とレビュー.
Slide topic for context: 章見出し/現在位置 6/6: 演習とレビュー.
Concept to visualize: 演習とレビューでは、サンプルコードを自分の台帳条件へ読み替え、動かす前にテスト観点を確認することで、業務適用版の読み替えメモにつなげる.
Suggested visual pattern: roadmap-timeline（章見出し/現在位置）.
Agenda transition source context. Use only the current, previous, next, progress, and time labels needed to make location obvious at a glance:
このスライドは目次切り替え専用。通常解説スライドより情報量を減らす。
現在章: 6/6 演習とレビュー
現在章の範囲: S44-S50
時間目安: 25分
前の章: カスタムメニューとエラー処理
次の章: まとめ・実務への接続
表示優先度: 現在章を最も大きく、次の章を次に大きく、前の章は小さく薄いチップで表示する。
表示要素は6〜8個程度まで。長い説明、比較表、チェックリスト、複数カード、業種別例は入れない。
上部ヘッドラインの意味: 演習とレビューでは、サンプルコードを自分の台帳条件へ読み替え、動かす前にテスト観点を確認することで、業務適用版の読み替えメモにつなげる
Visible text candidates to use, keeping the transition simple and not dense: 現在地 / 6/6 / 演習とレビュー / S44-S50 / 25分 / 前の章 / カスタムメニューとエラー処理 / 次の章 / まとめ・実務へ接続.
This is a chapter/agenda transition visual. The latest user instruction explicitly says to reduce information only at agenda-change timing.
Make the current agenda item the dominant visual: huge central Japanese text, with a clear progress mark such as 1/6 or 3/7.
Show the next agenda item clearly as the second-largest element, preferably on the right or lower-right with an arrow or '次は'.
Show the previous agenda item only as a small muted chip when available. Use a simple left-to-right transition feel.
Do not use detailed comparison tables, many content cards, checklists, industry examples, or dense learner-output blocks on this slide.
Use only about 6-8 readable text elements. The purpose is to make the section switch obvious at a glance.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated transition image should stay visually simple: big current agenda item, clear next agenda item, small previous chip, and a compact progress indicator.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, dense comparison tables, many content cards, industry-example lists, or long explanations. This is an agenda transition slide: keep it simple and make the current/next agenda items large and obvious.
```

## S45 演習：未対応行の抽出・転記・ログ追記

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: サンプルコードを業務条件（シート名・ヘッダー名・抽出条件）に読み替えて実行することで、「コードを動かす」から「コードを使いこなす」へ進める
- 推奨図解パターン: checklist-confirmation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S45 / 演習とレビュー.
Slide topic for context: 演習：未対応行の抽出・転記・ログ追記.
Concept to visualize: サンプルコードを業務条件（シート名・ヘッダー名・抽出条件）に読み替えて実行することで、「コードを動かす」から「コードを使いこなす」へ進める.
Suggested visual pattern: checklist-confirmation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: サンプルコードを業務条件（シート名・ヘッダー名・抽出条件）に読み替えて実行することで、「コードを動かす」から「コードを使いこなす」へ進める
**内容ブロック①：演習の手順**
1. スプレッドシートに `問い合わせ処理台帳_GAS演習.csv` を貼り付ける
2. 配布コード（`extractAndTransfer` 関数）を Apps Script エディタにコピーする
3. 設定ブロック（シート名・ヘッダー名・条件値）をワークシートに書いた値に書き換える
4. `checkRows` で読み取り確認をする
5. `extractAndTransfer` を実行して出力シートとログシートを確認する
**内容ブロック②：ワークシートの記入項目（対応）**
| コード内の設定 | 演習での値 | 自分の業務での値 |
|---|---|---|
| INPUT_SHEET | 問い合わせ台帳 | （記入） |
| OUTPUT_SHEET | 未対応一覧 | （記入） |
| STATUS_COL | ステータス | （記入） |
| TARGET_VAL | 未対応 | （記入） |
**内容ブロック③：成果物の確認ポイント**
- 出力シートに未対応行のみ転記されているか
- ヘッダー行が出力シートの1行目にあるか
- ログシートに「実行日時・処理名・件数」が追記されているか
- 元シート（台帳）のデータは変更されていないか
**内容ブロック④：自己レビュー（動画を一時停止して5分取り組んでください）**
- 対象行ゼロのテストケースで試したか
- 担当者列が空欄の行はスキップされるか
- 処理後、ログシートに何が記録されたか確認したか
Visible text candidates to use when useful, without reducing the source density: 演習の手順 / ワークシートの記入項目 / 成果物の確認ポイント / 自己レビュー / コード内の設定 / 演習での値 / INPUT_SHEET / 問い合わせ台帳 / OUTPUT_SHEET / 未対応一覧 / STATUS_COL / ステータス / TARGET_VAL / 未対応 / 出力シートに未対応行のみ転記されているか / ヘッダー行が出力シートの1行目にあるか / ログシートに「実行日時・処理名・件数」が追記されているか / 元シートのデータは変更されていないか / 対象行ゼロのテストケースで試したか / 担当者列が空欄の行はスキップされるか.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S46 サンプルコードの全体構造 ─ 設計の読み方

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: コードを「設定→入力→検証→処理→出力→ログ」の6ブロックに分けて読むと、どこを変えればよいかがすぐわかる
- 推奨図解パターン: code-structure-diagram（6ブロック色分け）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S46 / 演習とレビュー.
Slide topic for context: サンプルコードの全体構造 ─ 設計の読み方.
Concept to visualize: コードを「設定→入力→検証→処理→出力→ログ」の6ブロックに分けて読むと、どこを変えればよいかがすぐわかる.
Suggested visual pattern: code-structure-diagram（6ブロック色分け）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: コードを「設定→入力→検証→処理→出力→ログ」の6ブロックに分けて読むと、どこを変えればよいかがすぐわかる
**内容ブロック①：完全なサンプルコード（コメント付き）**
コード例:
// ─── 設定ブロック ─────────────────────────────────
const INPUT_SHEET  = '問い合わせ台帳';
const OUTPUT_SHEET = '未対応一覧';
const LOG_SHEET    = '処理ログ';
const STATUS_COL   = 'ステータス';
const TARGET_VAL   = '未対応';
// ────────────────────────────────────────────────
function extractAndTransfer() {
  const ss = SpreadsheetApp.getActive();
  // ─── 入力 ────────────────────────────────────────
  const inputSheet  = ss.getSheetByName(INPUT_SHEET);
  const outputSheet = ss.getSheetByName(OUTPUT_SHEET);
  const logSheet    = ss.getSheetByName(LOG_SHEET);
  // ─── 検証 ────────────────────────────────────────
  [inputSheet, outputSheet, logSheet].forEach((s, i) => {
    if (!s) throw new Error(`シートが見つかりません: インデックス${i}`);
  });
  const values  = inputSheet.getDataRange().getValues();
  const headers = values[0];
  const rows    = values.slice(1);
  const colMap  = createHeaderMap(headers);
  validateHeaders(headers, [STATUS_COL]);
  // ─── 処理 ────────────────────────────────────────
  const result = rows.filter(row =>
    row[colMap[STATUS_COL]].toString().trim() === TARGET_VAL
  );
  // ─── 出力 ────────────────────────────────────────
  clearOutputSheet(outputSheet);
  if (result.length > 0) {
    outputSheet.getRange(2, 1, result.length, headers.length)
               .setValues(result);
  }
  // ─── ログ ────────────────────────────────────────
  appendLog(logSheet, '未対応抽出', result.length, 0, '正常終了');
  Logger.log(`完了: ${result.length}件`);
}
コード例:
**内容ブロック②：ブロック別の「変えてよい場所・変えてはいけない場所」**
| ブロック | 変えてよい | 変えてはいけない |
|---|---|---|
| 設定ブロック | シート名・列名・条件値 | ─ |
| 入力 | シート名の定数 | getDataRange / getValues の構造 |
| 検証 | チェックする列名 | エラー処理の仕組み |
| 処理 | filter の条件 | result変数への追加方式 |
| 出力 | 出力先シート・列数 | setValues の構造 |
| ログ | 処理名・備考 | appendLog の引数の順番 |
Visible text candidates to use when useful, without reducing the source density: 完全なサンプルコード / ブロック別の「変えてよい場所・変えてはいけない場所」 / ブロック / 変えてよい / 設定ブロック / シート名・列名・条件値 / 入力 / シート名の定数 / 検証 / チェックする列名 / 処理 / filter の条件 / 出力 / 出力先シート・列数 / ログ / 処理名・備考.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S47 読み替える5項目 ─ ワーク

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: 「シート名・ヘッダー・抽出条件・出力先・ログ」の5項目を自分の業務に置き換えるだけで、サンプルコードが自社の自動処理になる
- 推奨図解パターン: checklist-confirmation
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S47 / 演習とレビュー.
Slide topic for context: 読み替える5項目 ─ ワーク.
Concept to visualize: 「シート名・ヘッダー・抽出条件・出力先・ログ」の5項目を自分の業務に置き換えるだけで、サンプルコードが自社の自動処理になる.
Suggested visual pattern: checklist-confirmation.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 「シート名・ヘッダー・抽出条件・出力先・ログ」の5項目を自分の業務に置き換えるだけで、サンプルコードが自社の自動処理になる
**内容ブロック①：5項目と読み替え方（ワーク指示）**
| 項目 | 演習データでの値 | 自分の業務での値 |
|---|---|---|
| INPUT_SHEET（入力シート名） | 問い合わせ台帳 | （記入） |
| STATUS_COL（抽出条件の列名） | ステータス | （記入） |
| TARGET_VAL（抽出する値） | 未対応 | （記入） |
| OUTPUT_SHEET（出力先シート名） | 未対応一覧 | （記入） |
| LOG_SHEET（ログシート名） | 処理ログ | （記入） |
**内容ブロック②：業種別の読み替え例（参考）**
| 業種 | シート名 | 列名 | 条件値 |
|---|---|---|---|
| 製造・卸売り | 注文台帳 | 出荷ステータス | 未出荷 |
| 士業 | 依頼管理台帳 | 処理状況 | 未着手 |
| サービス業 | 問い合わせ台帳 | 対応状況 | 要対応 |
| 建設業 | 日報台帳 | 承認フラグ | 未承認 |
> 動画を一時停止して3分取り組んでください。ワークシートの「コード読み替えメモ」欄に記入してください。
Visible text candidates to use when useful, without reducing the source density: 5項目と読み替え方 / 業種別の読み替え例 / 演習データでの値 / INPUT_SHEET / 問い合わせ台帳 / STATUS_COL / ステータス / TARGET_VAL / 未対応 / OUTPUT_SHEET / 未対応一覧 / LOG_SHEET / 処理ログ / シート名 / 製造・卸売り / 注文台帳 / 士業 / 依頼管理台帳 / サービス業 / 建設業.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S48 テスト観点 ─ 動かす前に確認する6項目

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: テストケースを事前に決めておくことで、本番データで初めて問題に気づく「ぶっつけ本番」を防げる
- 推奨図解パターン: test-case-table
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S48 / 演習とレビュー.
Slide topic for context: テスト観点 ─ 動かす前に確認する6項目.
Concept to visualize: テストケースを事前に決めておくことで、本番データで初めて問題に気づく「ぶっつけ本番」を防げる.
Suggested visual pattern: test-case-table.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: テストケースを事前に決めておくことで、本番データで初めて問題に気づく「ぶっつけ本番」を防げる
**内容ブロック①：テスト観点表**
| テストケース | テスト方法 | 期待する結果 | 自己確認欄 |
|---|---|---|---|
| 対象行あり（通常） | 演習データをそのまま実行 | 未対応行のみ転記 | □ |
| 対象行なし | ステータスを全て「完了」に変えて実行 | 「0件」ログ、転記なし | □ |
| 担当者列が空欄 | 担当者列に空欄行を追加して実行 | 空欄行はスキップ or エラーログ | □ |
| 重複あり | 同じ受付番号を2行入れて実行 | 重複件数がログに出る | □ |
| ヘッダー名が違う | STATUS_COLの値を変えて実行 | エラーメッセージがログに出る | □ |
| 出力シートがない | 出力シートのタブ名を変えて実行 | 「シートが見つかりません」が出る | □ |
**内容ブロック②：テスト後のレビューポイント**
- ログシートに全テストケースの記録が残っているか
- 元データ（入力シート）が変更されていないか
- 実行時間が制限内に収まっているか
> 動画を一時停止して5分取り組んでください。ワークシートの「テスト結果」欄にテストケースの結果を記入してください。
Visible text candidates to use when useful, without reducing the source density: テスト観点表 / テスト後のレビューポイント / テストケース / テスト方法 / 対象行あり / 演習データをそのまま実行 / 対象行なし / ステータスを全て「完了」に変えて実行 / 担当者列が空欄 / 担当者列に空欄行を追加して実行 / 重複あり / 同じ受付番号を2行入れて実行 / ヘッダー名が違う / STATUS_COLの値を変えて実行 / 出力シートがない / 出力シートのタブ名を変えて実行 / ログシートに全テストケースの記録が残っているか / 元データが変更されていないか / 実行時間が制限内に収まっているか / ぶっつけ本番.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S49 自分の業務に置き換える ─ 応用パターン

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: 今日のサンプルコードは「シート名・条件・出力先」を変えるだけで、問い合わせ・申請・日報・期限管理など様々な業務に転用できる
- 推奨図解パターン: use-case-card-set
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S49 / 演習とレビュー.
Slide topic for context: 自分の業務に置き換える ─ 応用パターン.
Concept to visualize: 今日のサンプルコードは「シート名・条件・出力先」を変えるだけで、問い合わせ・申請・日報・期限管理など様々な業務に転用できる.
Suggested visual pattern: use-case-card-set.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 今日のサンプルコードは「シート名・条件・出力先」を変えるだけで、問い合わせ・申請・日報・期限管理など様々な業務に転用できる
**内容ブロック①：業務別の応用パターン**
| 業務 | 入力シート | 抽出条件 | 出力先 | ログの使い道 |
|---|---|---|---|---|
| 問い合わせ管理 | 問い合わせ台帳 | ステータス=未対応 | 未対応一覧 | 週次未対応件数の推移確認 |
| 申請・承認管理 | 申請台帳 | 承認フラグ=未承認 | 承認待ち一覧 | 承認遅延のアラート候補 |
| 期限管理 | タスク台帳 | 期限日＜今日 | 期限超過一覧 | 毎朝トリガーで自動更新 |
| 日報管理 | 日報台帳 | 確認済み=未確認 | 未確認日報一覧 | 上長が毎朝確認 |
| 売上管理 | 売上台帳 | 請求ステータス=未請求 | 未請求一覧 | 月末の請求漏れ確認 |
**内容ブロック②：第4回・第5回への接続**
- 抽出した未対応行 → Gemini/Gemで返信案を生成（第4回）
- 自動処理の要件定義・例外フロー設計（第5回）
**内容ブロック③：自社業務への転用メモ（記入欄）**
- 転用したい業務: ___
- 入力シート名: ___
- 抽出条件（列名 / 値）: ___
- 出力先シート名: ___
- 実行タイミング（手動 / トリガー）: ___
Visible text candidates to use when useful, without reducing the source density: 業務別の応用パターン / 第4回・第5回への接続 / 自社業務への転用メモ / 業務 / 入力シート / 問い合わせ管理 / 問い合わせ台帳 / 申請・承認管理 / 申請台帳 / 期限管理 / タスク台帳 / 日報管理 / 日報台帳 / 売上管理 / 売上台帳 / 抽出した未対応行 → Gemini/Gemで返信案を生成 / 自動処理の要件定義・例外フロー設計 / 転用したい業務 / 入力シート名 / 抽出条件.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```

## S50 まとめと第4回の準備

- セクション: 演習とレビュー
- スライド側ヘッドライン（画像内には原則入れない）: 今日のGAS（機械的処理）と次回のGemini/Gem（分類・要約・下書き）が組み合わさることで、業務の「自動化」と「AI補助」が完成する
- 推奨図解パターン: module-block（3まとめカード）
- 参照素材・スクリーンショット: なし

```text
Create one dense supplemental isometric corporate diagram/reference-sheet image for a Japanese business training slide template.
Canvas: clean white, landscape 1536x1024 (3:2). The image is placed into a fixed 600x322pt zone (about 1.86:1) on every slide, so it will be displayed slightly wider than generated; prefer flat, wide, horizontal compositions and avoid shapes whose meaning depends on being perfectly round or square.
Reserve the top 20 percent of the canvas as clean empty white space: the editable template overlays the slide title and headline there. Compose all diagram content in the lower 80 percent, and keep a small clean margin along the bottom edge because the deck's chapter strip sits just below the image.
Style: clean white, navy and teal accents, light gray thin card borders, subtle soft shadows, calm corporate isometric infographic, screenshot-compatible.
Context only, do not render visible header text: 生成AI・GASで実践する業務変革・DX推進講座 / 第3回 GASによる業務プロセス自動化 / S50 / 演習とレビュー.
Slide topic for context: まとめと第4回の準備.
Concept to visualize: 今日のGAS（機械的処理）と次回のGemini/Gem（分類・要約・下書き）が組み合わさることで、業務の「自動化」と「AI補助」が完成する.
Suggested visual pattern: module-block（3まとめカード）.
Source slide information to preserve. Do not paste long paragraphs or speaker notes, but do preserve the core structures and short table/example cells as visible visual blocks when they carry the slide meaning:
ヘッドライン: 今日のGAS（機械的処理）と次回のGemini/Gem（分類・要約・下書き）が組み合わさることで、業務の「自動化」と「AI補助」が完成する
**内容ブロック①：第3回のまとめ（3点）**
- GASは「設定→入力→条件→処理→出力→ログ」の6ブロックで読む。コードより「読み替える5項目」が重要。
- SpreadsheetAppは「getValues→条件で絞る→setValues」の3ステップが基本。ヘッダーMapで列管理する。
- カスタムメニュー・try/catch・ログ設計で「現場が使える・壊れても戻れる」スクリプトになる。
**内容ブロック②：第4回の予告**
| 次回のテーマ | 内容 |
|---|---|
| GeminiとGemの使い分け | その場の相談 vs 固定業務 vs 共有・標準化 |
| Gem設計の7項目 | 役割・入力・出力形式・禁止事項・判断基準 |
| 業務別ユースケース | 問い合わせ分類・日報要約・Meet文字起こし・コード読解 |
| AI出力レビュー | 事実・推測・要確認・利用不可に分類する |
**内容ブロック③：第4回前の準備**
- ワークシートの「コード読み替えメモ」を完成させる
- 「自社業務に転用したい処理」を1つ決めておく
- 第4回で使う Gemini アカウント（google.com）が使える状態を確認する
Visible text candidates to use when useful, without reducing the source density: 第3回のまとめ / 第4回の予告 / 第4回前の準備 / GASは「設定→入力→条件→処理→出力→ログ」の6ブロックで読む / 次回のテーマ / GeminiとGemの使い分け / その場の相談 vs 固定業務 vs 共有・標準化 / Gem設計の7項目 / 役割・入力・出力形式・禁止事項・判断基準 / 業務別ユースケース / 問い合わせ分類・日報要約・Meet文字起こし・コード読解 / AI出力レビュー / 事実・推測・要確認・利用不可に分類する / ワークシートの「コード読み替えメモ」を完成させる / 「自社業務に転用したい処理」を1つ決めておく / 第4回で使う Gemini アカウントが使える状態を確認する / 自動化 / AI補助 / 読み替える5項目 / 現場が使える・壊れても戻れる.
Create a dense supplemental image, not a decorative icon. Use 2-4 structured zones such as a comparison table, process table, hierarchy diagram, checklist, decision canvas, output map, or industry-example list according to the source slide. Preserve the source slide's key information density by showing the main judgment axis, learner action, output, concrete examples, and review/risk point when they exist.
Visible text inside the image: include readable Japanese headings, table cells, card text, and concrete examples, not only labels. For dense source slides, use roughly 8-20 readable text cells/short phrases across the image. Keep text readable at slide size; numeric notes such as `5〜8個` or `10個以内` are allowed. Avoid long paragraphs, but do not remove core Before/After rows, process steps, hierarchy levels, or industry examples.
The editable Google Slides template will manage the course title, session title, S-number, section header/current position, and full slide title. The generated image may include its own concise content heading and the dense table/canvas text needed to understand the material, but it should not consume the title/header area like a full-slide screenshot.
If the slide needs official screenshots or logos, leave the diagram generic and do not invent real Google UI or brand marks.
```

ネガティブプロンプト:

```text
Do not include the editable deck header/footer, S-number, course title, session title, section header, fake Google logos or UI, personal data, placeholders, empty dashed frames, poster/recruitment/ad layouts, unreadable small text, incorrect Japanese, text-free decorative icon-only output, or short-label-only output with no explanation. Avoid long paragraph dumps and speaker notes. Use readable Japanese headings, table cells, card text, and examples as dense supplemental information.
```
