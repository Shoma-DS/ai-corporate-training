# 03-GASによる業務プロセス自動化 画像生成プロンプト（1枚まるごとラスター・ワイヤーフレーム参照版）

対象講座: 生成AI・GASで実践する業務変革・DX推進講座
対象セッション: 第3回 スライド構成案: GASによる業務プロセス自動化
対象スライド: S01-S30
採用テンプレート: `isometric-corporate-clean` + 固定レイアウトワイヤーフレーム参照
参照レイアウト画像（毎回必ず添付する）: `/Users/deguchishouma/Desktop/AI法人研修/skills/corporate-training-course-builder/references/assets/講座スライドレイアウトワイヤフレーム.png`

このファイルは、編集可能Google Slidesテンプレート方式ではなく、1スライド=1枚の完成ラスター画像を毎回ワイヤーフレーム参照画像付きで生成する標準に基づく。講師コメントがなくても、スライドだけで内容を想像できる密度を維持しつつ、レイアウトの再現性をワイヤーフレーム参照で担保する。

## 共通ルール

- Codex App Server / GPT image 2 / `imagegen` スキルで1枚まるごとの完成ラスター画像として生成する。
- 生成のたびに参照レイアウト画像 `講座スライドレイアウトワイヤフレーム.png` を添付し、帯構成・比率・罫線をそのまま踏襲する。テキストで説明するだけでなく、画像そのものを参照入力として渡す。
- SVG、HTML/CSS、canvas、ブラウザスクリーンショット、PIL/Pillow、ImageMagick、PDF/PPTX書き出し、ローカル変換、後載せテキスト合成は使わない。
- 1枚ごとに、講座タイトル帯、スライド番号/全体の枚数帯、スライド見出し帯、図解エリア（3〜6個の本文ブロック・具体例・成果物・確認観点）、今回のまとめ帯、目次見出しストリップを必ず入れる。
- 画像内テキストは、スライド案にある語句を優先し、勝手な要約や別概念を足さない。長すぎる本文は、意味を保ってカード・表・フローに分割する。
- 目次見出しストリップは、現在の章のチップだけを塗りつぶしてハイライトし、他のチップは白背景・薄い枠線にする。
- 実在ロゴや実在UIは参照素材がある場合だけ使う。参照素材なしにGoogleロゴやGoogle画面を描かせない。
- 会社名、顧客名、社員名、メールアドレス、価格、連絡先、契約情報、APIキー、社内固有情報は入れない。
- `素材配置枠`、`公式ロゴ`、空の破線枠、透かし、QRコード、架空UI、架空ロゴを入れない。

## 共通ベースプロンプト

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
```

## ワイヤーフレーム参照指示（共通・毎回付与）

```text
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
```

## 共通ネガティブプロンプト

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## 目次見出しストリップ（章構成）

- 1 導入（S01-S06）
- 2 JS最小限（S07-S11）
- 3 Sheets操作（S12-S16）
- 4 自動化処理（S17-S23）
- 5 メニュー（S24-S27）
- 6 演習（S28-S30）

## セッション全体の時間配分

画像生成時の文脈として参照する。時間配分そのものを全スライドに入れる必要はない。

```markdown
## 120分の時間配分

| ブロック | 時間 | スライド範囲 | ねらい |
|---|---:|---|---|
| 導入とGASの位置づけ | 15分 | S01-S06 | GASで減らす作業・減らさない判断を確認する |
| JavaScript最小限 | 15分 | S07-S11 | コード読解に必要な構文だけに絞る |
| SpreadsheetApp基礎 | 20分 | S12-S16 | シート・範囲・2次元配列・ヘッダー管理を理解する |
| Excel的処理の自動化 | 30分 | S17-S23 | 抽出・転記・集計・更新・ログを実装する |
| メニュー・エラー・権限 | 20分 | S24-S27 | 現場担当者が使える入口と止まった時の見方を設計する |
| 演習とレビュー | 20分 | S28-S30 | サンプルコードを業務条件に合わせて読み替える |
```

## Slide S01. GASで繰り返し表作業を自動化する ─ 第3回

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S01 / 30
- スライド見出し帯: GASで繰り返し表作業を自動化する ─ 第3回
- 今回のまとめ帯（要点・結論・次のアクション）: GASを事務ロボットとして設計すると、未対応抽出・転記・集計・ログ記録を安全に自動化できる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 導入
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:今日の到達点
未対応・未請求行を別シートへ抽出できる
処理件数・エラーをログシートに記録できる
現場担当者が押せるカスタムメニューまで設計できる
内容ブロック2:今日の成果物
| 成果物 | 内容 |
|---|---|
| 自動処理スクリプト | 未請求抽出・重複チェック・ログ追記が動く `.gs` |
| 抽出結果シート | 条件に合う行だけを別シートへ出力 |
| 処理ログシート | 実行日時・処理名・件数・エラー数を記録 |
| コード読み替えメモ | シート名・列名・条件・出力先を自社業務に置換 |
内容ブロック3:受講者への問い
今週、ExcelやSheetsで同じフィルタ・コピペ・集計を何回繰り返したかを書き出す
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S01 / 30
スライド見出し帯（そのまま表示）: GASで繰り返し表作業を自動化する ─ 第3回
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: GASを事務ロボットとして設計すると、未対応抽出・転記・集計・ログ記録を安全に自動化できる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 導入
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:今日の到達点
未対応・未請求行を別シートへ抽出できる
処理件数・エラーをログシートに記録できる
現場担当者が押せるカスタムメニューまで設計できる
内容ブロック2:今日の成果物
| 成果物 | 内容 |
|---|---|
| 自動処理スクリプト | 未請求抽出・重複チェック・ログ追記が動く `.gs` |
| 抽出結果シート | 条件に合う行だけを別シートへ出力 |
| 処理ログシート | 実行日時・処理名・件数・エラー数を記録 |
| コード読み替えメモ | シート名・列名・条件・出力先を自社業務に置換 |
内容ブロック3:受講者への問い
今週、ExcelやSheetsで同じフィルタ・コピペ・集計を何回繰り返したかを書き出す
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S02. 今日の2時間で作るものと演習データ

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S02 / 30
- スライド見出し帯: 今日の2時間で作るものと演習データ
- 今回のまとめ帯（要点・結論・次のアクション）: コードを丸暗記するより、入力元・条件・出力先・ログを自分の業務に読み替えることが重要
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 導入
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:演習データ
| ファイル名 | 内容 | 使う場面 |
|---|---|---|
| `CSV取込_売上明細_5月1週.csv` | 架空の売上明細50行 | 未請求抽出・ヘッダー確認 |
| `問い合わせ処理台帳_GAS演習.csv` | 架空の問い合わせ台帳30行 | 未対応抽出・ステータス更新 |
| `タスク期限リマインド_GAS演習.csv` | 架空のタスク一覧20行 | 期限超過確認・担当者別集計 |
内容ブロック2:読み替える4点
シート名
ヘッダー名
抽出条件
出力先とログ列
内容ブロック3:安全ルール
実在の顧客情報・社員情報・メール・契約情報は使わない
演習は `演習データ/` の架空データだけで成立させる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S02 / 30
スライド見出し帯（そのまま表示）: 今日の2時間で作るものと演習データ
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: コードを丸暗記するより、入力元・条件・出力先・ログを自分の業務に読み替えることが重要
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 導入
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:演習データ
| ファイル名 | 内容 | 使う場面 |
|---|---|---|
| `CSV取込_売上明細_5月1週.csv` | 架空の売上明細50行 | 未請求抽出・ヘッダー確認 |
| `問い合わせ処理台帳_GAS演習.csv` | 架空の問い合わせ台帳30行 | 未対応抽出・ステータス更新 |
| `タスク期限リマインド_GAS演習.csv` | 架空のタスク一覧20行 | 期限超過確認・担当者別集計 |
内容ブロック2:読み替える4点
シート名
ヘッダー名
抽出条件
出力先とログ列
内容ブロック3:安全ルール
実在の顧客情報・社員情報・メール・契約情報は使わない
演習は `演習データ/` の架空データだけで成立させる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S03. 第3回の目次/全体像

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S03 / 30
- スライド見出し帯: 第3回の目次/全体像
- 今回のまとめ帯（要点・結論・次のアクション）: 6章を通じて、読む・選ぶ・書く・記録するというGASの基本動作を1つの業務処理にまとめる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 導入
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:目次と成果物
| No. | 目次項目 | 時間 | スライド範囲 | 支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入とGASの位置づけ | 15分 | S01-S06 | 自動処理の対象範囲メモ |
| 2 | JavaScript最小限 | 15分 | S07-S11 | コード読み替えメモ |
| 3 | SpreadsheetApp基礎 | 20分 | S12-S16 | Sheets操作メモ |
| 4 | Excel的処理の自動化 | 30分 | S17-S23 | 自動処理スクリプト |
| 5 | メニュー・エラー・権限 | 20分 | S24-S27 | テスト観点・実行ルール |
| 6 | 演習とレビュー | 20分 | S28-S30 | 業務適用版読み替えメモ |
内容ブロック2:デモ/演習の位置
| スライド | ここで行うこと |
|---|---|
| S11 | 画面共有: まず読み取ってログに出す |
| S22 | 画面共有: 未請求抽出・転記・ログ追記を実行 |
| S29 | 演習: コードを自分の業務条件へ読み替える |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S03 / 30
スライド見出し帯（そのまま表示）: 第3回の目次/全体像
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 6章を通じて、読む・選ぶ・書く・記録するというGASの基本動作を1つの業務処理にまとめる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 導入
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:目次と成果物
| No. | 目次項目 | 時間 | スライド範囲 | 支える成果物 |
|---:|---|---:|---|---|
| 1 | 導入とGASの位置づけ | 15分 | S01-S06 | 自動処理の対象範囲メモ |
| 2 | JavaScript最小限 | 15分 | S07-S11 | コード読み替えメモ |
| 3 | SpreadsheetApp基礎 | 20分 | S12-S16 | Sheets操作メモ |
| 4 | Excel的処理の自動化 | 30分 | S17-S23 | 自動処理スクリプト |
| 5 | メニュー・エラー・権限 | 20分 | S24-S27 | テスト観点・実行ルール |
| 6 | 演習とレビュー | 20分 | S28-S30 | 業務適用版読み替えメモ |
内容ブロック2:デモ/演習の位置
| スライド | ここで行うこと |
|---|---|
| S11 | 画面共有: まず読み取ってログに出す |
| S22 | 画面共有: 未請求抽出・転記・ログ追記を実行 |
| S29 | 演習: コードを自分の業務条件へ読み替える |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S04. 章見出し/現在位置 1/6: 導入とGASの位置づけ

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 1/6 導入とGASの位置づけ
- 前の章: なし
- 次の章: JavaScript最小限
- 章の一言(画像内は小さく): ここから「1. 導入とGASの位置づけ」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「1」「導入とGASの位置づけ」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「1 / 6」。
前の章がないため、前の章の表示は省略する。
現在の章の右下または下に、次の章「JavaScript最小限」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S04」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「1. 導入とGASの位置づけ」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は15分、対象スライド範囲はS01-S06。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S05. 第1回・第2回からGAS実装へつなげる

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S05 / 30
- スライド見出し帯: 第1回・第2回からGAS実装へつなげる
- 今回のまとめ帯（要点・結論・次のアクション）: 業務選定→台帳設計→GAS実装の順で積み上げると、自動化は安定して動く
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 導入
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:3回分の接続
| 回 | やったこと | 今回との接続 |
|---|---|---|
| 第1回 | 手作業の洗い出し・優先度整理 | どの処理を自動化するかの根拠 |
| 第2回 | Forms設問・Sheets列設計 | GASが読む列名・ステータス値 |
| 第3回 | 抽出・転記・集計・ログ | 機械的な手作業を置き換える |
内容ブロック2:事務ロボットの比喩
GASは決まった手順を自動でこなす事務担当ロボット
渡す台帳の列名や値がそろっていないと、ロボットは迷って止まる
内容ブロック3:今日扱う台帳列
| 列名 | GASが使う操作 |
|---|---|
| ステータス | 未対応行の抽出 |
| 担当者 | 担当者別集計 |
| 請求フラグ | 未請求抽出 |
| GAS更新日 | スクリプトが書き込む |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S05 / 30
スライド見出し帯（そのまま表示）: 第1回・第2回からGAS実装へつなげる
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 業務選定→台帳設計→GAS実装の順で積み上げると、自動化は安定して動く
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 導入
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:3回分の接続
| 回 | やったこと | 今回との接続 |
|---|---|---|
| 第1回 | 手作業の洗い出し・優先度整理 | どの処理を自動化するかの根拠 |
| 第2回 | Forms設問・Sheets列設計 | GASが読む列名・ステータス値 |
| 第3回 | 抽出・転記・集計・ログ | 機械的な手作業を置き換える |
内容ブロック2:事務ロボットの比喩
GASは決まった手順を自動でこなす事務担当ロボット
渡す台帳の列名や値がそろっていないと、ロボットは迷って止まる
内容ブロック3:今日扱う台帳列
| 列名 | GASが使う操作 |
|---|---|
| ステータス | 未対応行の抽出 |
| 担当者 | 担当者別集計 |
| 請求フラグ | 未請求抽出 |
| GAS更新日 | スクリプトが書き込む |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S06. GAS・Gemini・人の役割を分ける

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S06 / 30
- スライド見出し帯: GAS・Gemini・人の役割を分ける
- 今回のまとめ帯（要点・結論・次のアクション）: 機械的な処理はGAS、曖昧な文章はGemini、承認と例外判断は人に戻す
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 1 導入
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:役割分担表
| 担当 | 向いている処理 | 例 |
|---|---|---|
| GAS | 条件が決まった機械的処理 | 未対応行抽出、ステータス更新、ログ追記 |
| Gemini/Gem | 文章の分類・要約・下書き | 問い合わせ分類、返信案、日報要約 |
| 人 | 承認・例外判断・最終確認 | 要確認フラグ行、外部送信判断 |
| Sheets | データ保持・共有 | 台帳、集計表、ログの閲覧 |
内容ブロック2:自動化しない領域
対外送信を確認なしで自動化しない
承認・決裁をGASだけで完結させない
個人情報を含む処理はダミー化・匿名化を前提にする
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S06 / 30
スライド見出し帯（そのまま表示）: GAS・Gemini・人の役割を分ける
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 機械的な処理はGAS、曖昧な文章はGemini、承認と例外判断は人に戻す
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 1 導入
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:役割分担表
| 担当 | 向いている処理 | 例 |
|---|---|---|
| GAS | 条件が決まった機械的処理 | 未対応行抽出、ステータス更新、ログ追記 |
| Gemini/Gem | 文章の分類・要約・下書き | 問い合わせ分類、返信案、日報要約 |
| 人 | 承認・例外判断・最終確認 | 要確認フラグ行、外部送信判断 |
| Sheets | データ保持・共有 | 台帳、集計表、ログの閲覧 |
内容ブロック2:自動化しない領域
対外送信を確認なしで自動化しない
承認・決裁をGASだけで完結させない
個人情報を含む処理はダミー化・匿名化を前提にする
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S07. 章見出し/現在位置 2/6: JavaScript最小限

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 2/6 JavaScript最小限
- 前の章: 導入とGASの位置づけ
- 次の章: SpreadsheetApp基礎
- 章の一言(画像内は小さく): ここから「2. JavaScript最小限」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「2」「JavaScript最小限」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「2 / 6」。
画面の隅に、前の章「導入とGASの位置づけ」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「SpreadsheetApp基礎」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S07」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「2. JavaScript最小限」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は15分、対象スライド範囲はS07-S11。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S08. コードは読む順番があると速い

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S08 / 30
- スライド見出し帯: コードは読む順番があると速い
- 今回のまとめ帯（要点・結論・次のアクション）: 設定→取得→条件→出力→ログの順で読むと、知らないコードでも業務への置き換え点が見える
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 JS最小限
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:読む順番
1. 設定ブロック: シート名・列名・条件
2. 取得ブロック: `getSheetByName` と `getValues`
3. 条件ブロック: `if` / `filter`
4. 出力ブロック: `setValues` / `appendRow`
5. ログブロック: 件数・エラー・実行日時
内容ブロック2:最小構文
| 構文 | 実務上の意味 |
|---|---|
| `const` | 後から変えない設定値 |
| `let` | 処理中に変わる値 |
| `if` | 条件に合う行だけ処理 |
| `for` | 全行を1件ずつ確認 |
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S08 / 30
スライド見出し帯（そのまま表示）: コードは読む順番があると速い
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 設定→取得→条件→出力→ログの順で読むと、知らないコードでも業務への置き換え点が見える
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 JS最小限
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:読む順番
1. 設定ブロック: シート名・列名・条件
2. 取得ブロック: `getSheetByName` と `getValues`
3. 条件ブロック: `if` / `filter`
4. 出力ブロック: `setValues` / `appendRow`
5. ログブロック: 件数・エラー・実行日時
内容ブロック2:最小構文
| 構文 | 実務上の意味 |
|---|---|
| `const` | 後から変えない設定値 |
| `let` | 処理中に変わる値 |
| `if` | 条件に合う行だけ処理 |
| `for` | 全行を1件ずつ確認 |
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S09. ヘッダー名から列番号を探す

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S09 / 30
- スライド見出し帯: ヘッダー名から列番号を探す
- 今回のまとめ帯（要点・結論・次のアクション）: 列番号ではなくヘッダー名で処理すると、列の追加や並び替えに強いコードになる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 JS最小限
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:headerMapの考え方
1行目のヘッダーを読み取り、`ステータス` や `請求フラグ` が何列目かを辞書化する
`C列` 固定ではなく、ヘッダー名で探すことで台帳変更に耐える
内容ブロック2:2次元配列の見方
| Sheets上 | コード上 |
|---|---|
| 1行 | `row` |
| 1セル | `row[col]` |
| 全体 | `values` |
| ヘッダー行 | `values[0]` |
内容ブロック3:よくある失敗
ヘッダー名の表記ゆれ
空欄列の追加
全角/半角スペースの混入
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S09 / 30
スライド見出し帯（そのまま表示）: ヘッダー名から列番号を探す
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 列番号ではなくヘッダー名で処理すると、列の追加や並び替えに強いコードになる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 JS最小限
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:headerMapの考え方
1行目のヘッダーを読み取り、`ステータス` や `請求フラグ` が何列目かを辞書化する
`C列` 固定ではなく、ヘッダー名で探すことで台帳変更に耐える
内容ブロック2:2次元配列の見方
| Sheets上 | コード上 |
|---|---|
| 1行 | `row` |
| 1セル | `row[col]` |
| 全体 | `values` |
| ヘッダー行 | `values[0]` |
内容ブロック3:よくある失敗
ヘッダー名の表記ゆれ
空欄列の追加
全角/半角スペースの混入
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S10. 日付・文字列・空欄の扱い

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S10 / 30
- スライド見出し帯: 日付・文字列・空欄の扱い
- 今回のまとめ帯（要点・結論・次のアクション）: 実務データはきれいではないため、日付・文字列・空欄の扱いを先に決めておく
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 JS最小限
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 左右比較またはBefore/After表。左に課題・現状、右に改善後・設計後を置き、差分が一目で分かる構成。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実務で起きるズレ
| 項目 | 起きること | 対応 |
|---|---|---|
| 日付 | 表示は同じでも型が違う | `new Date()` で比較前に整える |
| 文字列 | 前後に空白が混じる | `trim()` で整える |
| 空欄 | 未入力と0が混在 | 空欄チェックを明示する |
| 数値 | 文字列として入る | 必要なら数値変換する |
内容ブロック2:エラーを避ける原則
処理前に入力状態を確認する
想定外の値はログに残す
失敗しても元データを壊さない
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S10 / 30
スライド見出し帯（そのまま表示）: 日付・文字列・空欄の扱い
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 実務データはきれいではないため、日付・文字列・空欄の扱いを先に決めておく
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 JS最小限
図解エリアの推奨構成: 左右比較またはBefore/After表。左に課題・現状、右に改善後・設計後を置き、差分が一目で分かる構成。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実務で起きるズレ
| 項目 | 起きること | 対応 |
|---|---|---|
| 日付 | 表示は同じでも型が違う | `new Date()` で比較前に整える |
| 文字列 | 前後に空白が混じる | `trim()` で整える |
| 空欄 | 未入力と0が混在 | 空欄チェックを明示する |
| 数値 | 文字列として入る | 必要なら数値変換する |
内容ブロック2:エラーを避ける原則
処理前に入力状態を確認する
想定外の値はログに残す
失敗しても元データを壊さない
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S11. 画面共有: まず読み取ってログに出す

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S11 / 30
- スライド見出し帯: 画面共有: まず読み取ってログに出す
- 今回のまとめ帯（要点・結論・次のアクション）: 最初は書き換えず、読むだけ・ログに出すだけで台帳とコードの接続を確認する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 2 JS最小限
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実演手順
1. Apps Scriptエディタを開く
2. `getSheetByName` で台帳シートを取得
3. `getDataRange().getValues()` で全体を読む
4. ヘッダー名と行数を `Logger.log` に出す
内容ブロック2:見せるポイント
いきなり `setValues()` で書き換えない
まず行数・ヘッダー・対象行数を確認する
ログが「処理前の確認画面」になる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S11 / 30
スライド見出し帯（そのまま表示）: 画面共有: まず読み取ってログに出す
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 最初は書き換えず、読むだけ・ログに出すだけで台帳とコードの接続を確認する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 2 JS最小限
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実演手順
1. Apps Scriptエディタを開く
2. `getSheetByName` で台帳シートを取得
3. `getDataRange().getValues()` で全体を読む
4. ヘッダー名と行数を `Logger.log` に出す
内容ブロック2:見せるポイント
いきなり `setValues()` で書き換えない
まず行数・ヘッダー・対象行数を確認する
ログが「処理前の確認画面」になる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S12. 章見出し/現在位置 3/6: SpreadsheetApp基礎

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 3/6 SpreadsheetApp基礎
- 前の章: JavaScript最小限
- 次の章: Excel的処理の自動化
- 章の一言(画像内は小さく): ここから「3. SpreadsheetApp基礎」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「3」「SpreadsheetApp基礎」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「3 / 6」。
画面の隅に、前の章「JavaScript最小限」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「Excel的処理の自動化」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S12」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「3. SpreadsheetApp基礎」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は20分、対象スライド範囲はS12-S16。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S13. SpreadsheetAppで触れる4つの階層

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S13 / 30
- スライド見出し帯: SpreadsheetAppで触れる4つの階層
- 今回のまとめ帯（要点・結論・次のアクション）: スプレッドシート→シート→範囲→値の階層で考えると、コードの対象が明確になる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Sheets操作
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:4階層マップ
| 階層 | GASの書き方 | 実務で指すもの |
|---|---|---|
| Spreadsheet | `SpreadsheetApp.getActiveSpreadsheet()` | ファイル全体 |
| Sheet | `getSheetByName('台帳')` | タブ |
| Range | `getRange(2,1,10,5)` | セル範囲 |
| Values | `getValues()` | 行と列のデータ |
内容ブロック2:判断ポイント
ファイルが違うのか、タブが違うのか、範囲が違うのかを切り分ける
エラー時はこの階層のどこで止まったかを見る
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S13 / 30
スライド見出し帯（そのまま表示）: SpreadsheetAppで触れる4つの階層
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: スプレッドシート→シート→範囲→値の階層で考えると、コードの対象が明確になる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Sheets操作
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:4階層マップ
| 階層 | GASの書き方 | 実務で指すもの |
|---|---|---|
| Spreadsheet | `SpreadsheetApp.getActiveSpreadsheet()` | ファイル全体 |
| Sheet | `getSheetByName('台帳')` | タブ |
| Range | `getRange(2,1,10,5)` | セル範囲 |
| Values | `getValues()` | 行と列のデータ |
内容ブロック2:判断ポイント
ファイルが違うのか、タブが違うのか、範囲が違うのかを切り分ける
エラー時はこの階層のどこで止まったかを見る
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S14. 読む・処理する・まとめて書く

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S14 / 30
- スライド見出し帯: 読む・処理する・まとめて書く
- 今回のまとめ帯（要点・結論・次のアクション）: GASは1セルずつ触るより、まとめて読んで配列で処理し、最後にまとめて書く方が安定する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Sheets操作
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:3ステップ
1. 読む: `getValues()` で表全体または対象範囲を取得
2. 処理する: 配列上で条件抽出・集計・変換
3. 書く: `setValues()` または `appendRow()` で結果を出力
内容ブロック2:setValuesの形
| 出力したいもの | 必要な形 |
|---|---|
| 1行3列 | `[[A, B, C]]` |
| 3行1列 | `[[A], [B], [C]]` |
| 2行3列 | `[[A,B,C], [D,E,F]]` |
内容ブロック3:よくあるエラー
行数・列数が合わない
空の配列を書こうとする
ヘッダーと本文を混ぜて処理する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S14 / 30
スライド見出し帯（そのまま表示）: 読む・処理する・まとめて書く
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: GASは1セルずつ触るより、まとめて読んで配列で処理し、最後にまとめて書く方が安定する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Sheets操作
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:3ステップ
1. 読む: `getValues()` で表全体または対象範囲を取得
2. 処理する: 配列上で条件抽出・集計・変換
3. 書く: `setValues()` または `appendRow()` で結果を出力
内容ブロック2:setValuesの形
| 出力したいもの | 必要な形 |
|---|---|
| 1行3列 | `[[A, B, C]]` |
| 3行1列 | `[[A], [B], [C]]` |
| 2行3列 | `[[A,B,C], [D,E,F]]` |
内容ブロック3:よくあるエラー
行数・列数が合わない
空の配列を書こうとする
ヘッダーと本文を混ぜて処理する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S15. 原本・出力先・ログを分ける

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S15 / 30
- スライド見出し帯: 原本・出力先・ログを分ける
- 今回のまとめ帯（要点・結論・次のアクション）: 原本シートを守り、出力先とログを分けることで、失敗しても戻れる設計になる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Sheets操作
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:3シート構成
| シート | 役割 | GASの操作 |
|---|---|---|
| 原本台帳 | フォーム回答・元データ | 読む中心、直接上書きしすぎない |
| 抽出結果 | 未対応・未請求などの出力 | 毎回クリアして書き出す |
| 処理ログ | 実行日時・件数・エラー | `appendRow()` で追記 |
内容ブロック2:復旧しやすい理由
原本を壊していない
いつ何件処理したか分かる
出力先だけを再作成できる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S15 / 30
スライド見出し帯（そのまま表示）: 原本・出力先・ログを分ける
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 原本シートを守り、出力先とログを分けることで、失敗しても戻れる設計になる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Sheets操作
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:3シート構成
| シート | 役割 | GASの操作 |
|---|---|---|
| 原本台帳 | フォーム回答・元データ | 読む中心、直接上書きしすぎない |
| 抽出結果 | 未対応・未請求などの出力 | 毎回クリアして書き出す |
| 処理ログ | 実行日時・件数・エラー | `appendRow()` で追記 |
内容ブロック2:復旧しやすい理由
原本を壊していない
いつ何件処理したか分かる
出力先だけを再作成できる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S16. GASが更新する列・人が更新する列を分ける

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S16 / 30
- スライド見出し帯: GASが更新する列・人が更新する列を分ける
- 今回のまとめ帯（要点・結論・次のアクション）: 人とGASが同じ列を触らないようにすると、上書き事故と責任の曖昧さを防げる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 3 Sheets操作
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:列の担当分担
| 列 | 更新者 | 例 |
|---|---|---|
| 顧客入力 | フォーム回答者 | 氏名、依頼内容、希望日 |
| 担当者判断 | 人 | 対応方針、承認、コメント |
| GAS更新 | GAS | 受付番号、通知日時、処理済みフラグ |
| ログ | GAS | エラー内容、実行者、処理件数 |
内容ブロック2:保護の観点
GAS更新列を人が編集しないよう説明する
人の判断列はGASで上書きしない
第5回で権限・保護・復旧設計に接続する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S16 / 30
スライド見出し帯（そのまま表示）: GASが更新する列・人が更新する列を分ける
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 人とGASが同じ列を触らないようにすると、上書き事故と責任の曖昧さを防げる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 3 Sheets操作
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:列の担当分担
| 列 | 更新者 | 例 |
|---|---|---|
| 顧客入力 | フォーム回答者 | 氏名、依頼内容、希望日 |
| 担当者判断 | 人 | 対応方針、承認、コメント |
| GAS更新 | GAS | 受付番号、通知日時、処理済みフラグ |
| ログ | GAS | エラー内容、実行者、処理件数 |
内容ブロック2:保護の観点
GAS更新列を人が編集しないよう説明する
人の判断列はGASで上書きしない
第5回で権限・保護・復旧設計に接続する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S17. 章見出し/現在位置 4/6: Excel的処理の自動化

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 4/6 Excel的処理の自動化
- 前の章: SpreadsheetApp基礎
- 次の章: メニュー・エラー・権限
- 章の一言(画像内は小さく): ここから「4. Excel的処理の自動化」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「4」「Excel的処理の自動化」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「4 / 6」。
画面の隅に、前の章「SpreadsheetApp基礎」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「メニュー・エラー・権限」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S17」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「4. Excel的処理の自動化」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は30分、対象スライド範囲はS17-S23。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S18. 抽出条件を業務の言葉で決める

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S18 / 30
- スライド見出し帯: 抽出条件を業務の言葉で決める
- 今回のまとめ帯（要点・結論・次のアクション）: GASの条件式は、現場の判断基準をそのまま表したものにする
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:4つの抽出パターン
| 業務パターン | 条件例 | 出力先 |
|---|---|---|
| 未対応 | `ステータス == '未対応'` | 未対応一覧 |
| 未請求 | `請求フラグ == '未請求'` | 請求対象一覧 |
| 期限超過 | `期限 < 今日 && 完了でない` | リマインド対象 |
| 重複疑い | `受付ID` または `メール` が重複 | 確認リスト |
内容ブロック2:条件設計の注意
条件名をワークシートに書いてからコードへ移す
例外値（空欄・表記ゆれ）を先に決める
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S18 / 30
スライド見出し帯（そのまま表示）: 抽出条件を業務の言葉で決める
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: GASの条件式は、現場の判断基準をそのまま表したものにする
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:4つの抽出パターン
| 業務パターン | 条件例 | 出力先 |
|---|---|---|
| 未対応 | `ステータス == '未対応'` | 未対応一覧 |
| 未請求 | `請求フラグ == '未請求'` | 請求対象一覧 |
| 期限超過 | `期限 < 今日 && 完了でない` | リマインド対象 |
| 重複疑い | `受付ID` または `メール` が重複 | 確認リスト |
内容ブロック2:条件設計の注意
条件名をワークシートに書いてからコードへ移す
例外値（空欄・表記ゆれ）を先に決める
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S19. 転記・集計・重複チェックを組み合わせる

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S19 / 30
- スライド見出し帯: 転記・集計・重複チェックを組み合わせる
- 今回のまとめ帯（要点・結論・次のアクション）: Excelで毎回やっていた処理を、抽出→転記→集計→重複確認の順で自動化する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:処理の対応表
| 手作業 | GASでの置き換え |
|---|---|
| フィルタで未対応を探す | 条件に合う行を配列で抽出 |
| 別シートへ貼り付け | `setValues()` でまとめて出力 |
| 担当者別に数える | `Map` で件数集計 |
| 重複を目で探す | `Set` で重複検出 |
内容ブロック2:業種別例
士業: 未請求案件だけ抽出して請求チェックへ
製造業: 未出荷注文を抽出して納期確認へ
建設業: 未確認日報を抽出して現場責任者へ
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S19 / 30
スライド見出し帯（そのまま表示）: 転記・集計・重複チェックを組み合わせる
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Excelで毎回やっていた処理を、抽出→転記→集計→重複確認の順で自動化する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:処理の対応表
| 手作業 | GASでの置き換え |
|---|---|
| フィルタで未対応を探す | 条件に合う行を配列で抽出 |
| 別シートへ貼り付け | `setValues()` でまとめて出力 |
| 担当者別に数える | `Map` で件数集計 |
| 重複を目で探す | `Set` で重複検出 |
内容ブロック2:業種別例
士業: 未請求案件だけ抽出して請求チェックへ
製造業: 未出荷注文を抽出して納期確認へ
建設業: 未確認日報を抽出して現場責任者へ
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S20. ステータス更新とログ追記

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S20 / 30
- スライド見出し帯: ステータス更新とログ追記
- 今回のまとめ帯（要点・結論・次のアクション）: 処理結果を台帳とログに残すことで、後から何が起きたか説明できる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:書き戻す情報
| 書き戻し先 | 内容 | 注意 |
|---|---|---|
| 台帳 | 処理済みフラグ、GAS更新日 | 人の判断列は上書きしない |
| 抽出結果 | 対象行のコピー | 毎回作り直せる形にする |
| ログ | 実行日時、処理名、件数、エラー | 追記で履歴を残す |
内容ブロック2:ログの最小項目
実行日時
処理名
対象件数
成功件数
エラー件数
エラーメッセージ
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S20 / 30
スライド見出し帯（そのまま表示）: ステータス更新とログ追記
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 処理結果を台帳とログに残すことで、後から何が起きたか説明できる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:書き戻す情報
| 書き戻し先 | 内容 | 注意 |
|---|---|---|
| 台帳 | 処理済みフラグ、GAS更新日 | 人の判断列は上書きしない |
| 抽出結果 | 対象行のコピー | 毎回作り直せる形にする |
| ログ | 実行日時、処理名、件数、エラー | 追記で履歴を残す |
内容ブロック2:ログの最小項目
実行日時
処理名
対象件数
成功件数
エラー件数
エラーメッセージ
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S21. 重複チェックと一意キー

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S21 / 30
- スライド見出し帯: 重複チェックと一意キー
- 今回のまとめ帯（要点・結論・次のアクション）: 受付IDやメールだけに頼らず、業務に合う一意キーを決めると重複検出が安定する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:一意キー候補
| 業務 | キー候補 | 注意 |
|---|---|---|
| 問い合わせ | 受付ID | フォーム送信時に自動採番 |
| 請求 | 請求番号+取引先 | 月をまたぐ重複に注意 |
| 日報 | 現場ID+日付+担当者 | 同日複数投稿の扱いを決める |
| タスク | タスクID | 人が手入力しない方が安全 |
内容ブロック2:Setで見る理由
すでに出たキーを記録する
2回目に出たら重複候補としてログへ出す
削除ではなく確認リストに出す
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S21 / 30
スライド見出し帯（そのまま表示）: 重複チェックと一意キー
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 受付IDやメールだけに頼らず、業務に合う一意キーを決めると重複検出が安定する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:一意キー候補
| 業務 | キー候補 | 注意 |
|---|---|---|
| 問い合わせ | 受付ID | フォーム送信時に自動採番 |
| 請求 | 請求番号+取引先 | 月をまたぐ重複に注意 |
| 日報 | 現場ID+日付+担当者 | 同日複数投稿の扱いを決める |
| タスク | タスクID | 人が手入力しない方が安全 |
内容ブロック2:Setで見る理由
すでに出たキーを記録する
2回目に出たら重複候補としてログへ出す
削除ではなく確認リストに出す
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S22. 画面共有: 未請求抽出・転記・ログ追記を実行する

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S22 / 30
- スライド見出し帯: 画面共有: 未請求抽出・転記・ログ追記を実行する
- 今回のまとめ帯（要点・結論・次のアクション）: 読むだけの確認から、抽出・転記・ログ追記まで進めて一連の処理を確認する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実演手順
1. 売上明細CSVをSheetsへ取り込む
2. 未請求条件を設定ブロックで確認する
3. 未請求行を抽出し、抽出結果シートへ出力する
4. 処理ログシートに件数を追記する
内容ブロック2:見るポイント
原本は壊さない
出力先は再作成できる
ログに件数が残る
エラー時にどこで止まったか分かる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S22 / 30
スライド見出し帯（そのまま表示）: 画面共有: 未請求抽出・転記・ログ追記を実行する
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 読むだけの確認から、抽出・転記・ログ追記まで進めて一連の処理を確認する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 横または縦のプロセスフロー。各ステップに短い説明、入力、処理、出力、確認ポイントを付ける。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実演手順
1. 売上明細CSVをSheetsへ取り込む
2. 未請求条件を設定ブロックで確認する
3. 未請求行を抽出し、抽出結果シートへ出力する
4. 処理ログシートに件数を追記する
内容ブロック2:見るポイント
原本は壊さない
出力先は再作成できる
ログに件数が残る
エラー時にどこで止まったか分かる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S23. 失敗しても戻れる設計

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S23 / 30
- スライド見出し帯: 失敗しても戻れる設計
- 今回のまとめ帯（要点・結論・次のアクション）: 原本保護・出力先分離・ログ記録をそろえると、GAS処理は怖くなくなる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 4 自動化処理
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:戻れる設計の3点
| 観点 | 実装例 |
|---|---|
| 原本保護 | 読む中心。上書き列を限定する |
| 出力先分離 | 結果は別シートに作る |
| ログ記録 | 実行履歴・件数・エラーを追記する |
内容ブロック2:やってはいけない例
原本シートを毎回全クリアする
人のメモ列をGASが上書きする
エラーを握りつぶして成功扱いにする
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S23 / 30
スライド見出し帯（そのまま表示）: 失敗しても戻れる設計
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 原本保護・出力先分離・ログ記録をそろえると、GAS処理は怖くなくなる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 4 自動化処理
図解エリアの推奨構成: 読みやすい表またはカード型マトリクス。列見出しを明確にし、行数を絞って可読性を優先する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:戻れる設計の3点
| 観点 | 実装例 |
|---|---|
| 原本保護 | 読む中心。上書き列を限定する |
| 出力先分離 | 結果は別シートに作る |
| ログ記録 | 実行履歴・件数・エラーを追記する |
内容ブロック2:やってはいけない例
原本シートを毎回全クリアする
人のメモ列をGASが上書きする
エラーを握りつぶして成功扱いにする
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S24. 章見出し/現在位置 5/6: メニュー・エラー・権限

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 5/6 メニュー・エラー・権限
- 前の章: Excel的処理の自動化
- 次の章: 演習とレビュー
- 章の一言(画像内は小さく): ここから「5. メニュー・エラー・権限」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「5」「メニュー・エラー・権限」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「5 / 6」。
画面の隅に、前の章「Excel的処理の自動化」を小さく薄い色のチップとして添える(あれば)。
現在の章の右下または下に、次の章「演習とレビュー」を、現在の章よりはっきり小さいサイズで示す(あれば)。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S24」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「5. メニュー・エラー・権限」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は20分、対象スライド範囲はS24-S27。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S25. 現場担当者が押せるカスタムメニュー

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S25 / 30
- スライド見出し帯: 現場担当者が押せるカスタムメニュー
- 今回のまとめ帯（要点・結論・次のアクション）: Apps Scriptエディタを開かなくても使える入口を作ると、現場運用に近づく
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 5 メニュー
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 左右比較またはBefore/After表。左に課題・現状、右に改善後・設計後を置き、差分が一目で分かる構成。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:実行方法の比較
| 実行方法 | 向いている場面 | 注意 |
|---|---|---|
| エディタから実行 | 開発・テスト | 現場担当者には不向き |
| カスタムメニュー | 手動確認後に実行 | 押す前の確認手順が必要 |
| 時間トリガー | 定期集計 | 止まった時の通知が必要 |
| フォーム送信トリガー | 受付直後の処理 | 例外処理を厚くする |
内容ブロック2:メニュー名の例
`未請求一覧を更新`
`未対応一覧を更新`
`処理ログを確認`
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S25 / 30
スライド見出し帯（そのまま表示）: 現場担当者が押せるカスタムメニュー
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: Apps Scriptエディタを開かなくても使える入口を作ると、現場運用に近づく
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 5 メニュー
図解エリアの推奨構成: 左右比較またはBefore/After表。左に課題・現状、右に改善後・設計後を置き、差分が一目で分かる構成。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:実行方法の比較
| 実行方法 | 向いている場面 | 注意 |
|---|---|---|
| エディタから実行 | 開発・テスト | 現場担当者には不向き |
| カスタムメニュー | 手動確認後に実行 | 押す前の確認手順が必要 |
| 時間トリガー | 定期集計 | 止まった時の通知が必要 |
| フォーム送信トリガー | 受付直後の処理 | 例外処理を厚くする |
内容ブロック2:メニュー名の例
`未請求一覧を更新`
`未対応一覧を更新`
`処理ログを確認`
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S26. エラーは隠さず見える化する

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S26 / 30
- スライド見出し帯: エラーは隠さず見える化する
- 今回のまとめ帯（要点・結論・次のアクション）: try/catchとログを使い、失敗を成功のふりにしないことが運用の基本
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 5 メニュー
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:エラー分類
| 種類 | 例 | 対応 |
|---|---|---|
| 入力エラー | 必須列が空欄 | 対象外にしてログへ |
| 権限エラー | Drive/Gmailにアクセスできない | 管理者・権限を確認 |
| 形式エラー | setValuesの行列不一致 | 出力配列を確認 |
| 想定外エラー | 原因不明 | 処理名・行番号・メッセージを残す |
内容ブロック2:ログに残す文言
何の処理で
何件中何件成功し
何件が失敗し
次に誰が確認するか
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S26 / 30
スライド見出し帯（そのまま表示）: エラーは隠さず見える化する
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: try/catchとログを使い、失敗を成功のふりにしないことが運用の基本
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 5 メニュー
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:エラー分類
| 種類 | 例 | 対応 |
|---|---|---|
| 入力エラー | 必須列が空欄 | 対象外にしてログへ |
| 権限エラー | Drive/Gmailにアクセスできない | 管理者・権限を確認 |
| 形式エラー | setValuesの行列不一致 | 出力配列を確認 |
| 想定外エラー | 原因不明 | 処理名・行番号・メッセージを残す |
内容ブロック2:ログに残す文言
何の処理で
何件中何件成功し
何件が失敗し
次に誰が確認するか
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S27. テスト・権限・スコープ確認

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S27 / 30
- スライド見出し帯: テスト・権限・スコープ確認
- 今回のまとめ帯（要点・結論・次のアクション）: 初回実行前に正常系・異常系・権限を確認すると、録画後の再現性が上がる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 5 メニュー
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:テスト観点
| 観点 | 確認内容 |
|---|---|
| 正常系 | 未対応行が正しく抽出される |
| 0件 | 対象が0件でもエラーにならない |
| 空欄 | 必須列が空欄の行をどう扱うか |
| 重複 | 重複候補がログに出る |
| 権限 | Gmail/Drive/Sheetsの許可が通る |
内容ブロック2:公式確認メモ
GASの制限・権限・トリガー仕様は変わるため、導入時に最新の公式情報を確認する
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S27 / 30
スライド見出し帯（そのまま表示）: テスト・権限・スコープ確認
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: 初回実行前に正常系・異常系・権限を確認すると、録画後の再現性が上がる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 5 メニュー
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:テスト観点
| 観点 | 確認内容 |
|---|---|
| 正常系 | 未対応行が正しく抽出される |
| 0件 | 対象が0件でもエラーにならない |
| 空欄 | 必須列が空欄の行をどう扱うか |
| 重複 | 重複候補がログに出る |
| 権限 | Gmail/Drive/Sheetsの許可が通る |
内容ブロック2:公式確認メモ
GASの制限・権限・トリガー仕様は変わるため、導入時に最新の公式情報を確認する
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S28. 章見出し/現在位置 6/6: 演習とレビュー

- 種別: 章見出し/目次切り替え専用スライド(フルブリードのヒーロー表示。通常ワイヤーフレームは使わない)
- 現在の章: 6/6 演習とレビュー
- 前の章: メニュー・エラー・権限
- 次の章: なし
- 章の一言(画像内は小さく): ここから「6. 演習とレビュー」に入ります

- 画像プロンプト:

```text
16:9、1920x1080の法人研修スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。これは通常の情報密度が高いスライドではなく、章が切り替わる瞬間だけに使う「フルブリードのヒーロー表示」スライドである。
参照レイアウト画像(講座スライドレイアウトワイヤフレーム.png)にある帯構成・ヘッダー・フッター・目次ストリップ・表組みレイアウトは今回は使わない。画面いっぱいを使う、全く別の専用デザインにする。
背景は白またはごく薄いグラデーション地に、ネイビーとティールを基調とした大胆な幾何学アクセント(斜めの帯、太いライン、大きな数字の透かしなど)を1〜2箇所置き、法人研修らしい上質さを保つ。
画面の中央に、現在の章の番号と章名だけを画面の半分以上を占めるくらい極端に大きく、太いフォントで表示する: 「6」「演習とレビュー」。数字と章名はセットで、この1枚で最も目立つ主役にする。
現在の章の近くに、進捗を示す小さな表記を1つだけ添える: 「6 / 6」。
画面の隅に、前の章「メニュー・エラー・権限」を小さく薄い色のチップとして添える(あれば)。
最後の章のため、次の章の表示は省略し、代わりに「まとめへ」のような一言を小さく添えてもよい。
画面の隅の小さな文字で、講座タイトル「生成AI・GASで実践する業務変革・DX推進講座」とスライド番号「S28」だけを控えめに示してよいが、主役にはしない。
章の要点を伝える一文があれば、章名の下に小さめの一行で添えてよい: 「ここから「6. 演習とレビュー」に入ります」。
参考情報(画像内に大きくは出さない): この章の時間目安は20分、対象スライド範囲はS28-S30。
全体として、情報を詰め込まず、余白を大きく取り、章が変わったことが一瞬でわかる、雑誌の扉ページや基調講演のオープニングスライドのような、少ないが力強い要素構成にする。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換で作ったような見た目にしない。文字化け、誤字、読めない日本語、意味のないダミー文字、実在企業名、人物名、価格、連絡先、QRコード、透かし、架空ロゴ、架空UIを入れない。通常スライドのワイヤーフレーム（講座タイトル帯・S番号帯・目次ストリップ・表・カード・チェックリストなどの高密度レイアウト）を再現しない。小さい文字を大量に詰め込まない。情報カード、比較表、業種別リストなど通常スライドの密度要素を入れない。
```

## Slide S29. 演習: 自分の業務条件へ読み替える

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S29 / 30
- スライド見出し帯: 演習: 自分の業務条件へ読み替える
- 今回のまとめ帯（要点・結論・次のアクション）: シート名・列名・条件・出力先・ログを置き換えると、サンプルコードが自社業務のたたき台になる
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 6 演習
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:動画一時停止ワーク
動画を一時停止して、10分ほど取り組んでください。取り組めたら再生してください。
内容ブロック2:読み替える5項目
| 項目 | 記入例 |
|---|---|
| 入力シート | 問い合わせ台帳 |
| 対象条件 | ステータス=未対応 |
| 出力先 | 未対応一覧 |
| 更新列 | GAS更新日、処理済みフラグ |
| ログ項目 | 実行日時、処理件数、エラー数 |
内容ブロック3:自己レビュー
原本を壊していないか
人が確認する列を上書きしていないか
エラー時にログで追えるか
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S29 / 30
スライド見出し帯（そのまま表示）: 演習: 自分の業務条件へ読み替える
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: シート名・列名・条件・出力先・ログを置き換えると、サンプルコードが自社業務のたたき台になる
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 6 演習
図解エリアの推奨構成: 演習指示スライド。使うファイル、作業手順、完成物、自己レビュー基準を4つのカードで明示する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:動画一時停止ワーク
動画を一時停止して、10分ほど取り組んでください。取り組めたら再生してください。
内容ブロック2:読み替える5項目
| 項目 | 記入例 |
|---|---|
| 入力シート | 問い合わせ台帳 |
| 対象条件 | ステータス=未対応 |
| 出力先 | 未対応一覧 |
| 更新列 | GAS更新日、処理済みフラグ |
| ログ項目 | 実行日時、処理件数、エラー数 |
内容ブロック3:自己レビュー
原本を壊していないか
人が確認する列を上書きしていないか
エラー時にログで追えるか
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```

## Slide S30. 第3回まとめと次回準備

- 講座タイトル帯: 生成AI・GASで実践する業務変革・DX推進講座
- スライド番号/全体の枚数帯: S30 / 30
- スライド見出し帯: 第3回まとめと次回準備
- 今回のまとめ帯（要点・結論・次のアクション）: GASで機械的な処理を自動化したら、次はGemini/Gemで曖昧な文章を分類・要約する
- 目次見出しストリップのチップ文字（左から順に、この文字列だけを表示。記号や★は付けない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
- 目次見出しストリップでハイライト（塗りつぶし）するチップ: 6 演習
- 図解パターン: `high-density-structured-slide`
- 参照素材・スクリーンショット: なし
- 推奨レイアウト（図解エリア内）: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
- 審査向け密度: タイトルだけ、抽象アイコンだけ、短いラベルだけで終わらせない。本文ブロックを、読める表・カード・フロー・チェックリストとして図解エリア内に配置する。
- 図解エリアに必ず入れる内容:

```markdown
内容ブロック1:今日のまとめ
| 学んだこと | 持ち帰る成果物 |
|---|---|
| GASの役割分担 | 自動処理の対象範囲メモ |
| Sheets操作の基本 | SpreadsheetApp操作メモ |
| 抽出・転記・集計・ログ | 自動処理スクリプト |
| エラーと権限 | テスト観点・実行ルール |
内容ブロック2:次回への接続
第4回では、問い合わせ分類・日報要約・Meet文字起こし要約など、曖昧な文章処理をGemini/Gemで扱う
今日のログ・台帳設計は、AI出力を台帳へ戻すときの土台になる
```

- 画像プロンプト:

```text
16:9、1920x1080の法人研修用スライド画像を、Codex App Server / GPT image 2 / imagegenスキルで1枚の完成ラスター画像として生成する。白背景、ネイビー見出し、ブルー/ティール/ミントのアクセント、薄いグレー罫線、角丸カード、控えめな影、法人向けでフォーマルな情報設計にする。審査資料として、スライド単体で内容が理解できるように、タイトル、So What型ヘッドライン、本文カード、比較表、手順フロー、チェックリスト、業務例、成果物、確認観点を読みやすく配置する。講師コメントを読まなくても、何を学び、何を作り、どこを確認するかが分かる密度にする。
必ず添付の参照レイアウト画像（講座スライドレイアウトワイヤフレーム.png）を見て、その帯構成・比率・罫線をそのまま踏襲する。上段左に講座タイトル帯、上段右に「スライド番号 / 全体の枚数」帯、その下に横幅いっぱいのスライド見出し帯（枠線付き太字タイトル）、中央に大きな図解エリア（表・カード・フロー・チェックリストなどをここに密に配置する）、その下に「今回のまとめ」帯（左に見出しラベル、右に要点・結論・次のアクションの一文）、最下段に目次見出しストリップ（複数チップ、現在の章だけを塗りつぶしてハイライトし、他は白背景に薄い枠線）を配置する。ワイヤーフレーム画像の余白比率・帯の高さ・罫線の太さ・フォントの太さ感を再現し、そこに実際のテキスト・表・図を書き込む。
添付の参照レイアウト画像を使い、以下のテキストをその帯構成にそのまま流し込む。
講座タイトル帯（そのまま表示）: 生成AI・GASで実践する業務変革・DX推進講座
スライド番号/全体の枚数帯（そのまま表示）: S30 / 30
スライド見出し帯（そのまま表示）: 第3回まとめと次回準備
今回のまとめ帯（そのまま表示、右側に要点・結論・次のアクションとして）: GASで機械的な処理を自動化したら、次はGemini/Gemで曖昧な文章を分類・要約する
目次見出しストリップのチップ文字（左から順にこの文字列だけを表示、記号や★などの装飾は一切付け加えない）: 1 導入 / 2 JS最小限 / 3 Sheets操作 / 4 自動化処理 / 5 メニュー / 6 演習
目次見出しストリップで塗りつぶしてハイライトするチップはこれだけ（チップの文字自体は変えない）: 6 演習
図解エリアの推奨構成: リスク管理・チェックリスト型。注意点、確認者、禁止事項、代替手順をカードで整理する。
図解エリアには、以下のSOURCE TEXTを、読める表・カード・プロセス・チェックリストとして密に配置する。文言を勝手に変えたり、無関係な主張を足したりしない。
--- SOURCE TEXT START ---
内容ブロック1:今日のまとめ
| 学んだこと | 持ち帰る成果物 |
|---|---|
| GASの役割分担 | 自動処理の対象範囲メモ |
| Sheets操作の基本 | SpreadsheetApp操作メモ |
| 抽出・転記・集計・ログ | 自動処理スクリプト |
| エラーと権限 | テスト観点・実行ルール |
内容ブロック2:次回への接続
第4回では、問い合わせ分類・日報要約・Meet文字起こし要約など、曖昧な文章処理をGemini/Gemで扱う
今日のログ・台帳設計は、AI出力を台帳へ戻すときの土台になる
--- SOURCE TEXT END ---
参照素材・スクリーンショット: なし
講師コメントなしでも、何を学び、何を作り、どこを確認するかが分かる密度にする。フォーマルな法人研修トーンを維持する。
```

- ネガティブプロンプト:

```text
SVG、HTML/CSS、canvas、ブラウザスクリーンショット、ローカル変換、後載せテキスト合成で作ったような見た目にしない。文字化け、誤字、余計な文字、小さすぎる文字、読めない日本語、意味のないダミー文字、過度な長文詰め込み、参照画像なしでのGoogleロゴ再現、参照画像なしでの実在UI再現、架空のGoogle画面、架空のGoogleロゴ、実在企業名、人物名、メールアドレス、電話番号、価格、連絡先、QRコード、透かし、素材配置枠、公式ロゴという文字、空の破線枠、暗い背景、派手なグラデーション、装飾過多、漫画風、手書き風、雑然としたレイアウトを避ける。参照レイアウト画像にある帯・枠・比率を無視した独自レイアウトにしない。
```
