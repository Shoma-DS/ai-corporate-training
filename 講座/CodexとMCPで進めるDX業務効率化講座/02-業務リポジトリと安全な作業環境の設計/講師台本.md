# 第2回 講師台本: 業務リポジトリと安全な作業環境の設計

<!-- canva-url-list -->
> Canva版スライドURL一覧: `非公開/Canva/Canva_URL一覧.md`
> 実URLは公開リポジトリに貼らず、非公開ファイルで管理する。
<!-- /canva-url-list -->


## 準備物

- 第2回スライド S01-S40
- `ワークシート.md`
- `配布資料/演習ガイド.md`
- `演習データ/業務リポジトリ構成例.md`
- `演習データ/公開不可情報チェックサンプル.csv`
- `演習データ/AGENTSひな形.md`

講師メモ:
第2回は、Codexに作業させる前の安全な作業場づくりが主題。Gitやターミナルを深掘りしすぎず、境界、差分、承認、ダミーデータへ戻す。

スライド切替:
S01「業務リポジトリと安全な作業環境の設計」

読み上げ:
「第2回では、業務改善の材料をリポジトリとして整理します。Codexに作業を依頼する前に、どこを触ってよいか、どのデータを使うか、何をしてはいけないかを明確にします。」

スライド切替:
S06「標準フォルダ構成」

読み上げ:
「今回の標準構成は、docs、data/sample、scripts、outputs、tests、prompts、runbooksです。これは開発者向けの形というより、業務改善の証拠と手順を残すための棚です。」

画面共有 ── 実演1「業務リポジトリ構成例を見る」
⏱ 約5分

【手順1 – 約1分】
`演習データ/業務リポジトリ構成例.md` を開き、全体構成を表示する。

【手順2 – 約1分】
`docs/` には要件、設計、判断理由を置くと説明する。

【手順3 – 約1分】
`data/sample/` にはダミーデータだけを置き、実データは置かないと説明する。

【手順4 – 約1分】
`scripts/` と `tests/` は第3回でCodexが触る中心だと説明する。

【手順5 – 約1分】
`runbooks/` は本番化後の引き継ぎに必要だと説明する。

【見せるポイント】
リポジトリはコード置き場ではなく、業務改善の設計、検証、運用を残す場所である。

スライド切替:
S14「画面共有: AGENTSひな形を見る」

画面共有 ── 実演2「AGENTS.mdひな形を確認する」
⏱ 約4分

【手順1 – 約1分】
`演習データ/AGENTSひな形.md` を開く。

【手順2 – 約1分】
公開不可情報、秘密情報、実データ禁止の項目を確認する。

【手順3 – 約1分】
Codexに許可する作業、承認が必要な作業、禁止する作業を読み上げる。

【手順4 – 約1分】
第3回では、このひな形を自分の業務テーマに合わせて使うと説明する。

【見せるポイント】
AGENTS.mdはAIエージェント向けの作業手順書であり、業務上の安全ルールを明文化する。

スライド切替:
S15「ワーク1: フォルダ構成を書く」

ワーク指示:
「ここで動画を一時停止して、10分ほど取り組んでください。自分の改善テーマについて、docs、data/sample、scripts、outputs、tests、prompts、runbooksに何を置くかを書いてください。取り組めたら再生してください。」

スライド切替:
S19「画面共有: 公開不可チェック」

画面共有 ── 実演3「公開不可情報を判定する」
⏱ 約5分

【手順1 – 約1分】
`演習データ/公開不可情報チェックサンプル.csv` を開く。

【手順2 – 約1分】
顧客名、社員名、メールアドレス、契約番号、認証キーの例を確認する。

【手順3 – 約1分】
公開資料やAI入力に使ってよい形へ置き換える例を見せる。

【手順4 – 約1分】
社内で保管してよい情報と、公開教材・AI入力に入れない情報を分ける。

【手順5 – 約1分】
「迷ったら入れない。必要なら社内の管理者に確認する」と説明する。

【見せるポイント】
業務データを使う設計と、公開教材・AI入力に実データを入れることは別である。

スライド切替:
S21「ワーク2: ダミーデータ方針を書く」

ワーク指示:
「ここで動画を一時停止して、10分ほど取り組んでください。実データを使わずに業務に近い検証をするには、どの項目を架空化すればよいかを書いてください。取り組めたら再生してください。」

スライド切替:
S28「画面共有: 差分確認の流れ」

画面共有 ── 実演4「差分確認の流れを確認する」
⏱ 約4分

【手順1 – 約1分】
ダミー画面で `git status` 相当の状態を示し、変更があるファイルを確認する。

【手順2 – 約1分】
差分例を表示し、変更された行だけを見る意味を説明する。

【手順3 – 約1分】
Codexが意図しないファイルを変更していないか確認する観点を説明する。

【手順4 – 約1分】
承認が必要な操作は止める、確認してから進めると説明する。

【見せるポイント】
AIエージェントに任せても、変更の確認責任は人に残る。

スライド切替:
S31「ワーク3: 作業安全チェック」

ワーク指示:
「ここで動画を一時停止して、12分ほど取り組んでください。Codexへ作業を依頼する前に、使うデータ、触ってよいファイル、承認が必要な操作、検証方法をチェックリストにまとめてください。取り組めたら再生してください。」

スライド切替:
S40「次回予告」

読み上げ:
「第3回では、今日作った安全な作業環境を使って、Codexで小さなプロトタイプを作ります。重要なのは大きく作らないことです。1つの入力、1つの出力、検証できる条件から始めます。」
